---
phase: quick-260618-r2x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/format-uptime.utility.ts
  - src/renderer/src/components/format-uptime.utility.test.ts
  - src/renderer/src/components/debug-hud.tsx
autonomous: true
requirements: [QUICK-260618-r2x]
must_haves:
  truths:
    - "HUD status grid shows an 'Active panel' row reading 'AI' or 'Transcript' from status.activePanel"
    - "HUD shows a 'Session started' row with the overlay launch date+time, captured once and static"
    - "HUD shows an 'Uptime' row that ticks every second (MM:SS, or HH:MM:SS once past an hour)"
    - "The existing AI-panel corner pill is unchanged"
    - "formatUptime(ms) returns MM:SS under an hour and HH:MM:SS at/over an hour"
  artifacts:
    - path: "src/renderer/src/components/format-uptime.utility.ts"
      provides: "Pure ms-elapsed -> MM:SS / HH:MM:SS formatter"
      exports: ["formatUptime"]
    - path: "src/renderer/src/components/format-uptime.utility.test.ts"
      provides: "Unit tests for the uptime formatter"
    - path: "src/renderer/src/components/debug-hud.tsx"
      provides: "Three new status-grid rows: Active panel, Session started, Uptime"
  key_links:
    - from: "src/renderer/src/components/debug-hud.tsx"
      to: "src/renderer/src/components/format-uptime.utility.ts"
      via: "import { formatUptime }"
      pattern: "formatUptime"
    - from: "src/renderer/src/components/debug-hud.tsx"
      to: "status.activePanel"
      via: "render label from existing status state"
      pattern: "status\\.activePanel|activePanel"
---

<objective>
Add three rows to the Jedi Interviews HUD status grid in `debug-hud.tsx`:
1. **Active panel** — renders `AI` / `Transcript` from the already-arriving `status.activePanel` flag (renderer-only; the value is pushed over `window.jedi.onStatus`).
2. **Session started** — the overlay launch wall-clock date+time, captured once on mount, static.
3. **Uptime** — elapsed time since launch, ticking every second (MM:SS, widening to HH:MM:SS past an hour).

Purpose: give a glanceable session-health readout on the HUD without touching the audio/AI/IPC pipeline.
Output: a small pure uptime formatter + its test, plus the three new grid rows.

Scope guardrails:
- Renderer-only. NO main/preload/IPC change — `status.activePanel` already arrives via `window.jedi.onStatus` and is already consumed by `activePanelRef` in this file.
- The AI panel's existing corner pill (`ai-panel.tsx`, `data-testid="icon-active-panel"`) MUST stay untouched. This adds a *separate* HUD grid row; it does not replace or remove the pill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@src/renderer/src/components/debug-hud.tsx
@src/renderer/src/components/ai-panel.tsx

<interfaces>
<!-- Already declared in debug-hud.tsx; the executor consumes these, not re-declares them. -->

debug-hud.tsx already holds:
  const [status, setStatus] = useState<IOverlayStatus | null>(null);
  // IOverlayStatus.activePanel: 'transcript' | 'ai'  (pushed via window.jedi.onStatus)

ai-panel.tsx already establishes the 1s tick pattern to MIRROR (do NOT invent a new one):
  const [nowMs, setNowMs] = useState<number>(() => Date.now());
  // inside the empty-deps useEffect:
  const tick = window.setInterval(() => setNowMs(Date.now()), 1000);
  // cleanup: window.clearInterval(tick);

Test harness reality (load-bearing):
  vitest.config.ts → environment: 'node', include: ['src/**/*.test.ts']
  - Only *.test.ts runs (NOT *.test.tsx); node env has NO DOM.
  - Therefore: NO component/React-render test. The testable unit is the PURE formatter,
    which MUST live in its own .ts file so the .test.ts can import it without pulling JSX/React.
  - Mirror the existing renderer pattern: src/renderer/src/audio/rms.utility.ts + rms.utility.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure uptime formatter + test</name>
  <files>src/renderer/src/components/format-uptime.utility.ts, src/renderer/src/components/format-uptime.utility.test.ts</files>
  <behavior>
    formatUptime(elapsedMs: number): string
    - 0 ms -> "00:00"
    - 5_000 ms -> "00:05"
    - 65_000 ms -> "01:05"
    - 3_600_000 ms (exactly 1h) -> "01:00:00" (widens to HH:MM:SS at/over one hour)
    - 3_661_000 ms -> "01:01:01"
    - negative input clamps to 0 -> "00:00" (defensive: clock skew should never render a negative timer)
    - all MM and SS segments are zero-padded to 2 digits; the hours segment is shown only at/over 1h and zero-padded to 2 digits
  </behavior>
  <action>Create `format-uptime.utility.ts` exporting a pure function `formatUptime(elapsedMs: number): string`. Follow IDEXX standards: 4-space indent, single quotes, explicit return type, no side effects, no classes/state (this is a `.utility.ts` per naming-conventions — pure + idempotent). Clamp negative input to 0 with `Math.max(0, elapsedMs)`. Compute total whole seconds, then hours/minutes/seconds; build a `pad2` local helper that zero-pads to 2 digits. Below one hour return `MM:SS`; at/over one hour return `HH:MM:SS`. Add a TSDoc block explaining WHY native Math/Number is used here (presentation-only wall-clock formatting in the renderer — Luxon is reserved for main-process/business date logic per project standards). Do NOT import Luxon. Then create `format-uptime.utility.test.ts` mirroring `src/renderer/src/audio/rms.utility.test.ts`: `import { describe, expect, it } from 'vitest'`, AAA comments on their own lines (no trailing explanation), one assertion concern per `it`, covering every case in the behavior block.</action>
  <verify>
    <automated>npx vitest run src/renderer/src/components/format-uptime.utility.test.ts</automated>
  </verify>
  <done>All formatter tests pass; file is pure (no React/DOM imports, no Luxon, no side effects); MM:SS under 1h and HH:MM:SS at/over 1h confirmed by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add Active panel / Session started / Uptime rows to the HUD grid</name>
  <files>src/renderer/src/components/debug-hud.tsx</files>
  <action>Edit `debug-hud.tsx` only. (1) Add `import { formatUptime } from './format-uptime.utility';`. (2) Capture the launch instant ONCE: `const sessionStartRef = useRef<number>(Date.now());` (a ref so it is fixed for the component's life and never re-derived on render). (3) Add a 1s tick mirroring `ai-panel.tsx`: `const [nowMs, setNowMs] = useState<number>(() => Date.now());` and, inside the EXISTING empty-deps `useEffect` (the one wiring onStatus/onTranscript/onScrollTranscript), add `const tick = window.setInterval(() => setNowMs(Date.now()), 1000);` and add `window.clearInterval(tick);` to that effect's existing cleanup return. Do NOT add a second useEffect. (4) Derive labels alongside the other `*Label` consts: `activePanelLabel = status ? (status.activePanel === 'ai' ? 'AI' : 'Transcript') : '—'`; `sessionStartedLabel = new Date(sessionStartRef.current).toLocaleString()` (native Date is the deliberate choice for renderer wall-clock display — presentation, not business logic; Luxon stays in main per standards); `uptimeLabel = formatUptime(nowMs - sessionStartRef.current)`. (5) In the FIRST `<dl className="debug-hud__grid">` (the status grid, NOT the cheat-sheet `dl`), append three new `<dt>/<dd>` pairs after the existing 'Audio' row, matching the existing cell markup exactly: keys 'Active panel', 'Session started', 'Uptime' with `data-testid="cell-active-panel"`, `cell-session-started`, `cell-uptime` respectively. Keep `ai-panel.tsx` and its corner pill untouched.</action>
  <verify>
    <automated>npm run typecheck:web && npm run format:check src/renderer/src/components/debug-hud.tsx</automated>
  </verify>
  <done>typecheck:web passes; the status `dl` has three new rows with `cell-active-panel`/`cell-session-started`/`cell-uptime` testids; uptime uses `formatUptime` against `nowMs - sessionStartRef.current`; session-started rendered once from the mount ref; only ONE interval added inside the existing effect with matching cleanup; ai-panel.tsx unchanged.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/renderer/src/components/format-uptime.utility.test.ts` — formatter green.
- `npm run typecheck:web` — no type errors in the renderer bundle.
- `npm run format:check` — formatting clean (run `npm run format` if it flags).
- Manual sanity (optional, `npm run dev`): HUD shows Active panel (flips with Ctrl+Alt+F), a static Session started timestamp, and an Uptime that increments each second; AI-panel corner pill still present and unchanged.
</verification>

<success_criteria>
- Three new rows render in the HUD status grid with the specified testids.
- Active panel reflects `status.activePanel` live; Session started is static from mount; Uptime ticks every second and widens to HH:MM:SS past an hour.
- No IPC/main/preload changes; ai-panel.tsx corner pill untouched.
- Uptime formatter is a pure, tested `.utility.ts`.
</success_criteria>

<output>
Create `.planning/quick/260618-r2x-add-active-panel-session-started-uptime-/260618-r2x-SUMMARY.md` when done.
</output>
