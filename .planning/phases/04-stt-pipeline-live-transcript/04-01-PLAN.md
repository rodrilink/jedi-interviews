---
phase: 04-stt-pipeline-live-transcript
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - src/main/audio/pcm-resample.utility.ts
  - src/main/audio/pcm-resample.utility.test.ts
  - src/main/stt/stt-provider.interface.ts
autonomous: false
requirements: [TRN-01, TRN-05]
user_setup:
  - service: deepgram
    why: "Live streaming speech-to-text; required to demonstrate a live transcript (success criterion 1). Not needed for the capture go/no-go gate in this plan (RMS-only)."
    env_vars:
      - name: DEEPGRAM_API_KEY
        source: "Deepgram Console -> API Keys (https://console.deepgram.com/) -> create key"

must_haves:
  truths:
    - "The chosen native capture package loads in the Electron 35.7.5 main process and captures non-silent system-audio (loopback) PCM on this machine, proven by a human go/no-go run before any pipeline is built on it."
    - "A pure, unit-tested utility converts device-native PCM (e.g. 48 kHz stereo 16-bit LE) to 16 kHz mono Int16, and fails loudly when the declared input rate does not equal the device's actual rate."
    - "An ISttProvider seam exists that consumers depend on instead of Deepgram directly (start/stop/sendAudio + typed transcript / connection-state-change / error events)."
    - "D-01: in-main native capture addon — the go/no-go gate proves the addon loads and captures in-process (no child process). D-02: down-mix/resample to 16 kHz mono Int16 lives in a pure, unit-testable main-process utility. D-03: the native-module risk is de-risked FIRST via a human go/no-go before any pipeline is built on it. D-08: any local dev-config file holding the Deepgram key is gitignored now so the key is never committed."
  artifacts:
    - path: "src/main/stt/stt-provider.interface.ts"
      provides: "ISttProvider + ISttTranscriptEvent + SttConnectionState contract (TRN-05)"
      contains: "interface ISttProvider"
    - path: "src/main/audio/pcm-resample.utility.ts"
      provides: "downmixToMonoFloat32, resampleLinear, float32ToInt16, assertSampleRate"
      exports: ["downmixToMonoFloat32", "resampleLinear", "float32ToInt16", "assertSampleRate"]
    - path: "src/main/audio/pcm-resample.utility.test.ts"
      provides: "Unit coverage for downmix, resample, conversion, and rate-mismatch throw"
      contains: "describe"
  key_links:
    - from: "package.json"
      to: "native-recorder-nodejs prebuilt win32-x64 binary"
      via: "exact-pinned dependency + committed lockfile"
      pattern: "native-recorder-nodejs"
    - from: "consumers (later plans)"
      to: "ISttProvider"
      via: "interface import (never @deepgram/sdk directly)"
      pattern: "ISttProvider"
---

<objective>
De-risk the single biggest unknown in this phase FIRST — prove the chosen native WASAPI capture package loads and captures non-silent system-audio PCM on this exact machine (Electron 35.7.5) — then define the two contracts every downstream plan builds on: the `ISttProvider` seam (TRN-05) and the pure `pcm-resample.utility.ts` (TRN-01).

Purpose: Phase 3 proved every Chromium `getDisplayMedia` capture path is silent on this machine (DXGI duplicator failure). The whole Phase 4 architecture is forced into the main process around a young native package (`native-recorder-nodejs`). Per the Phase 2 uiohook lesson, the native module is gated by a human go/no-go BEFORE anything is built on it. The interface-first contracts let plans 02/03/04 implement against blueprints, not a scavenger hunt.

Output: A registry-pinned native capture dependency, a recorded capture go/no-go decision, the `ISttProvider` interface file, and the unit-tested resample utility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stt-pipeline-live-transcript/04-CONTEXT.md
@.planning/phases/04-stt-pipeline-live-transcript/04-RESEARCH.md
@.planning/phases/04-stt-pipeline-live-transcript/04-PATTERNS.md
@.planning/phases/04-stt-pipeline-live-transcript/04-VALIDATION.md
@.planning/phases/03-audio-loopback-spike/03-LOOPBACK-GATE.md

<interfaces>
<!-- Local analogs the executor copies structure from. Use these directly; do not re-explore. -->

From src/renderer/src/audio/rms.utility.ts (the EXACT pure-utility shape to copy for pcm-resample.utility.ts):
- `export function computeRms(frame: Float32Array): number` — explicit return type, edge-case-first guard (`if (frame.length === 0) return 0;`), 4-space indent, named `index` loop var, single quotes, TSDoc with @param/@returns.

From src/main/hotkey-registrar.service.ts (the local interface-naming + by-convention-singleton convention to copy for stt-provider.interface.ts):
- `export interface IHotkeyRegistrationResult { active: HotkeyLayer; failed: string[]; }` — I-prefixed PascalCase interfaces, TSDoc on every member.
- `export type HotkeyLayer = 'uiohook' | 'globalShortcut' | 'none';` — string-union `type` precedent for `SttConnectionState`.

ISttProvider target shape (RESEARCH 04-RESEARCH.md lines 431-444, VERIFIED against Deepgram v5 event model):
```typescript
export interface ISttTranscriptEvent { text: string; isFinal: boolean; }
export type SttConnectionState = 'connecting' | 'connected' | 'reconnecting' | 'disconnected' | 'error';
export interface ISttProvider {
    start(): Promise<void>;
    stop(): Promise<void>;
    sendAudio(pcm: Int16Array): void;
    on(event: 'transcript', listener: (e: ISttTranscriptEvent) => void): void;
    on(event: 'connection-state-change', listener: (state: SttConnectionState) => void): void;
    on(event: 'error', listener: (error: Error) => void): void;
}
```

native-recorder-nodejs@1.2.0 API (VERIFIED against installed dist/index.d.ts, RESEARCH lines 408-429):
- `AudioRecorder.getDevices('output')` -> array of `{ id, isDefault, ... }` WASAPI output endpoints.
- `AudioRecorder.getDeviceFormat(id)` -> `{ sampleRate, channels, bitDepth, rawBitDepth }` (the ACTUAL device format).
- `new AudioRecorder()` is an EventEmitter: `.on('data', (buffer: Buffer) => ...)` emits 16-bit LE PCM at device-native rate; `.on('error', (error: Error) => ...)`; `await recorder.start({ deviceType: 'output', deviceId })`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install native-recorder-nodejs and prove on-machine loopback capture (OQ-1 go/no-go gate)</name>
  <files>package.json, package-lock.json, .gitignore</files>
  <read_first>
    - .planning/phases/04-stt-pipeline-live-transcript/04-RESEARCH.md (Open Question OQ-1 lines 474-477; Package Legitimacy Audit lines 108-120; Pitfall 1 lines 376-380; Pitfall 4 lines 394-398)
    - .planning/phases/03-audio-loopback-spike/03-LOOPBACK-GATE.md (WHY the Chromium path is dead and WHY this gate matters)
    - src/main/hotkey-registrar.service.ts (the Phase 2 native-module precedent: prebuilt N-API binary loaded under Electron 35.7.5 where from-source failed)
  </read_first>
  <action>
    Install the capture dependency exactly pinned and commit the lockfile: `npm install native-recorder-nodejs@1.2.0` (no `@electron/rebuild` step — this package ships a prebuilt win32-x64 N-API binary and is ABI-stable across Electron versions; contrast uiohook-napi which required a rebuild). After install, confirm the prebuilt binary is present at `node_modules/native-recorder-nodejs/prebuilds/win32-x64/NativeAudioSDK.node`. Also install the Deepgram SDK now so it is committed in the same lockfile: `npm install @deepgram/sdk@5.4.0` (pure JS, no native build).

    Then run a MINIMAL throwaway capture spike from the Electron MAIN process under the pinned Electron 35.7.5: load `AudioRecorder` from `native-recorder-nodejs`, call `AudioRecorder.getDevices('output')`, pick `find((d) => d.isDefault) ?? outputs[0]`, call `AudioRecorder.getDeviceFormat(device.id)` and log `{ sampleRate, channels, bitDepth }`, then `new AudioRecorder()`, attach `.on('data', buffer => ...)` that computes a quick RMS over the 16-bit LE samples (reuse the math in src/renderer/src/audio/rms.utility.ts), and `await recorder.start({ deviceType: 'output', deviceId: device.id })`. Print the rolling RMS to the console. NEVER `console.log` any secret (no Deepgram key is needed for this gate — it is RMS-only per RESEARCH line 499). This spike code is throwaway scaffolding — it may live in a temporary scratch entry or a guarded branch in index.ts that you remove before plan completion; it MUST NOT remain in the committed bootstrap (04-04 wires the real pipeline).

    Guard the spike defensively per WR-01: if `getDevices('output')` returns an empty array, surface a clear message instead of indexing `outputs[0]`. Wrap `start()` in try/catch (JS-level errors only; a native segfault cannot be caught — that is the D-01 accepted risk, mitigated by this gate).

    GO criterion: the addon loads in-process under Electron 35.7.5 AND, while system audio plays (music/video/Teams test call), the printed RMS shows a clearly non-zero signal (above the silent floor) that drops toward zero when audio stops.

    NO-GO fallback: if the addon fails to load, or captures only silence/mic, STOP. Record NO-GO and switch to the `audiotee` (npm 0.0.7) child-process sidecar as the capture mechanism (accepting the D-01 in-process deviation and the extra PCM-framing plumbing) — this changes plan 04-04's wiring but not the ISttProvider seam or the resample utility. Do NOT build the rest of the pipeline until this gate is GO (or the fallback is confirmed working).

    Add any local dev-config file used to hold a Deepgram key later (D-08) to `.gitignore` now (e.g. a `.env`/`*.local` entry); confirm no key is committed.
  </action>
  <what-built>
    native-recorder-nodejs@1.2.0 + @deepgram/sdk@5.4.0 installed and pinned with a committed lockfile; the prebuilt win32-x64 N-API binary verified present; a throwaway main-process spike that enumerates the default output device, asserts/prints its actual format, captures loopback PCM, and prints rolling RMS.
  </what-built>
  <how-to-verify>
    1. Run `ls node_modules/native-recorder-nodejs/prebuilds/win32-x64/NativeAudioSDK.node` — file exists.
    2. Launch the app/spike under the pinned Electron 35.7.5 (`npm run dev` or the spike entry).
    3. Play system audio (a YouTube video, music, or a Teams test call). Watch the printed RMS: it should rise clearly above the silent floor while audio plays and fall toward zero when it stops.
    4. Confirm the printed device format (sampleRate/channels) so the resample utility (Task 3) and the runtime assertion know the real input rate.
    5. Confirm NO Deepgram key (or any secret) is printed anywhere.
  </how-to-verify>
  <acceptance_criteria>
    - `node_modules/native-recorder-nodejs/prebuilds/win32-x64/NativeAudioSDK.node` exists (no MSVC/node-gyp build occurred; no `gyp ERR!`/`cl.exe` in install output).
    - `package.json` pins `native-recorder-nodejs@1.2.0` and `@deepgram/sdk@5.4.0` exactly; `package-lock.json` is committed.
    - Human confirms: addon loads in-process under Electron 35.7.5 AND non-silent loopback PCM is captured while system audio plays (RMS clearly non-zero, drops on silence). A short GO/NO-GO note is recorded.
    - If NO-GO: the `audiotee` sidecar fallback decision is recorded and the downstream wiring plan (04-04) is flagged to adopt it; the seam and resample utility (Tasks 2-3) proceed unchanged.
    - `.gitignore` covers any local dev-config/env file intended to hold the Deepgram key; no secret committed.
  </acceptance_criteria>
  <resume-signal>Type "GO" (capture works on this machine) and the observed device format, or "NO-GO" with the failure detail (to trigger the audiotee fallback).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Define the ISttProvider seam (TRN-05, define FIRST)</name>
  <files>src/main/stt/stt-provider.interface.ts</files>
  <read_first>
    - src/main/hotkey-registrar.service.ts (lines 1-22, 75-78 — I-prefixed interface naming, string-union `type`, the by-convention-singleton @remarks note to echo)
    - .planning/phases/04-stt-pipeline-live-transcript/04-RESEARCH.md (ISttProvider shape lines 431-444; Architectural Responsibility Map lines 61-72)
    - .planning/phases/04-stt-pipeline-live-transcript/04-PATTERNS.md (stt-provider.interface.ts assignment lines 51-58)
  </read_first>
  <action>
    Create `src/main/stt/stt-provider.interface.ts` containing exactly the contract in the `<interfaces>` block: `export interface ISttTranscriptEvent { text: string; isFinal: boolean; }`, `export type SttConnectionState = 'connecting' | 'connected' | 'reconnecting' | 'disconnected' | 'error';`, and `export interface ISttProvider` with `start(): Promise<void>`, `stop(): Promise<void>`, `sendAudio(pcm: Int16Array): void`, and the three overloaded `on(event, listener)` signatures for `'transcript'`, `'connection-state-change'`, and `'error'`. No implementation, no Deepgram import — this is a pure contract file (TRN-05). Add TSDoc on the interface and every member per IDEXX standards (explicit return types, single quotes, 4-space). Document in a file-level TSDoc that consumers (the buffer feed in 04-03, the wiring in 04-04, and Phase 5's AI orchestrator) depend on THIS interface so the Deepgram backend can be swapped for Whisper (v2) without touching them.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/stt/stt-provider.interface.ts` exists and exports `ISttProvider`, `ISttTranscriptEvent`, `SttConnectionState`.
    - Source assertion: file contains `interface ISttProvider`, `sendAudio(pcm: Int16Array)`, and the three `on(` overloads for `'transcript'`/`'connection-state-change'`/`'error'`.
    - File imports nothing from `@deepgram/sdk` (grep for `@deepgram` returns no match in this file).
    - TypeScript compiles with no errors.
  </acceptance_criteria>
  <done>The ISttProvider contract is defined, Deepgram-agnostic, and ready for DeepgramSttGateway (04-02) to implement and for consumers to depend on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure PCM down-mix + resample utility with loud rate assertion (TRN-01)</name>
  <files>src/main/audio/pcm-resample.utility.ts, src/main/audio/pcm-resample.utility.test.ts</files>
  <read_first>
    - src/renderer/src/audio/rms.utility.ts + src/renderer/src/audio/rms.utility.test.ts (the EXACT .utility.ts + co-located Vitest style to mirror — AAA comments on their own lines, `toBeCloseTo` for floats, typed arrange objects)
    - .planning/phases/04-stt-pipeline-live-transcript/04-RESEARCH.md (Pattern 2 bodies lines 233-282; the rate-assertion note lines 282-285; Pitfall 2 lines 382-386)
    - .planning/phases/04-stt-pipeline-live-transcript/04-VALIDATION.md (Per-Task Verification Map: resample downmix + rate assertion rows; Wave 0 stub requirement)
  </read_first>
  <behavior>
    - downmixToMonoFloat32(stereoBuffer, 2): a 4-frame interleaved Int16-LE stereo Buffer yields a 4-sample Float32Array in [-1, 1] equal to the per-frame channel average.
    - downmixToMonoFloat32 on an empty Buffer returns an empty Float32Array (edge-case guard).
    - resampleLinear(input, 48000, 16000): output length is floor(input.length / 3); resampleLinear(input, 16000, 16000) returns the input unchanged (passthrough).
    - float32ToInt16([1, -1, 0]) -> Int16Array [32767, -32768, 0] (asymmetric scaling; clamps out-of-range to [-1,1] first).
    - assertSampleRate(declaredRate, actualRate): no-op when equal; throws a descriptive Error when declaredRate !== actualRate (loud fail per the ROADMAP "declared == actual" rule).
  </behavior>
  <action>
    First create the Wave 0 test stub `src/main/audio/pcm-resample.utility.test.ts` expressing the behaviors above (RED), then implement `src/main/audio/pcm-resample.utility.ts` to make them pass (GREEN). Implement four exported pure functions exactly as the RESEARCH Pattern 2 bodies specify: `downmixToMonoFloat32(pcm: Buffer, channels: number): Float32Array` (read interleaved 16-bit LE via `pcm.readInt16LE`, normalize `/ 32768`, average channels), `resampleLinear(input: Float32Array, inRate: number, outRate: number): Float32Array` (linear interpolation; passthrough when `inRate === outRate`), `float32ToInt16(input: Float32Array): Int16Array` (clamp to [-1,1] then scale by 32768 for negatives / 32767 for non-negatives), and `assertSampleRate(declaredRate: number, actualRate: number): void` that throws a plain `Error` with a descriptive message when the rates differ (the established local precedent for failing loudly on a broken invariant is the plain `Error` thrown in preload/index.ts:73 — no ipay-common-lib in this local app). Pure, idempotent, no side effects, no classes/state, explicit return types, TSDoc with @param/@returns on every export, edge-case-first guards. These functions consume the device-native format proven in Task 1 and produce the 16 kHz mono Int16 wire format Deepgram requires (`encoding: 'linear16', sample_rate: 16000`).
  </action>
  <verify>
    <automated>npx vitest run src/main/audio/pcm-resample.utility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/audio/pcm-resample.utility.test.ts` passes; tests cover downmix (incl. empty-buffer guard), resample (48k->16k length + 16k->16k passthrough), float32ToInt16 conversion, and the rate-mismatch throw.
    - `src/main/audio/pcm-resample.utility.ts` exports `downmixToMonoFloat32`, `resampleLinear`, `float32ToInt16`, `assertSampleRate`.
    - Source assertion: `assertSampleRate` throws on `declaredRate !== actualRate` (covered by a dedicated `expect(() => ...).toThrow()` test).
    - No side effects / no classes in the utility (pure functions only).
  </acceptance_criteria>
  <done>The conversion from device-native PCM to 16 kHz mono Int16 is implemented and unit-tested, and a mismatch between the declared and actual device sample rate fails loudly instead of producing a silently garbled transcript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> in-process native addon | A young, low-download native package executes in the Electron main process |
| Deepgram API key -> process | Secret enters the process from env/dev-config (key is sourced/used in 04-02; this plan only installs the SDK and gitignores the dev-config) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering/Elevation | native-recorder-nodejs in-process addon (supply-chain, young package) | mitigate | slopcheck [OK] + exact version pin + committed lockfile; binary is bundled in-tarball (no install-time network fetch); blocking human capture go/no-go gate (Task 1) validates it loads and captures before anything is built on it |
| T-4-02 | Information Disclosure | Deepgram API key in dev-config | mitigate | `.gitignore` covers the dev-config/env file in this plan; key is read in main only (04-02); never logged, never crosses IPC; the throwaway spike is RMS-only and uses no key |
| T-4-SC | Tampering | npm installs (`native-recorder-nodejs`, `@deepgram/sdk`) | mitigate | Both registry-verified this session; `@deepgram/sdk` is mature/official; `native-recorder-nodejs` is [ASSUMED] and gated by the blocking human-verify checkpoint (Task 1) — never auto-approvable |
</threat_model>

<verification>
- `npm install` completes with NO `node-gyp`/`cl.exe`/`gyp ERR!` output and the prebuilt `.node` binary is present.
- Human-verified GO: non-silent loopback PCM captured in-process under Electron 35.7.5.
- `npx vitest run src/main/audio/pcm-resample.utility.test.ts` green.
- `npx tsc --noEmit` clean for the new interface + utility.
- No Deepgram key (or any secret) committed or logged.
</verification>

<success_criteria>
- Capture go/no-go decision recorded (GO with observed device format, or NO-GO -> audiotee fallback).
- `ISttProvider`/`ISttTranscriptEvent`/`SttConnectionState` contract exists and is Deepgram-agnostic (TRN-05).
- Resample utility + rate assertion implemented and unit-tested (TRN-01).
- Dependencies pinned, lockfile committed, dev-config gitignored.
</success_criteria>

<output>
Create `.planning/phases/04-stt-pipeline-live-transcript/04-01-SUMMARY.md` when done. Record the capture go/no-go decision (GO/NO-GO), the observed device sample rate + channel count, and whether the audiotee fallback was triggered (this directly affects 04-04 wiring).
</output>
