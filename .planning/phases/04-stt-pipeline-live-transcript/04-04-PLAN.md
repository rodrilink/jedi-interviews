---
phase: 04-stt-pipeline-live-transcript
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/main/audio/audio-capture.service.ts
  - src/main/index.ts
  - src/main/overlay-window.manager.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/src/components/debug-hud.tsx
  - electron.vite.config.ts
  - src/renderer/src/services/audio-capture.service.ts
  - src/renderer/src/audio/rms-meter.worklet.ts
autonomous: false
requirements: [TRN-01, TRN-02, TRN-03, TRN-04]

must_haves:
  truths:
    - "Captured system audio flows main-side capture -> resample -> DeepgramSttGateway -> TranscriptBuffer -> a one-way jedi:transcript push, and a live rolling transcript renders in the DebugHud."
    - "Interim transcript text renders visually distinct (dimmed/italic) from finalized text."
    - "Connection state is visible on the overlay; a dropped connection reconnects automatically and the state reflects it."
    - "The clear-transcript hotkey wipes the buffer and the overlay reflects the empty buffer."
    - "The dead renderer audio path is removed: audio-capture.service.ts, rms-meter.worklet.ts, the worklet rollup entry, setDisplayMediaRequestHandler/desktopCapturer, and the jedi:audio-level channel/handler/preload method are gone."
    - "onStatus and onTranscript return an unsubscribe fn that DebugHud calls on cleanup (no leaked listeners under Strict Mode)."
  artifacts:
    - path: "src/main/audio/audio-capture.service.ts"
      provides: "Main-process AudioRecorder wrapper: idempotent start(), empty-device guard, data->resample->sendAudio, teardown()"
      contains: "class AudioCaptureService"
    - path: "src/main/index.ts"
      provides: "Bootstrap wiring capture->gateway->buffer->pushTranscript; clear-transcript handler; teardown; dead-code removed"
      contains: "pushTranscript"
    - path: "src/main/overlay-window.manager.ts"
      provides: "TRANSCRIPT_CHANNEL + IOverlayTranscript + pushTranscript; jedi:audio-level state removed (or re-sourced)"
      contains: "TRANSCRIPT_CHANNEL"
    - path: "src/renderer/src/components/debug-hud.tsx"
      provides: "Transcript + connection-state rows; interim dimmed/italic; onStatus/onTranscript unsubscribe cleanup"
      contains: "onTranscript"
  key_links:
    - from: "src/main/index.ts"
      to: "AudioCaptureService -> DeepgramSttGateway -> TranscriptBuffer -> pushTranscript"
      via: "wired once in app.whenReady"
      pattern: "pushTranscript"
    - from: "src/renderer/src/components/debug-hud.tsx"
      to: "window.jedi.onTranscript"
      via: "one-way main->renderer subscription with unsubscribe cleanup"
      pattern: "onTranscript"
    - from: "src/preload/index.ts"
      to: "jedi:transcript channel"
      via: "ipcRenderer.on returning removeListener"
      pattern: "jedi:transcript"
---

<objective>
Wire the whole pipeline end-to-end and make the transcript appear on the overlay: main-process capture -> resample -> `DeepgramSttGateway` -> `TranscriptBuffer` -> a new one-way `jedi:transcript` push -> `DebugHud` render (interim distinct from final, connection state visible). Retire the dead renderer audio path (D-02/IN-01), fix the leaked status listener (WR-03), and confirm the live transcript + auto-reconnect on this machine.

Purpose: This is the integration that delivers the phase's user-visible value (TRN-01/02/03/04). It depends on the seam (04-01), gateway (04-02), and buffer + clear chord (04-03), and it cleans up the now-orphaned Chromium/renderer-audio code that the Phase 3 NO-GO left behind.

Output: A live transcript on the overlay, a clean one-way main->renderer boundary (zero renderer->main write channels), and recorded manual verifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stt-pipeline-live-transcript/04-CONTEXT.md
@.planning/phases/04-stt-pipeline-live-transcript/04-RESEARCH.md
@.planning/phases/04-stt-pipeline-live-transcript/04-PATTERNS.md
@.planning/phases/04-stt-pipeline-live-transcript/04-VALIDATION.md
@src/main/stt/stt-provider.interface.ts
@src/main/index.ts
@src/main/overlay-window.manager.ts
@src/preload/index.ts
@src/renderer/src/components/debug-hud.tsx

<interfaces>
<!-- Everything the wiring touches. Use directly; no exploration. -->

native-recorder-nodejs@1.2.0 (VERIFIED; RESEARCH lines 408-429) — wrapped in the new main AudioCaptureService:
- `AudioRecorder.getDevices('output')` -> `{ id, isDefault, ... }[]`.
- `AudioRecorder.getDeviceFormat(id)` -> `{ sampleRate, channels, bitDepth, rawBitDepth }`.
- `new AudioRecorder()` EventEmitter: `.on('data', (buffer: Buffer) => ...)` (16-bit LE PCM, device rate), `.on('error', (error: Error) => ...)`, `await recorder.start({ deviceType: 'output', deviceId })`.
- NOTE: if 04-01 recorded NO-GO, substitute the `audiotee` child-process sidecar here per that SUMMARY; the gateway/buffer/render wiring is unchanged.

From 04-01 — src/main/audio/pcm-resample.utility.ts:
- `downmixToMonoFloat32(pcm: Buffer, channels: number): Float32Array`, `resampleLinear(input, inRate, outRate): Float32Array`, `float32ToInt16(input): Int16Array`, `assertSampleRate(declared, actual): void`.

From 04-01/04-02 — src/main/stt/stt-provider.interface.ts + deepgram-stt.gateway.ts:
- `ISttProvider`: start/stop/sendAudio(Int16Array)/on('transcript'|'connection-state-change'|'error').
- `ISttTranscriptEvent { text; isFinal }`, `SttConnectionState`.

From 04-03 — src/main/stt/transcript-buffer.ts:
- `new TranscriptBuffer()`; `appendFinal(text)`, `setInterim(text)`, `clear()`, `renderable(): { finalText; interimText }`.
- HOTKEY_CHORDS now includes `clear-transcript` (Ctrl+Alt+K) — index.ts buildHandlers must add a handler for it.

Existing overlay push triad to mirror for transcript (src/main/overlay-window.manager.ts):
- `export const STATUS_CHANNEL = 'jedi:status';` (line 39) -> add sibling `TRANSCRIPT_CHANNEL = 'jedi:transcript'`.
- `pushStatus(window)` (lines 163-169) with the `isDestroyed()`/`webContents.isDestroyed()` guard -> mirror as `pushTranscript(window, payload)`.
- `IOverlayStatus` (lines 11-36) currently has `audioLevel` (line 35), `lastAudioLevel` (line 70), `setAudioLevel` (lines 79-81), `audioLevel:` in buildStatus (line 153) — REMOVE these (IN-01) unless the OQ-3 main meter is kept.

Existing preload to extend (src/preload/index.ts):
- `onStatus` (lines 49-51) currently registers an inline listener with NO removal (WR-03 bug) -> fix to return an unsubscribe fn.
- `reportAudioLevel` (lines 53-64) + `AUDIO_LEVEL_CHANNEL` (line 26) -> REMOVE (IN-01); shrinks the renderer->main write surface to zero.

Existing DebugHud to extend (src/renderer/src/components/debug-hud.tsx):
- `useEffect` (lines 71-73) subscribes to onStatus with no cleanup -> WR-03: capture unsubscribe fns and call them in the returned cleanup.
- `formatAudioMeter`/`AUDIO_METER_CELLS` (lines 20-37) + the `Audio` row (lines 108-111) + `audioLevel` field (line 17) -> REMOVE (IN-01) unless OQ-3 meter kept.
- Add transcript rows after the `dl.debug-hud__grid` with data-testids `cell-transcript-final`, `cell-transcript-interim`, `cell-connection-state`; interim rendered with a `debug-hud__interim` dimmed/italic class (D-04). Visibility already coupled to `hudVisible` (D-05) — no extra work.

Dead code to delete (04-PATTERNS.md "Retired / Dead Code" lines 177-189):
- DELETE `src/renderer/src/services/audio-capture.service.ts`, `src/renderer/src/audio/rms-meter.worklet.ts`.
- Remove the `rms-meter.worklet` rollup input + its `entryFileNames` branch in electron.vite.config.ts (lines 49-54).
- Remove `installAudioPipeline`, `setDisplayMediaRequestHandler`, `desktopCapturer`, `ipcMain.on('jedi:audio-level')` from src/main/index.ts (lines 1, 44-84, 109-112).
- KEEP src/renderer/src/audio/rms.utility.ts (reused by the optional OQ-3 main meter).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Main-process AudioCaptureService + retire the dead renderer audio path (D-02, WR-01/WR-02, IN-01)</name>
  <files>src/main/audio/audio-capture.service.ts, electron.vite.config.ts, src/renderer/src/services/audio-capture.service.ts (delete), src/renderer/src/audio/rms-meter.worklet.ts (delete)</files>
  <read_first>
    - src/main/index.ts (lines 1-3, 44-84, 106-138 — what installAudioPipeline/setDisplayMediaRequestHandler/jedi:audio-level looks like before removal)
    - src/main/hotkey-registrar.service.ts (lines 75-78 by-convention-singleton @remarks; lines 107-117 no-throw report; lines 123-133 teardown — the wrapper discipline to copy)
    - src/renderer/src/services/audio-capture.service.ts (the retired lifecycle shape: stored handles to carry forward, MINUS the missing idempotent guard)
    - .planning/phases/04-stt-pipeline-live-transcript/04-RESEARCH.md (Code Examples lines 408-429; Pitfall 4 in-process crash lines 394-398; Pitfall 5 idempotent start lines 400-404)
    - .planning/phases/04-stt-pipeline-live-transcript/04-PATTERNS.md (audio-capture.service.ts assignment lines 77-86; Retired/Dead Code table lines 177-189)
    - .planning/phases/04-01-SUMMARY.md (the observed device format + GO/NO-GO; if NO-GO, use the audiotee sidecar instead of native-recorder-nodejs)
  </read_first>
  <action>
    Create `src/main/audio/audio-capture.service.ts` exporting `class AudioCaptureService` that wraps `native-recorder-nodejs` `AudioRecorder` in the main process (or the `audiotee` sidecar if 04-01 recorded NO-GO). Constructor takes a `sendPcm: (pcm: Int16Array) => void` sink (the gateway's `sendAudio`, wired in index.ts) and optionally an `onError: (error: Error) => void`. `start()`: early-return if already capturing (`if (this.recorder) return;` — WR-02 idempotent guard so React Strict-Mode/re-entry can't leak a handle); call `AudioRecorder.getDevices('output')` and if the array is empty, surface the condition via onError instead of indexing `outputs[0]` (WR-01 empty-device guard); pick `find((d) => d.isDefault) ?? outputs[0]`; read `AudioRecorder.getDeviceFormat(device.id)` and call `assertSampleRate(format.sampleRate, format.sampleRate)`-style guard — i.e. pass `format.sampleRate` as `inRate` to the resampler so the declared rate always equals the actual rate (RESEARCH rate-assertion note); attach `recorder.on('data', buffer => sink(float32ToInt16(resampleLinear(downmixToMonoFloat32(buffer, format.channels), format.sampleRate, 16000))))`, `recorder.on('error', onError)` (never crash — RESEARCH Pitfall 4), and `await recorder.start({ deviceType: 'output', deviceId: device.id })` in try/catch (JS-level only). Store the recorder handle. `teardown()`: stop the recorder and clear the handle. Add the by-convention-singleton @remarks note (no TSyringe; instantiated once in index.ts).

    Then RETIRE the dead renderer audio path (D-02/IN-01) in this same task: DELETE `src/renderer/src/services/audio-capture.service.ts` and `src/renderer/src/audio/rms-meter.worklet.ts`; remove the `rms-meter.worklet` input entry and its `entryFileNames` branch from `electron.vite.config.ts` (revert the renderer rollupOptions to a single `index` input with the default `assets/[name]-[hash].js` output). KEEP `src/renderer/src/audio/rms.utility.ts` (reused if the OQ-3 main meter is kept). Do NOT touch index.ts wiring here beyond what Task 3 needs — index.ts edits live in Task 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/main/audio/pcm-resample.utility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/audio/audio-capture.service.ts` exists, exports `class AudioCaptureService`, has an idempotent `start()` (early-return when already capturing, WR-02), an empty-device guard (WR-01), a `getDeviceFormat`-driven rate passed to the resampler, and a `teardown()`.
    - Source assertion: `recorder.on('error', ...)` is wired and `start()` is try/catch-wrapped (never throws on device failure).
    - `src/renderer/src/services/audio-capture.service.ts` and `src/renderer/src/audio/rms-meter.worklet.ts` are deleted (files no longer exist).
    - `electron.vite.config.ts` no longer references `rms-meter.worklet`.
    - `npx tsc --noEmit` clean (no dangling imports to the deleted files).
  </acceptance_criteria>
  <done>System audio is captured in the main process, converted to 16 kHz mono Int16, and pushed to a sink; capture is idempotent and degrades gracefully; the dead renderer audio path and its build entry are gone.</done>
</task>

<task type="auto">
  <name>Task 2: jedi:transcript channel + overlay-window.manager + preload + DebugHud render (TRN-01/02, WR-03, IN-01)</name>
  <files>src/main/overlay-window.manager.ts, src/preload/index.ts, src/preload/index.d.ts, src/renderer/src/components/debug-hud.tsx</files>
  <read_first>
    - src/main/overlay-window.manager.ts (lines 8-39 IOverlayStatus + STATUS_CHANNEL; lines 65-81 lastAudioLevel/setAudioLevel; lines 144-169 buildStatus + pushStatus — the triad to mirror and the audioLevel state to remove)
    - src/preload/index.ts (lines 10-26 IOverlayStatus + channels; lines 49-64 onStatus + reportAudioLevel — fix onStatus unsubscribe, add onTranscript, remove reportAudioLevel)
    - src/preload/index.d.ts (the JediApi + IOverlayStatus re-export to keep in sync)
    - src/renderer/src/components/debug-hud.tsx (lines 8-37 local IOverlayStatus + formatAudioMeter; lines 71-73 useEffect; lines 88-112 render grid — add transcript rows, fix cleanup, remove Audio row)
    - .planning/phases/04-stt-pipeline-live-transcript/04-PATTERNS.md (overlay-window.manager MOD lines 89-98; preload MOD lines 110-122; debug-hud MOD lines 126-138; Shared Patterns lines 144-151)
    - .planning/phases/04-stt-pipeline-live-transcript/04-CONTEXT.md (D-04 interim distinct from final; D-05 transcript coupled to HUD toggle; OQ-3 meter fate)
  </read_first>
  <action>
    In `src/main/overlay-window.manager.ts`: add `export const TRANSCRIPT_CHANNEL = 'jedi:transcript';` next to STATUS_CHANNEL; add `export interface IOverlayTranscript { finalText: string; interimText: string; connectionState: SttConnectionState; }` (import `SttConnectionState` from the stt-provider interface); add `export function pushTranscript(window: BrowserWindow, payload: IOverlayTranscript): void` mirroring `pushStatus` EXACTLY including the `if (window.isDestroyed() || window.webContents.isDestroyed()) return;` guard, sending on TRANSCRIPT_CHANNEL. REMOVE (IN-01): the `audioLevel` field from `IOverlayStatus`, `lastAudioLevel`, `setAudioLevel`, and the `audioLevel:` line in `buildStatus` — UNLESS the OQ-3 main meter is kept, in which case keep the `audioLevel` field but re-source it from a main-computed `setAudioLevel` (called by index.ts from RMS over captured PCM) rather than the renderer. Decide OQ-3 per 04-01's findings (lean: keep a main-computed meter; drop if it doesn't earn its keep) and record the choice in the SUMMARY.

    In `src/preload/index.ts`: fix `onStatus` to store the listener and RETURN `() => ipcRenderer.removeListener(STATUS_CHANNEL, listener)` (WR-03); add `onTranscript(callback: (t: IOverlayTranscript) => void): () => void` mirroring the fixed onStatus on TRANSCRIPT_CHANNEL (declare `IOverlayTranscript` locally — preload is bundled separately, same reason IOverlayStatus is re-declared); REMOVE `AUDIO_LEVEL_CHANNEL` and `reportAudioLevel` (IN-01) so the renderer->main write surface is zero. Update `src/preload/index.d.ts` if it re-exports the changed types. If OQ-3 keeps the meter, keep `audioLevel` on the local IOverlayStatus; otherwise remove it there too (keep all three IOverlayStatus declarations — main/preload/renderer — in sync).

    In `src/renderer/src/components/debug-hud.tsx`: WR-03 — change the `useEffect` to capture `const offStatus = window.jedi?.onStatus(...)` and `const offTranscript = window.jedi?.onTranscript((t) => setTranscript(t))`, returning `() => { offStatus?.(); offTranscript?.(); }`. Add a `transcript` state (`IOverlayTranscript | null`). Render transcript rows beneath the existing grid: final text normal (`data-testid="cell-transcript-final"`), interim text in a dimmed/italic `debug-hud__interim` class (`data-testid="cell-transcript-interim"`, D-04), and a connection-state row (`data-testid="cell-connection-state"`) showing `transcript.connectionState`. Remove `formatAudioMeter`/`AUDIO_METER_CELLS`/the `Audio` row/`audioLevel` (IN-01) unless OQ-3 keeps the meter (then re-source only). Add a cheat-sheet row for the clear-transcript chord (`Ctrl+Alt+K`, label "Clear transcript"). Visibility is already coupled to `hudVisible` (D-05) — no change needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `overlay-window.manager.ts` exports `TRANSCRIPT_CHANNEL`, `IOverlayTranscript`, and `pushTranscript` (with the isDestroyed guard).
    - Source assertion: `preload/index.ts` `onStatus` and `onTranscript` both return an unsubscribe fn; `reportAudioLevel`/`AUDIO_LEVEL_CHANNEL` removed (grep returns no match).
    - Source assertion: `debug-hud.tsx` has `data-testid="cell-transcript-final"`, `cell-transcript-interim`, `cell-connection-state`, calls `onTranscript`, and cleans up both subscriptions in the useEffect return.
    - All three `IOverlayStatus` declarations (main/preload/renderer) are in sync regarding `audioLevel` (all removed, or all kept+re-sourced per OQ-3).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A dedicated one-way jedi:transcript channel carries finalText/interimText/connectionState to the DebugHud, which renders final and interim distinctly plus the connection state; the leaked status listener is fixed and the renderer->main write channel is removed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the pipeline in index.ts and tear it down cleanly (TRN-01/03/04)</name>
  <files>src/main/index.ts</files>
  <read_first>
    - src/main/index.ts (the full file — buildHandlers lines 24-42, installAudioPipeline lines 44-84 to remove, whenReady lines 106-129, window-all-closed lines 131-138)
    - src/main/hotkey-registrar.service.ts (lines 80-133 — register()/teardown() lifecycle to mirror for the new services)
    - .planning/phases/04-stt-pipeline-live-transcript/04-PATTERNS.md (index.ts MOD lines 21; By-convention singleton wired at the entry point lines 157-159; Native-source wrapper teardown lines 153-155)
    - 04-01/04-02/04-03 SUMMARYs (device format, backoff/state values + env var name, buffer constants + clear chord letter)
  </read_first>
  <action>
    Rework `src/main/index.ts` to wire the pipeline once in `app.whenReady()` (by-convention singletons resolved at the entry point only — no service-locator mid-method): instantiate `const buffer = new TranscriptBuffer()`, `const gateway = new DeepgramSttGateway(...)` (key from `process.env.DEEPGRAM_API_KEY` per D-08), `const capture = new AudioCaptureService((pcm) => gateway.sendAudio(pcm), (err) => { /* surface to HUD via a connection-state/error path; never crash */ })`. Wire gateway events: `gateway.on('transcript', ({ text, isFinal }) => { if (isFinal) buffer.appendFinal(text); else buffer.setInterim(text); pushTranscript(window, { ...buffer.renderable(), connectionState }); })` and `gateway.on('connection-state-change', (state) => { connectionState = state; pushTranscript(window, { ...buffer.renderable(), connectionState: state }); })` and `gateway.on('error', ...)` (log non-secret error, keep running). Call `await gateway.start()` then `await capture.start()`. REMOVE `installAudioPipeline`, the `setDisplayMediaRequestHandler`/`desktopCapturer` import + body, and the `ipcMain.on('jedi:audio-level')` listener (IN-01); remove now-unused imports (`session`, `desktopCapturer`, `ipcMain`, `IpcMainEvent`, `setAudioLevel`) — keep only what remains used. Add the `clear-transcript` handler to `buildHandlers`: `'clear-transcript': () => { buffer.clear(); pushTranscript(window, { ...buffer.renderable(), connectionState }); }`. In `app.on('window-all-closed')`, call `void capture.teardown(); void gateway.stop();` alongside the existing `hotkeyRegistrar?.teardown()` (mirror the registrar teardown discipline). If OQ-3 keeps the main meter, compute RMS over captured PCM in the capture sink and push it via the kept `setAudioLevel` path.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `index.ts` instantiates `TranscriptBuffer`, `DeepgramSttGateway`, `AudioCaptureService` once in `app.whenReady`, wires `gateway.on('transcript'|'connection-state-change'|'error')`, and calls `pushTranscript`.
    - Source assertion: `installAudioPipeline`, `setDisplayMediaRequestHandler`, `desktopCapturer`, and `ipcMain.on('jedi:audio-level')` are removed (grep returns no match in index.ts); no unused imports remain.
    - Source assertion: `buildHandlers` includes a `clear-transcript` handler calling `buffer.clear()`; `window-all-closed` tears down capture + gateway.
    - The Deepgram key is read only from `process.env`/dev-config and never logged.
    - `npx tsc --noEmit` clean and `npm test` (full suite) green.
  </acceptance_criteria>
  <done>The full main-side pipeline is wired and torn down cleanly: captured audio reaches Deepgram, transcripts fill the buffer and push to the overlay, the clear chord wipes the buffer, and all native/socket resources release on quit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-machine live transcript, interim/final, reconnect, and clear verification</name>
  <files>src/main/hotkey-registrar.service.ts, src/renderer/src/components/debug-hud.tsx</files>
  <read_first>
    - .planning/phases/04-stt-pipeline-live-transcript/04-VALIDATION.md (Manual-Only Verifications table lines 69-76)
    - .planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md (the conflict-test procedure for the clear chord)
    - .planning/phases/04-stt-pipeline-live-transcript/04-CONTEXT.md (success criteria; D-04/D-05 render expectations)
  </read_first>
  <action>
    Set a real Deepgram key in the env/dev-config (`DEEPGRAM_API_KEY`, gitignored per 04-01), launch the app under Electron 35.7.5, and verify the live behaviors that cannot be unit-tested. Also re-run the Phase 2 conflict check for the new `Ctrl+Alt+K` clear chord against Microsoft Teams, Zoom, and VS Code holding focus (confirm clearing fires AND each app's own Ctrl+Alt+K still works); if a collision is found, switch the chord to `Ctrl+Alt+X` in `hotkey-registrar.service.ts` + the DebugHud cheat-sheet and re-verify.
  </action>
  <what-built>
    The full STT pipeline: main-side WASAPI capture -> resample -> Deepgram v5 -> time-bounded buffer -> jedi:transcript push -> DebugHud render, with a clear-transcript hotkey and auto-reconnect.
  </what-built>
  <how-to-verify>
    1. With a real Deepgram key configured, launch the app and play system audio with speech (a video, a Teams test call, or talk during a loopback-captured call).
    2. Confirm a rolling transcript updates in the DebugHud, and that interim (partial) text is visually distinct (dimmed/italic) from finalized text (D-04). [TRN-01, TRN-02]
    3. Confirm the connection-state row shows `connected` during normal operation.
    4. Force a drop: disable network (or kill the socket) mid-session; confirm the state shows `reconnecting`/`disconnected`, then returns to `connected` automatically with backoff, and the transcript resumes. [TRN-03]
    5. Press the clear-transcript chord (Ctrl+Alt+K); confirm the transcript area empties immediately. [TRN-04]
    6. Toggle the HUD off (Ctrl+Alt+H); confirm the transcript hides with it (D-05); toggle back on.
    7. Re-run the Teams/Zoom/VS Code conflict check for Ctrl+Alt+K; confirm no collision (or switch to Ctrl+Alt+X and re-verify).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms a live rolling transcript renders, with interim distinct from final (TRN-01, TRN-02).
    - Human confirms a forced mid-session drop auto-reconnects with backoff and the connection state is visible throughout (TRN-03).
    - Human confirms the clear chord empties the transcript (TRN-04) and the chosen letter is conflict-free vs Teams/Zoom/VS Code (chord finalized).
    - Human confirms toggling the HUD off hides the transcript (D-05).
    - No Deepgram key appears in logs or the committed tree.
  </acceptance_criteria>
  <resume-signal>Type "verified" (all live behaviors confirmed, clear chord finalized) or describe the issue and which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main -> renderer (jedi:transcript) | One-way push of transcript text + connection state to the read-only overlay view |
| renderer -> main | After IN-01 removal, the renderer->main write surface is ZERO (no control channel) |
| in-process native capture -> main | A native addon crash can take down the whole app (D-01 accepted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-08 | Tampering | Renderer->main control surface | mitigate | `jedi:transcript` is one-way main->renderer, render-only; removing `jedi:audio-level` (IN-01) shrinks the renderer->main write surface to zero — renderer cannot send control messages |
| T-4-09 | Information Disclosure | Deepgram key reaching renderer/logs/commits | mitigate | Key stays in main (env/dev-config, D-08); `jedi:transcript` carries text + state only, never the key; gitignored dev-config; manual verify asserts no key in logs/tree |
| T-4-01 | Denial of Service / Elevation | In-process native capture crash (D-01) | accept | Documented accepted trade-off; mitigated by the 04-01 go/no-go gate + idempotent start (WR-02) + empty-device guard (WR-01) + error event handling (never throw); `audiotee` sidecar is the escape hatch if unstable |
| T-4-05 | Denial of Service | Reconnect churn / socket idle-close | mitigate | Inherited from 04-02 gateway: keep-alive during silence + bounded backoff; verified live in Task 4 |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (no dangling imports after dead-code removal).
- `npm test` (full Vitest suite) green.
- Grep confirms `installAudioPipeline`/`setDisplayMediaRequestHandler`/`jedi:audio-level`/`reportAudioLevel` are gone and the deleted renderer files no longer exist.
- Human-verified: live transcript + interim/final + reconnect + clear + HUD-coupled visibility; clear chord conflict-checked and finalized.
- No Deepgram key in logs or the committed tree.
</verification>

<success_criteria>
- Live rolling transcript on the overlay with interim distinct from final (TRN-01, TRN-02).
- Auto-reconnect with backoff and visible connection state (TRN-03).
- Clear-transcript hotkey wipes the buffer; overlay reflects empty (TRN-04).
- Dead renderer audio path + Chromium capture path + jedi:audio-level write channel fully retired (D-02/IN-01); WR-01/WR-02/WR-03 folded todos closed.
- Strictly one-way main->renderer boundary restored (zero write channels).
</success_criteria>

<output>
Create `.planning/phases/04-stt-pipeline-live-transcript/04-04-SUMMARY.md` when done. Record the OQ-3 meter decision (kept/dropped), the finalized clear chord (K or X), and the live-verification results (transcript, reconnect, clear).
</output>
