---
phase: 06-session-context-settings-window
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/main/ai/ai-orchestrator.ts
  - src/main/ai/ai-orchestrator.test.ts
  - src/main/ai/anthropic-ai.gateway.ts
  - src/main/index.ts
autonomous: false
requirements: [CTX-02, CTX-03, AI-06, SET-02]

must_haves:
  truths:
    - "Every AI mode (answer + talking-points) is grounded in the active session context at trigger time"
    - "A mid-session context Save is picked up on the very next AI trigger — no restart (pull-on-trigger)"
    - "An empty/absent context yields a byte-for-byte Phase-5 prompt (the seam fails safe)"
    - "Saving an Anthropic key re-keys the live gateway in place — the next call uses the new key, no restart"
    - "Saving a Deepgram key tears down and reconnects the live STT websocket with the new key, no restart"
    - "Saving a context via the settings window persists it and grounds the next call (settings:save-context fully wired)"
  artifacts:
    - path: "src/main/ai/ai-orchestrator.ts"
      provides: "5th ctor param getActiveContext + line-165 context injection (D-10)"
      contains: "getActiveContext"
    - path: "src/main/ai/anthropic-ai.gateway.ts"
      provides: "in-place rekey(newKey) (D-07, Pattern 5b)"
      contains: "rekey"
    - path: "src/main/index.ts"
      provides: "context repo wiring, settings:*context* handlers, live re-key on saveKeys, sttGateway re-point + attachSttGatewayHandlers"
      contains: "attachSttGatewayHandlers"
  key_links:
    - from: "src/main/ai/ai-orchestrator.ts:165"
      to: "this.getActiveContext()"
      via: "assemblePrompt({ mode, span, context })"
      pattern: "context: this.getActiveContext\\(\\)"
    - from: "src/main/index.ts saveKeys handler"
      to: "anthropicGateway.rekey + deepgram instance swap"
      via: "live re-key (D-07)"
      pattern: "rekey|attachSttGatewayHandlers"
    - from: "src/main/index.ts settings:save-context handler"
      to: "contextRepo.saveActive"
      via: "ipcMain.handle"
      pattern: "saveActive"
---

<objective>
Complete the phase's end-to-end proof: inject the active session context into every AI prompt
(D-10, pull-on-trigger), fully wire the `settings:get-context`/`settings:save-context` IPC handlers to
the repository, and make both API keys apply LIVE on save with no restart (D-07) — Anthropic re-keyed
in place, Deepgram torn down and reconnected. After this plan, the user opens settings, pastes both keys
+ context, saves, and the very next AI trigger is grounded with the new keys — no relaunch.

Purpose: AI-06 (every call grounded in active context + transcript span), CTX-03 (injected into all
modes), CTX-02 (the save→persist→ground loop), SET-02 (keys applied live). This is the D-13 "seam built
empty in Phase 5, filled in Phase 6" completion.
Output: The orchestrator's 5th constructor param + line-165 swap, the in-place Anthropic `rekey()`, the
Deepgram instance-swap re-key with re-attached handlers, the context-repo wiring, and the four
`settings:*` handlers fully implemented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-context-settings-window/06-CONTEXT.md
@.planning/phases/06-session-context-settings-window/06-RESEARCH.md
@.planning/phases/06-session-context-settings-window/06-PATTERNS.md
@.planning/phases/06-session-context-settings-window/06-VALIDATION.md
@.planning/phases/06-session-context-settings-window/06-01-SUMMARY.md
@.planning/phases/06-session-context-settings-window/06-02-SUMMARY.md
@.planning/phases/06-session-context-settings-window/06-03-SUMMARY.md

<interfaces>
<!-- AiOrchestrator constructor TODAY (ai-orchestrator.ts:115-122) — add a 5th param exactly like pushAi. -->
new AiOrchestrator(gateway, transcriptBuffer, history, pushAi)
<!-- AFTER (D-10): -->
new AiOrchestrator(gateway, transcriptBuffer, history, pushAi, getActiveContext: () => IGroundingContext | undefined)
<!-- ai-orchestrator.ts:165 today: assemblePrompt({ mode, span, context: undefined }) -->

<!-- Repo (06-02): -->
contextRepo.activeAsGrounding(): IGroundingContext | undefined
contextRepo.getActive(): ISessionContextDto | undefined
contextRepo.saveActive(fields): void

<!-- Key store (06-01): -->
apiKeyStore.saveDeepgram(plaintext) / saveAnthropic(plaintext) / getDeepgram() / getAnthropic()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrator context injection (D-10) — 5th ctor param + line-165 swap + extended test</name>
  <read_first>
    - src/main/ai/ai-orchestrator.ts:108-175 (the constructor :115-122 to extend exactly like pushAi; trigger() and the assemblePrompt call at :165 to swap)
    - src/main/ai/ai-orchestrator.test.ts (the existing fake-gateway test to extend — assert getActiveContext result reaches assemblePrompt)
    - src/main/ai/prompt-assembler.ts:52-70,91-115 (assemblePrompt's existing optional context? slot — no signature change; formatContext empty→'')
    - src/main/context/session-context.repository.ts (activeAsGrounding from 06-02 — the provider source)
    - .planning/phases/06-session-context-settings-window/06-RESEARCH.md (Pattern 6; VALIDATION Req→test map for AI-06/D-10)
  </read_first>
  <behavior>
    - When getActiveContext() returns a filled IGroundingContext, trigger('answer') calls assemblePrompt with that context (the four blocks appear in the assembled prompt)
    - When getActiveContext() returns undefined, the assembled prompt is byte-for-byte identical to the Phase-5 (context:undefined) prompt
    - getActiveContext is pulled at EACH trigger() (a value change between two triggers is reflected in the second) — no cached/mutable orchestrator state
  </behavior>
  <action>
    Extend `src/main/ai/ai-orchestrator.test.ts` FIRST with a fake `getActiveContext` provider asserting the
    three behaviors above (use the existing fake-gateway harness; capture the `assemblePrompt` argument or the
    resulting system/userContent). Then add the 5th constructor parameter to `AiOrchestrator`
    `private readonly getActiveContext: () => IGroundingContext | undefined` (exactly like `pushAi`,
    ai-orchestrator.ts:119; import `IGroundingContext` from prompt-assembler), and change line 165 from
    `context: undefined` to `context: this.getActiveContext()`. No `assemblePrompt` signature change. TSDoc
    the new param mirroring the pushAi doc. Pull-on-trigger = zero new mutable orchestrator state.
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/ai-orchestrator.test.ts src/main/ai/prompt-assembler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/ai/ai-orchestrator.test.ts src/main/ai/prompt-assembler.test.ts` exits 0; the new context-injection cases are green
    - Source assertion: ai-orchestrator.ts:165 reads `context: this.getActiveContext()` (no remaining `context: undefined`)
    - The undefined-context case proves a Phase-5-identical prompt (assert equality against the no-context assembly)
    - `npm run typecheck:node` passes
  </acceptance_criteria>
  <done>The orchestrator pulls the active context at each trigger and injects it at line 165; empty context = Phase-5-identical prompt; covered by the extended test.</done>
</task>

<task type="auto">
  <name>Task 2: Live re-key — Anthropic in-place rekey() + Deepgram instance swap + handler re-attach refactor</name>
  <read_first>
    - src/main/ai/anthropic-ai.gateway.ts:20-51,88-97 (client constructed once :35-38; never-log discipline :20-21,88-89; sanitizeAiError :88-97 — drop readonly on client/apiKey, add rekey())
    - src/main/stt/deepgram-stt.gateway.ts:77-105,135 (private readonly apiKey :80, captured in connect() :135, stop() teardown :98-105 — no setter, so re-key = instance swap)
    - src/main/index.ts:162-217 (wireSttPipeline: the three gateway.on(...) bindings :172-191 to extract into attachSttGatewayHandlers; the capture callback's gateway.sendAudio at :205 to re-point at module-level sttGateway)
    - .planning/phases/06-session-context-settings-window/06-RESEARCH.md (Pattern 5a/5b; Pitfall 3 re-attach handlers; Pitfall 4 drop readonly)
    - .planning/phases/06-session-context-settings-window/06-PATTERNS.md (anthropic rekey + index.ts re-key sections)
  </read_first>
  <action>
    (a) `anthropic-ai.gateway.ts`: drop `readonly` on `client` and `apiKey` (Pitfall 4 — else TS2540); add
    `public rekey(newKey: string): void { this.apiKey = newKey; this.client = new Anthropic({ apiKey: newKey }); }`
    with TSDoc noting it never logs the key and the gateway still never reads `process.env`. The next
    `stream()` uses the new client; the orchestrator's `readonly gateway` reference + wired handlers are
    untouched (Pattern 5b).
    (b) `index.ts`: extract the three `gateway.on('transcript'|'connection-state-change'|'error', ...)`
    bindings from `wireSttPipeline` (:172-191) into a reusable `attachSttGatewayHandlers(gateway, window,
    buffer, ...)` called on BOTH boot and re-key (Pitfall 3). Re-point the capture callback to call
    `sttGateway?.sendAudio(pcm)` (the module-level re-pointable ref) instead of closing over the local
    `gateway` const (currently :205) — so the running AudioCaptureService keeps feeding whatever the current
    gateway instance is. Provide an internal `rekeyDeepgram(newKey)`: `await sttGateway.stop()` → construct
    `new DeepgramSttGateway(newKey)` → `attachSttGatewayHandlers(...)` → assign to the module-level
    `sttGateway` → `await sttGateway.start()`. Never log the key.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck && npm run lint && npm test` pass
    - `anthropic-ai.gateway.ts` has a `rekey(newKey: string): void` method; `client`/`apiKey` are no longer `readonly`
    - Grep gate: across the changed files, no log/console statement includes the key — `grep -v '^#' src/main/index.ts src/main/ai/anthropic-ai.gateway.ts | grep -cE 'console\.(log|error|warn).*(apiKey|newKey|deepgram.*[Kk]ey|anthropic.*[Kk]ey)'` is 0
    - The capture callback calls `sttGateway?.sendAudio` (the re-pointable ref), not a captured local `gateway` const
    - `attachSttGatewayHandlers` is defined once and called from both the boot path and rekeyDeepgram
  </acceptance_criteria>
  <done>Anthropic re-keys in place; Deepgram re-keys via a clean instance swap with re-attached handlers and an uninterrupted capture feed; no key is ever logged.</done>
</task>

<task type="auto">
  <name>Task 3: Wire context repo + getActiveContext provider + fully implement the four settings:* IPC handlers (incl. live re-key on saveKeys)</name>
  <read_first>
    - src/main/index.ts:219-262 (app.whenReady block; orchestrator wiring :243; the buildHandlers threading; the apiKeyStore + ipcMain.handle stubs registered in 06-01)
    - src/main/context/session-context.repository.ts (saveActive/getActive/activeAsGrounding from 06-02)
    - src/main/context/parse-links.utility.ts (if the renderer sends newline-joined links per the 06-03 SUMMARY decision, parse here; if it sends links[], skip)
    - .planning/phases/06-session-context-settings-window/06-03-SUMMARY.md (the single chosen links-parsing location — match it)
    - .planning/phases/06-session-context-settings-window/06-PATTERNS.md (index.ts wiring sites section: orchestrator D-10, ipcMain.handle D-04, DTO validation)
  </read_first>
  <action>
    In `index.ts`, inside `app.whenReady().then(...)`:
    (a) Instantiate `const contextRepo = new SessionContextRepository();` (by-convention singleton, D-09) and
    add the provider as the orchestrator's 5th arg at :243: `new AiOrchestrator(aiGateway, buffer, aiHistory,
    (event) => pushAi(window, event), () => contextRepo.activeAsGrounding())` (D-10, pull-on-trigger).
    (b) Fully implement the four `settings:*` `ipcMain.handle` channels that 06-01 stubbed:
      - `settings:get-keys` → `{ deepgram: apiKeyStore.hasDeepgram(), anthropic: apiKeyStore.hasAnthropic() }` (already from 06-01; keep).
      - `settings:save-keys` → trim + string-validate; for each present non-empty key: persist via apiKeyStore
        AND apply live (D-07) — Anthropic: `aiGateway.rekey(newDeepgram?…)`/`aiGateway.rekey(newAnthropicKey)`;
        Deepgram: `await rekeyDeepgram(newDeepgramKey)`. Never log the key.
      - `settings:get-context` → `contextRepo.getActive()` (the editor pre-fills from this).
      - `settings:save-context` → VALIDATE the DTO shape (reject non-object / wrong field types — Tampering,
        Security Domain), parse links if the renderer sent newline text (per 06-03 SUMMARY), then
        `contextRepo.saveActive({ notes, ticketText, repoSnippets, links })`. The next AI trigger pulls it
        via the provider (D-06 immediate-active, no restart).
    Resolve all singletons at this entry point only (no service-locator mid-method).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck && npm run lint && npm test && npm run build` all pass
    - `index.ts` constructs `AiOrchestrator` with a 5th arg `() => contextRepo.activeAsGrounding()`
    - All four `settings:*` channels are registered with non-stub bodies; `settings:save-context` validates the DTO before `saveActive`
    - `settings:save-keys` calls both `apiKeyStore.save*` AND the live re-key paths (rekey + rekeyDeepgram)
    - Grep gate: `grep -v '^#' src/main/index.ts | grep -c 'saveActive'` ≥ 1
  </acceptance_criteria>
  <done>The context repo + provider are wired; the four IPC handlers are fully implemented; saveKeys re-keys both gateways live; saveContext persists-and-activates and grounds the next trigger.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-machine end-to-end grounding + live re-key verification</name>
  <what-built>The complete end-to-end phase outcome: open settings → paste both keys + context → save →
  the very next AI trigger is grounded with the new keys, no restart. Live Anthropic + Deepgram re-key and
  pull-on-trigger context injection.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. With NO saved context, trigger Answer (Ctrl+Alt+A) on some audio — note the answer's
       generic relevance (Phase-5 baseline).
    2. Press `Ctrl+Alt+S`, paste meaningful project context (notes/ticket/repo/links) on the Context tab,
       Save. WITHOUT restarting, trigger Answer again — confirm the answer is observably MORE grounded in the
       pasted context (AI-06 observable-improvement criterion). Confirm an empty/cleared context returns to
       the Phase-5 generic behavior.
    3. On the Keys tab, save a fresh valid Anthropic key — trigger Answer and confirm the next call succeeds
       with the new key (no restart). Save a fresh valid Deepgram key while STT is running — confirm the live
       transcript pauses briefly then RESUMES (websocket torn down + reconnected with the new key), with no
       app restart and no frozen connection-state (Pitfall 3 regression check).
    4. Confirm nothing in the terminal logs printed any key value during the re-key.
  </how-to-verify>
  <resume-signal>Type "approved", or describe the failure (e.g. transcript froze after Deepgram re-key, context not picked up).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings renderer → main (save-keys/save-context) | Plaintext key (inbound once) + context DTO cross here |
| main → live gateways (re-key) | New key re-supplied to in-memory gateway instances |
| main → Deepgram websocket | Key flows to the external STT service on reconnect (acceptable per project privacy decision) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Information Disclosure | key in re-key logs | mitigate | rekey()/rekeyDeepgram never log the key; gateways never read process.env; sanitizeAiError strips SDK bodies (anthropic-ai.gateway.ts:88-97) |
| T-06-14 | Tampering | save-context DTO shape | mitigate | Validate DTO (object + field types) at the `settings:save-context` handler before calling saveActive (Security Domain) |
| T-06-15 | Information Disclosure | context leaking into prompt unexpectedly | accept | Context is user-pasted and intentionally injected (AI-06); empty context = no block (fails safe) |
| T-06-16 | Denial of Service | re-key churn freezing STT | mitigate | Deepgram re-key reuses the gateway's own connect/reconnect/backoff; handlers re-attached (Pitfall 3) so transcripts resume |
| T-06-SC | Tampering | npm installs | accept | No package installs in this plan (wiring-only); ulid handled in 06-02 |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` green.
- `npx vitest run src/main/ai/ai-orchestrator.test.ts src/main/ai/prompt-assembler.test.ts` green (context injection + Phase-5-identical empty path).
- Human checkpoint: observable grounding improvement vs Phase 5; live Anthropic + Deepgram re-key with no restart and no frozen transcript; no key logged.
</verification>

<success_criteria>
- AI-06 / CTX-03: every mode is grounded in the active context + transcript span; improvement observable vs Phase 5.
- CTX-02: save→persist→ground loop works end-to-end across the next trigger (and across restart via 06-02 persistence).
- SET-02 (live half): both keys apply live on save with no restart (Anthropic in place, Deepgram reconnect).
- The empty-context path is byte-for-byte Phase-5-identical (the seam fails safe).
</success_criteria>

<output>
Create `.planning/phases/06-session-context-settings-window/06-04-SUMMARY.md` when done. Record the observed
grounding-improvement evidence and confirm the Deepgram live-reconnect resumed the transcript.
</output>
