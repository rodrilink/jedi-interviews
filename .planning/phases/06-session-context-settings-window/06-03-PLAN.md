---
phase: 06-session-context-settings-window
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/renderer/src/settings.tsx
  - src/renderer/src/SettingsApp.tsx
  - src/renderer/src/components/KeysTab.tsx
  - src/renderer/src/components/ContextTab.tsx
  - src/renderer/src/settings.css
autonomous: false
requirements: [CTX-01, SET-02, SET-04]
user_setup: []

must_haves:
  truths:
    - "The settings window shows two tabs, Keys and Context, with Context as the default landing tab"
    - "The Context tab has four fields: Notes, Ticket text, Repo snippets, Links (one URL per line)"
    - "The Context tab pre-fills from the saved active context and shows an unsaved-changes (dirty) indicator"
    - "An explicit Save button writes the context once via saveContext and clears the dirty state"
    - "The Keys tab lets the user enter and save Deepgram + Anthropic keys and shows which keys are already set"
  artifacts:
    - path: "src/renderer/src/SettingsApp.tsx"
      provides: "two-tab shell (Context default + Keys) — D-03"
      contains: "Context"
    - path: "src/renderer/src/components/ContextTab.tsx"
      provides: "four-field editor + dirty indicator + explicit Save (D-05/D-06)"
      contains: "Save"
    - path: "src/renderer/src/components/KeysTab.tsx"
      provides: "two-key entry/save form + presence display (SET-02)"
      contains: "saveKeys"
  key_links:
    - from: "src/renderer/src/components/ContextTab.tsx"
      to: "window.settingsApi.saveContext / getContext"
      via: "settings preload contextBridge"
      pattern: "settingsApi\\.(save|get)Context"
    - from: "src/renderer/src/components/KeysTab.tsx"
      to: "window.settingsApi.saveKeys / getKeys"
      via: "settings preload contextBridge"
      pattern: "settingsApi\\.(save|get)Keys"
---

<objective>
Replace the 06-01 placeholder settings renderer with the real two-tab editor UI: a Context tab (the
landing tab, D-03) with the four `IGroundingContext` fields and an explicit Save + dirty indicator
(D-05/D-06), and a Keys tab for entering/saving both API keys with a presence display (SET-02). This is
the renderer-only slice — all persistence/encryption stays in main behind the `settingsApi` two-way
bridge (06-01) and the repository (06-02); the live save→grounding path is completed by 06-04.

Purpose: CTX-01 (context editor reachable), SET-04 (editor reachable from the same hotkey-opened window),
SET-02 (the key-entry UI half). Standard focusable-window form UX — component structure is Claude's
discretion per CONTEXT.md.
Output: A real `SettingsApp` with `ContextTab` + `KeysTab`, wired to `window.settingsApi.*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-context-settings-window/06-CONTEXT.md
@.planning/phases/06-session-context-settings-window/06-RESEARCH.md
@.planning/phases/06-session-context-settings-window/06-PATTERNS.md
@.planning/phases/06-session-context-settings-window/06-01-SUMMARY.md
@.planning/phases/06-session-context-settings-window/06-02-SUMMARY.md

<interfaces>
<!-- The two-way bridge exposed by src/preload/settings.ts (06-01). getKeys returns presence booleans ONLY. -->
window.settingsApi:
  getKeys(): Promise<{ deepgram: boolean; anthropic: boolean }>
  saveKeys(keys: { deepgram?: string; anthropic?: string }): Promise<void>
  getContext(): Promise<ISessionContextDto | undefined>   // fully wired in 06-04
  saveContext(dto): Promise<void>                          // fully wired in 06-04

<!-- The four editable grounding fields (from session-context.interface.ts, 06-02). -->
ISessionContextDto: { id; notes?; ticketText?; repoSnippets?; links?: string[]; name?; source?; createdAt? }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-tab SettingsApp shell (Context landing + Keys) + KeysTab</name>
  <read_first>
    - src/renderer/src/settings.tsx (the 06-01 placeholder entry rendering the root — replace its placeholder with <SettingsApp/>)
    - src/renderer/src/App.tsx (the existing overlay React root — component/style conventions, no inline style props, data-testid prefixes per naming-conventions)
    - src/renderer/src/main.tsx (createRoot+StrictMode shape already mirrored in settings.tsx)
    - src/preload/settings.ts (the settingsApi contract this UI calls; getKeys returns booleans only)
    - .planning/phases/06-session-context-settings-window/06-01-SUMMARY.md (the verified dev URL suffix + final chord; the SettingsApi type export to import for typing window.settingsApi)
  </read_first>
  <action>
    Create `src/renderer/src/SettingsApp.tsx`: a two-tab shell with `Context` and `Keys` tabs, `Context`
    as the DEFAULT/landing tab (D-03). Use `data-testid="tab-context"` and `data-testid="tab-keys"` (naming
    conventions). Update `src/renderer/src/settings.tsx` to render `<SettingsApp />` instead of the
    placeholder. Create `src/renderer/src/components/KeysTab.tsx`: two masked inputs (`input-deepgram-key`,
    `input-anthropic-key`) with an optional show/hide affordance (discretion), a Save button
    (`btn-save-keys`) that calls `window.settingsApi.saveKeys({ deepgram, anthropic })` for the
    fields the user filled, and a presence display driven by `getKeys()` on mount (e.g. "Deepgram: set ✓ /
    not set") — NEVER renders a key value (getKeys returns booleans only). Add `src/renderer/src/settings.css`
    (SCSS-module or plain CSS import — NO inline `style` props per code-standards). Type `window.settingsApi`
    via the `SettingsApi` type exported from the preload (or a local declaration mirroring it).
  </action>
  <verify>
    <automated>npm run typecheck:web && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:web && npm run lint && npm run build` pass; `out/renderer/settings.html` still emits
    - `SettingsApp.tsx` renders with the Context tab active by default (assert default-tab state in source: initial tab === 'context')
    - Grep gate: `grep -v '^#' src/renderer/src/components/KeysTab.tsx | grep -c 'getKeys'` ≥ 1 AND the component never assigns a key string from getKeys (getKeys typed to return booleans)
    - No inline `style=` prop anywhere in the new components (grep for `style=` returns 0 in the new files)
  </acceptance_criteria>
  <done>The settings window renders a two-tab shell (Context default) and a working Keys entry/save form showing key presence only.</done>
</task>

<task type="auto">
  <name>Task 2: ContextTab — four fields + dirty indicator + explicit Save (D-05/D-06)</name>
  <read_first>
    - src/main/ai/prompt-assembler.ts:52-61 (the four IGroundingContext fields the form maps 1:1 to)
    - src/main/context/parse-links.utility.ts (links one-per-line parsing — the UI joins links[] with '\n' for editing; main re-parses on save, or the UI parses before sending — keep parsing authoritative in ONE place)
    - src/renderer/src/SettingsApp.tsx (the shell from Task 1)
    - src/preload/settings.ts (getContext/saveContext signatures)
  </read_first>
  <action>
    Create `src/renderer/src/components/ContextTab.tsx`: four labeled fields mapping 1:1 to
    `IGroundingContext` (D-05) — textareas for Notes (`input-notes`), Ticket text (`input-ticket-text`),
    Repo snippets (`input-repo-snippets`), and Links (`input-links`, one URL per line). On mount call
    `window.settingsApi.getContext()` and pre-fill: Notes/Ticket/Repo from their fields, Links from
    `links.join('\n')`. Track a `dirty` flag (true when any field differs from the loaded value) and show
    an "unsaved changes" indicator (`text-dirty-indicator`). An explicit Save button (`btn-save-context`)
    calls `window.settingsApi.saveContext(...)` ONCE per click (NOT autosave, D-06), sending the four
    fields with Links as a newline-joined string (let main parse via parseLinks — keep parsing authoritative
    in main) OR pre-parsed `links[]` — pick ONE and document it; on success clear dirty and show a "saved ✓"
    confirmation. No inline style props.
  </action>
  <verify>
    <automated>npm run typecheck:web && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:web && npm run lint && npm run build` pass
    - ContextTab has all four fields with the data-testids above; the Links field is a textarea
    - The Save button calls `settingsApi.saveContext` exactly once per click (no autosave/onChange-save) — assert in source there is no save call inside onChange handlers
    - A dirty indicator element (`text-dirty-indicator`) is present and gated on a dirty state
    - getContext pre-fill maps links[] back to newline-joined text for the textarea
  </acceptance_criteria>
  <done>The Context tab edits the four grounding fields, shows a dirty indicator, and saves once on explicit click.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-machine settings editor UI + dirty/save UX verification</name>
  <what-built>The full two-tab settings editor: Context (landing) with the four fields + dirty indicator +
  explicit Save, and Keys with two-key entry/save + presence display. The renderer is wired to the
  settingsApi bridge. (The end-to-end live save→grounding path is completed once 06-04 lands; this checkpoint
  verifies the UI + dirty/save UX in isolation.)</what-built>
  <how-to-verify>
    1. Run `npm run dev`, press `Ctrl+Alt+S` (or the 06-01 fallback chord). Confirm the window lands on the
       Context tab by default and shows four labeled fields plus a Links textarea.
    2. Type into a field — confirm the unsaved-changes indicator appears. Click Save — confirm it clears and
       a "saved ✓" confirmation shows. Reopen the window (close + Ctrl+Alt+S) — confirm the saved values
       pre-fill (this exercises getContext, fully meaningful once 06-04 wires the handler; if 06-04 has not
       merged yet, confirm at least no error and the Save call resolves).
    3. Switch to the Keys tab — enter a Deepgram and an Anthropic key, Save. Confirm the presence display
       updates to "set". Confirm NO key value is ever echoed back into the input after reopening (getKeys
       returns booleans only).
    4. Confirm the overlay remains click-through/unaffected throughout (focus discipline intact).
  </how-to-verify>
  <resume-signal>Type "approved", or describe the UI/UX issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings renderer → main (IPC) | The Context/Keys forms send user input over the `settings:*` invoke channels |
| settings renderer display | getKeys presence booleans + getContext non-secret text rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | key value rendered in UI | mitigate | KeysTab renders presence booleans from getKeys; NEVER renders a key string; inputs are write-only (cleared/masked after save) |
| T-06-11 | Tampering/Injection | settings.html / React render | mitigate | CSP `<meta>` on settings.html (06-01); React escapes text by default; no dangerouslySetInnerHTML |
| T-06-12 | Elevation of Privilege | renderer reaching Node | mitigate | contextIsolation:true + sandbox:true (06-01); only the typed settingsApi bridge is reachable |
| T-06-SC | Tampering | npm installs | accept | No package installs in this plan (renderer-only); ulid handled in 06-02 |
</threat_model>

<verification>
- `npm run typecheck:web && npm run lint && npm run build` green.
- Human checkpoint: Context-default two-tab UI; four fields; dirty indicator; explicit single Save; key presence display never echoes a key.
</verification>

<success_criteria>
- CTX-01: the context editor (notes/ticket/snippets/links) exists and is editable.
- SET-04: it is reachable from the same window opened by `Ctrl+Alt+S` (the Context landing tab).
- SET-02 (UI half): both keys can be entered and saved from the Keys tab with presence feedback.
</success_criteria>

<output>
Create `.planning/phases/06-session-context-settings-window/06-03-SUMMARY.md` when done. Record whether links
parsing is authoritative in the renderer or in main (the single chosen location), so 06-04's handler matches.
</output>
