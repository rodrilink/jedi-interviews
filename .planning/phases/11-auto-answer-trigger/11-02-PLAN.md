---
phase: 11-auto-answer-trigger
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/preload/index.ts
  - src/renderer/src/components/ai-panel.tsx
autonomous: true
requirements: [AA-01, AA-02]

must_haves:
  truths:
    - "The preload IAiPushEvent thinking variant carries source: RequestSource, mirroring main structurally"
    - "The renderer IAiPushEvent thinking variant carries source, and the renderer carries source onto the panel entry"
    - "An entry whose source is 'auto' renders a tiny auto badge; a 'manual' entry renders no badge"
    - "The badge is in the SAME AI panel with the SAME streaming render — no new panel, no layout change (SC 3 preserved)"
  artifacts:
    - path: "src/preload/index.ts"
      provides: "source field on the preload IAiPushEvent thinking mirror"
      contains: "type: 'thinking'"
    - path: "src/renderer/src/components/ai-panel.tsx"
      provides: "source on IAiPushEvent + IAiPanelEntry + conditional auto badge in the entry header"
      contains: "source"
  key_links:
    - from: "src/renderer/src/components/ai-panel.tsx reduceEntries thinking branch"
      to: "IAiPanelEntry.source"
      via: "event.source carried onto the new entry"
      pattern: "source: event.source"
    - from: "src/renderer/src/components/ai-panel.tsx entry header"
      to: "conditional auto badge"
      via: "entry.source === 'auto'"
      pattern: "source === 'auto'"
---

<objective>
Add the single renderer-side half of D-04: a tiny "auto" badge on auto-answer entries in the EXISTING AI panel. Mirror the `source` field onto the `thinking` variant of the structurally-duplicated `IAiPushEvent` in the preload and renderer (matching the main change from Plan 01), carry `source` onto the rendered panel entry, and render a minimal conditional badge next to the mode label for `source: 'auto'` entries. A manual entry shows no badge.

Purpose: This is the ONLY renderer-touching change in the phase (D-04). It preserves SC 3 — same panel, same `jedi:ai` push, same streaming render, just annotated — so the auto answer is visually distinguishable without a new surface or layout change.
Output: The AI panel visibly badges auto answers while manual answers render exactly as today.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-auto-answer-trigger/11-CONTEXT.md
@.planning/phases/11-auto-answer-trigger/11-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. The type is intentionally duplicated
     across the process boundary (main / preload / renderer are bundled separately). Plan 01 already
     added `source` to the MAIN copy (src/main/ai/ai-orchestrator.ts). This plan mirrors it in the
     other two copies and renders it. -->

The thinking variant appears identically in all three files and must gain `source: RequestSource`
('manual' | 'auto') to stay structurally identical:
- src/main/ai/ai-orchestrator.ts:  `| { type: 'thinking'; requestId; id; mode: AiMode; at; source: RequestSource }`  (DONE in Plan 01)
- src/preload/index.ts (~line 101): `| { type: 'thinking'; requestId; id; mode: AiMode; at }`  (ADD source here)
- src/renderer/src/components/ai-panel.tsx (~line 15): `| { type: 'thinking'; requestId; id; mode: AiMode; at }`  (ADD source here)

The renderer declares its types LOCALLY (it must not import from main/preload). It already has a local
`type AiMode = 'answer' | 'talking-points' | 'code-challenge';` — add a local
`type RequestSource = 'manual' | 'auto';` (mirror, do not import).

In ai-panel.tsx:
- `interface IAiPanelEntry { id; mode: AiMode; text; state: AiEntryState; at: number }` — add `source: RequestSource`.
- `reduceEntries` thinking branch (creates the entry): `return [...entries, { id: event.id, mode: event.mode, text: '', state: 'thinking', at: event.at }];` — add `source: event.source`.
- The entry header renders: `<span className="ai-panel__entry-mode">{MODE_LABEL[entry.mode]}</span>` then a time span. Add the conditional badge here.
- Existing test-id convention: `data-testid` uses `icon-`/`badge-` prefixes (naming-conventions). Use e.g. `icon-auto-badge` on the badge.

overlay-window.manager.ts RE-EXPORTS `IAiPushEvent` from the orchestrator (`export type { IAiPushEvent };`) — it picks up Plan 01's change automatically; NO edit needed there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror source onto preload + renderer IAiPushEvent thinking variant</name>
  <files>src/preload/index.ts, src/renderer/src/components/ai-panel.tsx</files>
  <read_first>
    - src/preload/index.ts (the exported `IAiPushEvent` union + the `AiMode` type declaration around lines 91-109)
    - src/renderer/src/components/ai-panel.tsx (the local `AiMode` type, the `IAiPushEvent` union, `IAiPanelEntry`, and `reduceEntries` — lines 1-107)
    - src/main/ai/ai-orchestrator.ts (the thinking variant + `RequestSource` type as changed in Plan 01 — the shape to mirror)
    - .planning/phases/11-auto-answer-trigger/11-PATTERNS.md (the renderer section: the three mirrored locations + "structural type duplication across the process boundary")
  </read_first>
  <action>
    In `src/preload/index.ts`: add `source: RequestSource` to the `thinking` variant of the exported `IAiPushEvent`. The preload already exports `AiMode`; add an exported `export type RequestSource = 'manual' | 'auto';` mirroring the main declaration (do not import from main). Keep every other variant unchanged.

    In `src/renderer/src/components/ai-panel.tsx`: add a local `type RequestSource = 'manual' | 'auto';` (mirror, do not import), and add `source: RequestSource` to the `thinking` variant of the local `IAiPushEvent`. Add `source: RequestSource` to the `IAiPanelEntry` interface. In `reduceEntries`, in the `thinking` branch that creates a new entry, set `source: event.source` on the new entry object (alongside `mode`). Do NOT set `source` in the `empty` branch — `empty` entries are terminal placeholders and carry no source badge (leave that branch as-is, or default the entry's source to `'manual'` there so the type is satisfied — your call, provided empty entries never render an auto badge).

    Keep the three `IAiPushEvent` copies structurally identical to main. Do NOT add `source` to any other variant (delta/done/error/cancelled/empty) in any file.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/preload/index.ts` `thinking` variant contains `source: RequestSource` and exports `RequestSource`.
    - `src/renderer/src/components/ai-panel.tsx` declares a local `RequestSource`, its `thinking` variant carries `source`, `IAiPanelEntry` carries `source`, and the `reduceEntries` thinking branch sets `source: event.source`.
    - `npm run typecheck` exits 0 (both node and web projects — the three copies stay structurally identical).
  </acceptance_criteria>
  <done>
    `source` is mirrored onto the thinking variant in preload + renderer, carried onto the panel entry, and the codebase typechecks with all three IAiPushEvent copies aligned.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render the tiny auto badge in the AI panel entry header</name>
  <files>src/renderer/src/components/ai-panel.tsx</files>
  <read_first>
    - src/renderer/src/components/ai-panel.tsx (the entry `<header className="ai-panel__entry-header">` block that renders the mode label + relative time — the single render touch point; and `IAiPanelEntry` with the `source` field from Task 1)
    - .planning/phases/11-auto-answer-trigger/11-CONTEXT.md (D-04: tiny badge, same panel, no layout change; badge styling is discretion)
    - .planning/phases/11-auto-answer-trigger/11-PATTERNS.md (the ai-panel.tsx:221-224 header analog + the data-testid naming guidance)
  </read_first>
  <action>
    In the entry header (`<header className="ai-panel__entry-header">`), render a small conditional badge next to the mode label span when `entry.source === 'auto'`. Keep it tiny — a short text tag (e.g. "auto") or a dot — with a `data-testid` following the naming convention (e.g. `icon-auto-badge`). A `source: 'manual'` entry renders NO badge (the absence is the manual state). Do NOT change the panel layout, the streaming render path, the scroll behavior, or any other entry state rendering — SC 3 requires the same panel + same rendering, just annotated. If a class name is needed for styling, add a minimal `ai-panel__entry-badge` (or similar) class; only add CSS if the badge is otherwise invisible, keeping it within "no layout change."
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The entry header renders a badge element (with a `data-testid` such as `icon-auto-badge`) conditionally on `entry.source === 'auto'`.
    - No badge element renders for `entry.source === 'manual'` (the conditional is strictly on `'auto'`).
    - No change to the panel's outer structure, the scroll refs, or the delta/done/error render paths (grep confirms `renderEntryBody` and the list/refs are untouched).
    - `npm run typecheck` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>
    Auto entries show a tiny badge in the existing AI panel; manual entries look exactly as before; layout and streaming render unchanged; typecheck + lint green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (jedi:ai push) | The one-way `IAiPushEvent` push carries the new `source` field; the renderer is a pure read-only view (no new control channel) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Elevation of Privilege | New renderer→main control surface | accept (none added) | D-04 adds ONLY a field on the existing one-way `jedi:ai` push; no new IPC channel, no renderer→main write; the renderer stays a pure view (IN-01) |
| T-11-07 | Information Disclosure | Badge leaking transcript/key content | mitigate | The badge is derived solely from `source: 'manual' | 'auto'` — it renders no transcript text and no secret; only the lane discriminator crosses |
| T-11-08 | Tampering | Auto/manual visual conflation breaking SC 3 | mitigate | The badge is additive-only next to the existing mode label; the streaming render path, panel structure, and scroll behavior are untouched — same panel, same rendering (SC 3) |
</threat_model>

<verification>
- `npm run typecheck` exits 0 — all three `IAiPushEvent` copies (main from Plan 01, preload, renderer) are structurally identical with `source` on the thinking variant.
- `npm run lint` exits 0.
- grep confirms the badge renders conditionally on `entry.source === 'auto'` and the delta/done/error render paths are unchanged.
</verification>

<success_criteria>
- The auto badge renders on `source: 'auto'` entries in the existing AI panel and is absent on manual entries (D-04).
- No new panel, no new control channel, no layout/streaming-render change (SC 3 preserved).
- The three-way structural type duplication of `IAiPushEvent` stays aligned and typechecks.
</success_criteria>

<output>
Create `.planning/phases/11-auto-answer-trigger/11-02-SUMMARY.md` when done.
</output>
