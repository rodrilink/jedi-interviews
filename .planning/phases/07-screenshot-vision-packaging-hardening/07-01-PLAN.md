---
phase: 07-screenshot-vision-packaging-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/vision/downscale.utility.ts
  - src/main/vision/downscale.utility.test.ts
  - src/main/vision/screenshot.service.ts
  - src/main/vision/screenshot.service.test.ts
  - src/main/ai/ai-gateway.interface.ts
  - src/main/ai/prompt-assembler.ts
  - src/main/ai/prompt-assembler.test.ts
  - src/main/ai/anthropic-ai.gateway.ts
  - src/main/ai/ai-orchestrator.ts
  - src/main/ai/ai-orchestrator.test.ts
  - src/main/hotkey-registrar.service.ts
  - src/main/overlay-window.manager.ts
  - src/main/index.ts
  - src/renderer/src/components/vision-panel.tsx
  - src/renderer/src/App.tsx
autonomous: false
requirements: [AI-03]
user_setup:
  - service: anthropic
    why: "End-to-end screenshot-solve requires a live Anthropic key for the manual verify"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Already configured (safeStorage via settings window, or .env fallback — Phase 6 D-08). No new key path."

must_haves:
  truths:
    - "Pressing Ctrl+Alt+C captures the overlay's monitor and streams an AI code solution into a dedicated vision panel"
    - "The captured image is downscaled to <=1568px long edge before being sent, with no data: prefix on the base64"
    - "The vision prompt is grounded in the active session context + recent transcript span, and triggers even with an empty transcript span"
    - "code-challenge routes to claude-opus-4-8 under the single-in-flight orchestrator; re-press cancels, cross-mode cancels-and-starts"
    - "Answer and talking-points prompts are byte-for-byte unchanged (string content path)"
  artifacts:
    - path: "src/main/vision/downscale.utility.ts"
      provides: "Pure fitLongEdge math + VISION_MAX_LONG_EDGE + toBase64Png (no data: prefix)"
      contains: "VISION_MAX_LONG_EDGE"
    - path: "src/main/vision/screenshot.service.ts"
      provides: "captureOverlayDisplay + pure source-select helper"
      contains: "getDisplayMatching"
    - path: "src/renderer/src/components/vision-panel.tsx"
      provides: "Dedicated streaming vision panel (one-way view)"
      min_lines: 40
  key_links:
    - from: "src/main/index.ts"
      to: "aiOrchestrator.trigger('code-challenge')"
      via: "capture-code-challenge handler in buildHandlers"
      pattern: "capture-code-challenge"
    - from: "src/main/ai/ai-orchestrator.ts"
      to: "screenshot.service.captureOverlayDisplay"
      via: "code-challenge branch in trigger()"
      pattern: "code-challenge"
    - from: "src/main/ai/prompt-assembler.ts"
      to: "Anthropic content blocks"
      via: "image branch emits [{image},{text}] array"
      pattern: "code-challenge|image"
---

<objective>
Deliver the complete vision slice end-to-end: press `Ctrl+Alt+C`, capture the monitor the overlay sits on, downscale to <=1568px long edge in main, send an image+text content-block array through the existing `AnthropicGateway.stream()` to `claude-opus-4-8`, and stream the solution into a dedicated vision panel — grounded in the active session context plus the recent transcript span.

Purpose: Satisfy AI-03 (screenshot -> AI code-challenge solution). Vision reuses the ENTIRE Phase-5 AI path; the only genuinely new pieces are one capture service, one pure downscale utility, one assembler image branch, one model constant, one chord, and one renderer panel (CONTEXT D-01..D-11; RESEARCH "the whole vision feature is 'answer mode + an image param'").
Output: A working screenshot-solve mode behind `Ctrl+Alt+C`, with co-located unit tests for the downscale math, the source-select helper, the assembler image branch, and the orchestrator state machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-screenshot-vision-packaging-hardening/07-CONTEXT.md
@.planning/phases/07-screenshot-vision-packaging-hardening/07-RESEARCH.md
@.planning/phases/07-screenshot-vision-packaging-hardening/07-PATTERNS.md
@.planning/phases/07-screenshot-vision-packaging-hardening/07-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — the AI path is the extension point. -->

From src/main/ai/ai-gateway.interface.ts (CURRENT — widen these):
```typescript
export type AiMode = 'answer' | 'talking-points'; // widen: add | 'code-challenge'
export interface IAiPromptRequest {
    model: string;
    maxTokens: number;
    system: string;
    userContent: string; // widen: string | Anthropic.ContentBlockParam[]
}
```

From src/main/ai/ai-orchestrator.ts (CURRENT — extend these):
```typescript
export const ANSWER_MODEL = 'claude-haiku-4-5';
export const TALKING_POINTS_MODEL = 'claude-opus-4-8';
export const MAX_TOKENS: Record<AiMode, number> = { answer: 400, 'talking-points': 500 };
// trigger(mode) at line 140; empty-span guard at 144; model select + assemblePrompt at 170-178;
// constructor took getActiveContext as its 5th arg (per index.ts:331).
```

From src/main/ai/prompt-assembler.ts (CURRENT — add image branch + VISION_SYSTEM_PROMPT):
```typescript
export function assemblePrompt(input: IAssembleInput): IAssembledPrompt {
    const system = input.mode === 'answer' ? ANSWER_SYSTEM_PROMPT : TALKING_POINTS_SYSTEM_PROMPT;
    const contextBlock = formatContext(input.context);
    const userContent = `${contextBlock}Recent transcript (last ~60s):\n${input.span}`;
    return { system, userContent };
}
```

Confirmed Anthropic vision wire shape (RESEARCH §1, official docs): `{ type:'image', source:{ type:'base64', media_type:'image/png', data:'<RAW base64, NO data: prefix>' } }`, image block placed BEFORE the text block. Supported media types: image/jpeg, image/png, image/gif, image/webp.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure downscale utility + co-located test (Wave 0)</name>
  <files>src/main/vision/downscale.utility.ts, src/main/vision/downscale.utility.test.ts</files>
  <read_first>
    - src/main/audio/rms.utility.ts (the canonical pure-utility analog — no class, no IO, full TSDoc, explicit return type)
    - src/main/audio/rms.utility.test.ts (co-located test analog — describe/it, AAA comments on their own lines, explicit type annotation on the result variable)
    - 07-RESEARCH.md §3 (the target fitLongEdge + toBase64Png shape) and Pitfall 2 (no data: prefix) and Pitfall 5 (1568 is the app target, NOT the model ceiling)
    - 07-PATTERNS.md "NEW src/main/vision/downscale.utility.ts" and the co-located-test convention correction
  </read_first>
  <behavior>
    - fitLongEdge(width, height, maxEdge) returns {width,height} unchanged when max(width,height) <= maxEdge (only-if-larger; no upscale)
    - fitLongEdge shrinks proportionally when the longest edge exceeds maxEdge, preserving aspect ratio (landscape AND portrait)
    - Exact 1568 boundary: a 1568px long edge is NOT resized
    - toBase64Png(image) returns base64 that contains NO 'data:' substring
  </behavior>
  <action>
    Create `src/main/vision/downscale.utility.ts` as a pure utility (NO class, NO `@singleton()`, idempotent). Export `VISION_MAX_LONG_EDGE = 1568` with the comment that this is the app downscale target per D-05 and that `claude-opus-4-8` itself allows up to 2576px (Pitfall 5 — do NOT raise it to 2576). Export pure `fitLongEdge(width, height, maxEdge): { width: number; height: number }` (preserve aspect ratio; shrink only-if-larger). Export `toBase64Png(image: Electron.NativeImage): { base64: string; mediaType: string }` which calls `fitLongEdge` against `image.getSize()`, resizes via `image.resize({ width, height })` only when smaller is needed, and returns `{ base64: resized.toPNG().toString('base64'), mediaType: 'image/png' }` — use `.toPNG().toString('base64')`, NEVER `.toDataURL()` (Pitfall 2). Full TSDoc with `@param`/`@returns` on exports, explicit return types, single quotes, 4-space indent. Write the co-located `downscale.utility.test.ts` covering the four behaviors above (`import { describe, expect, it } from 'vitest'`, relative import to the sibling source). The base64-no-`data:` test may construct a small `nativeImage` from a known PNG buffer (or assert the prefix-free contract on a mocked resize result) — assert the result string does not include `'data:'`.
  </action>
  <verify>
    <automated>npx vitest run src/main/vision/downscale.utility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/vision/downscale.utility.test.ts` exits 0 with all four behaviors green
    - `downscale.utility.ts` exports `VISION_MAX_LONG_EDGE` equal to 1568 with the Pitfall-5 comment
    - The test asserts the produced base64 contains no `data:` substring
    - No `test/` subdirectory is created (co-located convention)
  </acceptance_criteria>
  <done>Pure downscale math and prefix-free base64 encoder exist and are unit-tested green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScreenshotService (active-monitor capture) + pure source-select test (Wave 0)</name>
  <files>src/main/vision/screenshot.service.ts, src/main/vision/screenshot.service.test.ts</files>
  <read_first>
    - src/main/audio/audio-capture.service.ts (main-process service-class analog — by-convention singleton, no @singleton(), report-don't-throw fault callback)
    - src/main/overlay-window.manager.ts (the screen.* + BrowserWindow.getBounds() usage precedent; lines ~343-353 react to display change)
    - src/main/ai/ai-orchestrator.ts (lines 96-103 — the verbatim "main process has no TSyringe container" @remarks block to copy)
    - 07-RESEARCH.md §2 (captureOverlayDisplay shape), Pattern 2 (display.id is number, source.display_id is string — compare String(display.id)), Pitfall 3 (capture at real pixels via scaleFactor)
    - 07-PATTERNS.md "NEW src/main/vision/screenshot.service.ts"
  </read_first>
  <behavior>
    - selectSourceForDisplay(sources, displayId): returns the source whose display_id === String(displayId); falls back to sources[0] when no match; returns undefined for an empty sources array
  </behavior>
  <action>
    Create `src/main/vision/screenshot.service.ts`. Export a pure helper `selectSourceForDisplay(sources, displayId): DesktopCapturerSource | undefined` that matches `source.display_id === String(displayId)` and falls back to `sources[0]` (Pattern 2 — `display.id` is a number, `display_id` is a string). Create a `ScreenshotService` class (by-convention singleton; copy the no-TSyringe `@remarks` note verbatim from `AiOrchestrator`) with `async captureForOverlay(overlay: BrowserWindow): Promise<{ base64: string; mediaType: string }>` that: resolves `screen.getDisplayMatching(overlay.getBounds())` (D-01); calls `desktopCapturer.getSources({ types: ['screen'], thumbnailSize: { width: Math.round(display.size.width * display.scaleFactor), height: Math.round(display.size.height * display.scaleFactor) } })` (Pitfall 3); selects the source via `selectSourceForDisplay`; and returns `toBase64Png(source.thumbnail)` from `downscale.utility.ts`. Capture faults must REPORT not crash — surface a failed `getSources` so the orchestrator can push an inline error entry (mirror the audio-capture fault discipline at `index.ts:221-224`); do NOT swallow silently. NO renderer canvas, NO IPC of image bytes (IN-01). Write the co-located `screenshot.service.test.ts` exercising `selectSourceForDisplay` with mocked sources (match, fallback, empty) — `desktopCapturer`/`screen` are mocked or simply not exercised in the pure-helper test.
  </action>
  <verify>
    <automated>npx vitest run src/main/vision/screenshot.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/vision/screenshot.service.test.ts` exits 0
    - `selectSourceForDisplay` matches by `String(display.id)`, falls back to `sources[0]`, returns undefined on empty
    - `captureForOverlay` uses `screen.getDisplayMatching(overlay.getBounds())` and a `thumbnailSize` scaled by `scaleFactor`
    - The service class carries the verbatim no-TSyringe `@remarks` note; no `@singleton()` decorator
  </acceptance_criteria>
  <done>The active-monitor capture+downscale service exists with a unit-tested pure source-select helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Image seam on the AI path — interface, assembler branch, gateway, orchestrator, chord, wiring, vision panel</name>
  <files>src/main/ai/ai-gateway.interface.ts, src/main/ai/prompt-assembler.ts, src/main/ai/prompt-assembler.test.ts, src/main/ai/anthropic-ai.gateway.ts, src/main/ai/ai-orchestrator.ts, src/main/ai/ai-orchestrator.test.ts, src/main/hotkey-registrar.service.ts, src/main/overlay-window.manager.ts, src/main/index.ts, src/renderer/src/components/vision-panel.tsx, src/renderer/src/App.tsx</files>
  <read_first>
    - src/main/ai/ai-gateway.interface.ts (the AiMode union + IAiPromptRequest to widen — line 20, 29-38; TSDoc already says "Phase 7 adds a vision mode")
    - src/main/ai/prompt-assembler.ts (assemblePrompt at 127-133; ANSWER_SYSTEM_PROMPT/TALKING_POINTS_SYSTEM_PROMPT as the DRAFT-tunable const + caveat analog)
    - src/main/ai/prompt-assembler.test.ts (extend — add image-branch + text-mode-unchanged cases)
    - src/main/ai/anthropic-ai.gateway.ts (stream() at 71-99 — the one-line content type widening; sanitizeAiError discipline)
    - src/main/ai/ai-orchestrator.ts (per-mode constants 26-37; trigger() 140-183; empty-span guard 144; constructor took getActiveContext as 5th arg)
    - src/main/ai/ai-orchestrator.test.ts (extend — empty-span bypass + 3-mode single-in-flight cross-cancel)
    - src/main/hotkey-registrar.service.ts (HOTKEY_CHORDS entries 53-112; the open-settings entry at 105-111 is the closest template)
    - src/main/index.ts (buildHandlers map 83-143; 'ai-answer' handler at ~125; aiOrchestrator construction at ~331; focus-cycle handler 118-121; window in scope at ~309)
    - src/main/overlay-window.manager.ts (activePanel type at lines 144/152/161 — setActivePanel/getActivePanel/module flag; setContentProtection at 336/395)
    - src/renderer/src/components/ai-panel.tsx (the ENTIRE structure the vision panel copies — locally-declared mirror types 6-25, reduceEntries 73-94, stickToBottomRef + focused-panel scroll 136-194, data-testid conventions 197-206)
    - src/renderer/src/App.tsx (where AiPanel is composed — add VisionPanel as a sibling)
    - 07-RESEARCH.md §1 (content blocks + SDK call), §6 (latency log — startMs before async capture), Pattern 3 (empty-span bypass), Security Domain (render escaped text, NO dangerouslySetInnerHTML)
    - 07-PATTERNS.md all "EDIT" sections + the vision-panel section
  </read_first>
  <behavior>
    - assemblePrompt with no image returns userContent as the SAME string it returns today (text modes byte-for-byte unchanged) and selects ANSWER/TALKING_POINTS system prompt
    - assemblePrompt with an image present returns userContent as the block array [{type:'image',source:{type:'base64',media_type,data}},{type:'text',text}] (image BEFORE text) and selects VISION_SYSTEM_PROMPT; the text block reuses the same contextBlock + transcript span (D-07 grounding)
    - orchestrator.trigger('code-challenge') does NOT short-circuit on an empty transcript span (image alone is actionable); other modes still short-circuit
    - orchestrator: re-pressing code-challenge mid-stream cancels it; pressing answer/talking-points mid-vision-stream cancels vision and starts the new one (single-in-flight across all three modes)
  </behavior>
  <action>
    Widen `AiMode` to `'answer' | 'talking-points' | 'code-challenge'` in `ai-gateway.interface.ts`; add `image?: { base64: string; mediaType: string }` to `IAiPromptRequest` and widen `userContent` to `string | Anthropic.ContentBlockParam[]` (a `type`-only import of `Anthropic.ContentBlockParam` is acceptable — no runtime dependency). In `prompt-assembler.ts` add `VISION_SYSTEM_PROMPT` (same `export const` + DRAFT-caveat TSDoc style as the existing prompts; draft per RESEARCH §1: solve the code challenge in the image, use context/transcript as supporting constraints, lead with one-line approach then code then brief complexity note, no preamble); add `image?: { base64; mediaType }` to `IAssembleInput`; widen `IAssembledPrompt.userContent`; branch so an absent image is byte-for-byte identical to today and a present image selects `VISION_SYSTEM_PROMPT` and returns the block array with the SAME `contextBlock + transcript span` text block (image first). In `anthropic-ai.gateway.ts` widen only the `content` type at the `messages: [{ role:'user', content: request.userContent }]` call site — no logic change; NEVER log the image base64 (extend the existing T-5-02 no-payload rule). In `ai-orchestrator.ts`: add `export const CODE_CHALLENGE_MODEL = 'claude-opus-4-8'` (D-06) and add the `'code-challenge': 1500` key to the `MAX_TOKENS` record (compiler-forced once AiMode widens — A2; tune at the latency gate); change the empty-span guard to `if (mode !== 'code-challenge' && span.trim().length === 0)` (Pattern 3); in the code-challenge branch capture `startMs` BEFORE awaiting the capture (RESEARCH §6 so latency includes capture), make the branch async — `await` `ScreenshotService.captureForOverlay(window)`, then `assemblePrompt({ mode, span, context: this.getActiveContext(), image })` and route `CODE_CHALLENGE_MODEL`; if capture reports a fault, push an inline `error` entry (reuse the existing AI-error push path) rather than crashing; reuse the single-in-flight/cancel/request-id-guard/debounce/first-token-log machinery UNCHANGED (D-11). Add the `ScreenshotService` (or a `() => captureForOverlay(window)` closure) as a new constructor arg the SAME way `getActiveContext` was threaded. In `hotkey-registrar.service.ts` add `{ label: 'capture-code-challenge', keycode: UiohookKey.C, accelerator: 'Ctrl+Alt+C', kind: 'discrete' }` to `HOTKEY_CHORDS` with TSDoc noting `C` is outside the locked conflict-tested set and the 02-03 Teams/Zoom/VS Code re-check is pending with a documented fallback letter (CTL-03); NO registrar logic change. In `index.ts`: add `'capture-code-challenge': (): void => aiOrchestrator.trigger('code-challenge')` to `buildHandlers` (key MUST match the chord label); construct `ScreenshotService` once at the entry point and thread it (and `window`) into the `AiOrchestrator` constructor; extend the `focus-cycle` handler to cycle transcript -> ai -> vision. Widen the `activePanel` type `'transcript' | 'ai'` to add `'vision'` in `overlay-window.manager.ts` (setActivePanel/getActivePanel/module flag) and in the renderer `IOverlayStatus` mirror. Do NOT add separate overlay-hide capture logic — content-protection self-exclusion (D-02) is VERIFIED at the manual gate in 07-02, not coded here. Create `src/renderer/src/components/vision-panel.tsx` copying the entire `ai-panel.tsx` structure (locally-declared `AiMode` mirror widened to include `'code-challenge'`, `reduceEntries`, `stickToBottomRef` + focused-panel scroll guarding on `'vision'`, bounded local history per D-09); filter to keep only `code-challenge`-mode entries (D-08 dedicated panel); render streamed code as escaped text / `<pre>` — NEVER `dangerouslySetInnerHTML` (Security Domain); use `card-vision-panel` / `row-vision-entry-${id}` data-testids, SCSS class convention, no inline `style=`. Compose `VisionPanel` as a sibling in `App.tsx`. Extend `prompt-assembler.test.ts` (image-branch block array iff image present + text-mode-unchanged regression) and `ai-orchestrator.test.ts` (code-challenge empty-span bypass + 3-mode single-in-flight cross-cancel).
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/prompt-assembler.test.ts src/main/ai/ai-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/ai/prompt-assembler.test.ts src/main/ai/ai-orchestrator.test.ts` exits 0
    - prompt-assembler test asserts: no-image path returns a string identical to the pre-change output (text modes byte-identical); image path returns a block array with the image block before the text block
    - ai-orchestrator test asserts: code-challenge triggers with an empty transcript span (no `empty` short-circuit); re-press cancels; cross-mode cancels-and-starts (one active request)
    - `MAX_TOKENS` has a `'code-challenge'` key (compiler error if missing once AiMode widens); `CODE_CHALLENGE_MODEL === 'claude-opus-4-8'`
    - `HOTKEY_CHORDS` contains a `capture-code-challenge` entry (`Ctrl+Alt+C`, discrete) and `index.ts` `buildHandlers` has the matching handler wired
    - `vision-panel.tsx` contains no `dangerouslySetInnerHTML`
    - `npm test` (full suite) exits 0
  </acceptance_criteria>
  <done>Vision rides an optional image field on the existing AI seam; the chord, panel, three-way focus cycle, and grounding are wired; text modes are unchanged; full unit suite green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Confirm vision API shape + model id via the claude-api skill</name>
  <action>Consult the claude-api skill (ROADMAP research flag "Phase 7 API shape") and confirm: the model id claude-opus-4-8, the base64-no-data:-prefix image content-block shape, the supported image media types, and a sane vision max_tokens ceiling. Present the decision below.</action>
  <decision>Confirm the Anthropic vision request shape and model id via the claude-api skill before relying on them</decision>
  <context>The ROADMAP research flag "Phase 7 API shape" mandates the `claude-api` skill as authoritative. RESEARCH A1 sourced the shape from official Anthropic docs (the same source the skill wraps) and flagged the skill-availability step as unmet. The skill is not present in `.claude/skills/` or the user skills dir, but the planning_context states it IS available this session. Re-verify before the manual end-to-end gate so a wrong model id / block shape does not silently fail the vision call.</context>
  <options>
    <option id="skill-confirms">
      <name>claude-api skill confirms the facts</name>
      <pros>Model id `claude-opus-4-8`, base64-no-`data:`-prefix block shape, supported media types, and a sane vision max_tokens ceiling are all confirmed authoritative — proceed unchanged</pros>
      <cons>None</cons>
    </option>
    <option id="skill-corrects">
      <name>claude-api skill corrects a fact</name>
      <pros>Catch a wrong model id or block shape before the live gate</pros>
      <cons>Update `CODE_CHALLENGE_MODEL`, the block shape in `prompt-assembler.ts`, and/or the `'code-challenge'` MAX_TOKENS value, then re-run the unit suite</cons>
    </option>
  </options>
  <resume-signal>Confirm "skill-confirms" (proceed) or describe the correction needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured screen -> main | Local screen content is captured as an image; trusted source (the user's own screen), processed entirely in main |
| main -> Anthropic API | Image base64 + prompt leave the machine over TLS to Anthropic |
| main -> renderer | One-way `jedi:ai` push of streamed text + active-panel state; no renderer->main control surface for vision (IN-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-IL | Information Disclosure | ai-orchestrator latency log / gateway error path | mitigate | Latency log carries `mode/model/latencyMs` only (verified `ai-orchestrator.ts:213`); NEVER log the image base64; reuse `sanitizeAiError` (T-5-02) which strips key-adjacent error bodies |
| T-7-XSS | Tampering/Information Disclosure | vision-panel.tsx rendering streamed code | mitigate | Render streamed code as escaped text / `<pre>`; React escapes by default; NO `dangerouslySetInnerHTML` |
| T-7-PII | Information Disclosure | sensitive on-screen content sent to Anthropic | accept | Documented/accepted in PROJECT.md (audio + AI calls leave the machine for v1); the user controls when to press the chord; no new exposure beyond existing policy |
| T-7-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan (RESEARCH Package Legitimacy Audit: phase adds zero dependencies — all Electron built-ins or already-present); nothing to slopcheck |
</threat_model>

<verification>
- `npm test` (full Vitest suite) green: downscale math, source-select helper, assembler image branch, orchestrator 3-mode state machine
- Text modes (answer/talking-points) provably unchanged via the regression case in `prompt-assembler.test.ts`
- `vision-panel.tsx` contains no `dangerouslySetInnerHTML`
- Live capture -> stream -> vision panel is manual/integration — verified end-to-end in the 07-02 packaged gate (D-13d) and recorded in 07-VERIFICATION.md
</verification>

<success_criteria>
- Pressing `Ctrl+Alt+C` captures the overlay's monitor, downscales to <=1568px, and streams a `claude-opus-4-8` solution into a dedicated vision panel grounded in active context + transcript span (AI-03)
- Base64 has no `data:` prefix (unit-asserted)
- Vision triggers even with an empty transcript span; single-in-flight holds across all three modes
- Answer/talking-points prompts are byte-for-byte unchanged
</success_criteria>

<output>
Create `.planning/phases/07-screenshot-vision-packaging-hardening/07-01-SUMMARY.md` when done.
</output>
