---
phase: 07-screenshot-vision-packaging-hardening
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/main/index.ts
  - docs/HARDENING.md
autonomous: true
requirements: [PKG-01]

must_haves:
  truths:
    - "Hotkey->first-token latency is logged for the code-challenge mode in main (mode/model/latencyMs only, no payload)"
    - "An opt-in JEDI_DISABLE_GPU=1 env flag disables hardware acceleration before app.ready (transparency fallback)"
    - "SmartScreen 'Run anyway' friction is documented as accepted, with the GPU-fallback and hotkey-recovery toggles"
    - "Hotkey registration failures surface visibly in the packaged build (CTL-03 recovery path hardened)"
    - "Covers CONTEXT decision D-15 (full hardening set: latency, SmartScreen doc, GPU fallback, hotkey recovery); D-16 (Whisper stub DROPPED — correctly not built)"
  artifacts:
    - path: "src/main/index.ts"
      provides: "Top-level JEDI_DISABLE_GPU guard before app.ready"
      contains: "disableHardwareAcceleration"
    - path: "docs/HARDENING.md"
      provides: "SmartScreen friction + GPU fallback + hotkey-recovery documentation"
      contains: "SmartScreen"
  key_links:
    - from: "src/main/index.ts"
      to: "app.disableHardwareAcceleration()"
      via: "JEDI_DISABLE_GPU=1 guard at module top before any app.whenReady"
      pattern: "JEDI_DISABLE_GPU"
---

<objective>
Complete the ROADMAP 07-03 hardening set: confirm hotkey->first-token latency instrumentation covers the vision mode, add the opt-in `JEDI_DISABLE_GPU=1` hardware-acceleration fallback at the top of `index.ts` (before `app.ready`), harden the hotkey-registration-failure recovery/visibility path (CTL-03), and document SmartScreen friction as accepted in a hardening doc.

Purpose: Satisfy PKG-01 success criterion 4 (latency instrumented + SmartScreen documented as accepted friction) and finish the hardening so the packaged build is robust on the target machine (CONTEXT D-15). The Whisper stub is DROPPED (D-16 — v2 / out of scope; the ISttProvider seam already proves swappability via TRN-05).
Output: A GPU-fallback guard, a confirmed latency log for all three modes, a hardened CTL-03 path, and `docs/HARDENING.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-screenshot-vision-packaging-hardening/07-CONTEXT.md
@.planning/phases/07-screenshot-vision-packaging-hardening/07-RESEARCH.md
@.planning/phases/07-screenshot-vision-packaging-hardening/07-PATTERNS.md

<interfaces>
<!-- src/main/index.ts head — imports start at line 1; app/BrowserWindow already imported (line 2). -->
<!-- The GPU guard goes synchronously after imports, BEFORE any app.whenReady() hook. -->

<!-- Latency log already exists in ai-orchestrator.ts (~line 213):
     '[ai] first-token mode=… model=… latencyMs=…' — logs mode/model/latencyMs ONLY, never payload.
     Adding code-challenge as a third mode (07-01) makes this log fire for vision automatically (RESEARCH §6).
     07-01 already captures startMs before the async capture so latency includes capture time the user feels. -->

<!-- Hotkey registration failure surfacing already exists: register().failed -> setHotkeyStatus (CTL-03). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hardware-accel fallback guard + confirm vision latency log + harden CTL-03 recovery</name>
  <files>src/main/index.ts</files>
  <read_first>
    - src/main/index.ts (imports 1-30; `app` already imported line 2; the `setHotkeyStatus` import line 14 + its register().failed usage; the buildHandlers + aiOrchestrator wiring)
    - src/main/ai/ai-orchestrator.ts (the existing `[ai] first-token` log ~line 213 — confirm it fires for code-challenge with no new code; startMs captured before async capture per 07-01)
    - src/main/hotkey-registrar.service.ts (the `register()` result shape with `.failed`, and setHotkeyStatus surfacing — the CTL-03 path to harden)
    - 07-RESEARCH.md §5 (GPU guard placement), Pitfall 6 (must run before app.ready; no-op if after), §6 (latency log — no new code), D-15
    - 07-PATTERNS.md "EDIT src/main/index.ts" sections (1) GPU guard, (4) and the CTL-03 chord-wiring note
  </read_first>
  <action>
    At the TOP of `src/main/index.ts`, synchronously after the import block and BEFORE any `app.whenReady()` call, add the opt-in guard: `if (process.env.JEDI_DISABLE_GPU === '1') { app.disableHardwareAcceleration(); }` (Pitfall 6 — a no-op if called after ready; `app` is already imported at line 2). Confirm the vision latency log requires NO new code — the existing `[ai] first-token mode=… model=… latencyMs=…` in `ai-orchestrator.ts` (~line 213) fires for the third `code-challenge` mode automatically (RESEARCH §6); if 07-01 did not already capture `startMs` before the async capture, ensure it does so capture time is included (the user feels it). Harden the CTL-03 recovery/visibility path: when `register()` returns any `failed` chords (including the new `capture-code-challenge`), ensure they are surfaced via the existing `setHotkeyStatus` path so a registration failure in the package is VISIBLE (never silently dropped) — keep the existing failure-surfacing mechanism, add the new chord to its coverage if not already implicit, and ensure the failed-chord set is logged in main. Do NOT log any secret or payload. NEVER call `app.disableHardwareAcceleration()` unconditionally — it MUST stay behind the env flag so hardware acceleration is the default and the fallback is opt-in.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/main/index.ts','utf8'); if(!s.includes('JEDI_DISABLE_GPU')||!s.includes('disableHardwareAcceleration')) process.exit(1); const gpuIdx=s.indexOf('disableHardwareAcceleration'); const readyIdx=s.indexOf('whenReady'); if(readyIdx!==-1 && gpuIdx>readyIdx) { console.error('GPU guard must precede whenReady'); process.exit(1);} console.log('GPU guard placed before whenReady')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/index.ts` contains the `JEDI_DISABLE_GPU === '1'` guard calling `app.disableHardwareAcceleration()` and it appears BEFORE the first `app.whenReady()` reference
    - The GPU disable is gated behind the env flag (not unconditional)
    - The `capture-code-challenge` chord is covered by the `register().failed` -> `setHotkeyStatus` surfacing (CTL-03)
    - `npm test` (full Vitest suite) exits 0 (no regression)
  </acceptance_criteria>
  <done>The opt-in GPU fallback runs before app.ready, the vision latency log is confirmed, and the CTL-03 recovery path is hardened.</done>
</task>

<task type="auto">
  <name>Task 2: docs/HARDENING.md — SmartScreen friction, GPU fallback, hotkey recovery</name>
  <files>docs/HARDENING.md</files>
  <read_first>
    - 07-RESEARCH.md §5 + Pitfall 6 (GPU flag), D-15 (SmartScreen accepted friction + the four hardening items)
    - 07-CONTEXT.md D-12/D-15 (portable unsigned .exe, SmartScreen accepted, NOT a code-signing cert) and Deferred Ideas (code signing out of scope)
    - .planning/phases/07-screenshot-vision-packaging-hardening/07-VERIFICATION.md (reference the GPU-fallback toggle from the packaged gate)
  </read_first>
  <action>
    Create `docs/HARDENING.md` documenting: (1) SMARTSCREEN — the unsigned portable `.exe` triggers a Windows SmartScreen warning; document this as ACCEPTED friction for a personal single-user tool (D-12/D-15), with the exact "More info" -> "Run anyway" click-through steps; state explicitly that a code-signing certificate is out of scope (CONTEXT Deferred Ideas). (2) HARDWARE-ACCELERATION FALLBACK — if the overlay transparency glitches on the target machine in the packaged build, relaunch with `JEDI_DISABLE_GPU=1` set (document how to set the env var for a portable `.exe` launch); note this is opt-in and that hardware acceleration is the default; note electron#51363 (the flag no longer fully kills the GPU process on Electron 38+) is irrelevant here because Electron is pinned at 35.7.5. (3) HOTKEY-CONFIG RECOVERY — if a hotkey fails to register in the package, the failure surfaces via the HUD/status path (CTL-03, never silently dropped); document the documented fallback letter for `Ctrl+Alt+C` if it collides on the machine, and that the user should check the status line. (4) LATENCY — note that hotkey->first-token latency is logged in main (`[ai] first-token …`) for all three modes including vision (capture time included), and that the `'code-challenge'` MAX_TOKENS (1500) is the tunable knob if the solution is truncated or too slow. Keep it concise and operational (markdown, no code-signing detour).
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('docs/HARDENING.md','utf8'); for(const t of ['SmartScreen','JEDI_DISABLE_GPU','Run anyway','Ctrl+Alt+C']){ if(!d.includes(t)){ console.error('missing: '+t); process.exit(1);} } console.log('hardening doc complete')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/HARDENING.md` documents SmartScreen "Run anyway" as accepted friction (code signing out of scope)
    - It documents the `JEDI_DISABLE_GPU=1` opt-in transparency fallback
    - It documents the CTL-03 hotkey-failure visibility + the `Ctrl+Alt+C` fallback-letter path
    - It notes the main-only latency log covers the vision mode
  </acceptance_criteria>
  <done>The hardening doc records SmartScreen friction, the GPU fallback, hotkey recovery, and latency instrumentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env -> main | `JEDI_DISABLE_GPU` is a local, user-set env var read once at process start; not attacker-controlled on a single-user machine |
| main -> log sink | Latency/registration logs go to the main-process console only; must carry no secret or payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-IL2 | Information Disclosure | latency / hotkey-failure logging in index.ts + orchestrator | mitigate | Logs carry `mode/model/latencyMs` and failed-chord labels only; NEVER the transcript, the key, the image base64, or an error payload (reuse the T-5-02 discipline) |
| T-7-SS | Spoofing | unsigned portable .exe / SmartScreen | accept | Documented in `docs/HARDENING.md` as accepted friction; code signing out of scope for a personal tool (D-15 / CONTEXT Deferred Ideas) |
| T-7-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan (RESEARCH Package Legitimacy Audit: zero new dependencies); nothing to slopcheck |
</threat_model>

<verification>
- `JEDI_DISABLE_GPU=1` guard calls `app.disableHardwareAcceleration()` before `app.whenReady()` (verified by the placement check)
- Vision latency logs via the existing `[ai] first-token` path with no payload
- `register().failed` surfaces the new chord visibly (CTL-03)
- `docs/HARDENING.md` covers SmartScreen, GPU fallback, hotkey recovery, latency
- `npm test` green (no regression)
</verification>

<success_criteria>
- Latency is instrumented for the vision mode (hotkey->first-token, main-only, no payload) (PKG-01 criterion 4)
- SmartScreen behavior is documented as accepted friction (PKG-01 criterion 4)
- The opt-in hardware-acceleration fallback exists and runs before app.ready
- The hotkey-config recovery path is hardened and visible (CTL-03)
</success_criteria>

<output>
Create `.planning/phases/07-screenshot-vision-packaging-hardening/07-03-SUMMARY.md` when done.
</output>
