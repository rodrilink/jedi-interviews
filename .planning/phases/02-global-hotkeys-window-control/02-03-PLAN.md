---
phase: 02-global-hotkeys-window-control
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/main/hotkey-registrar.service.ts
  - src/main/hotkey-registrar.service.test.ts
  - src/renderer/src/components/debug-hud.tsx
  - .planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md
autonomous: false
requirements: [CTL-02, CTL-03, OVL-05, OVL-03, CTL-01]

must_haves:
  truths:
    - "Each chord in the locked action set is empirically tested against Teams, Zoom, and VS Code holding focus"
    - "The final default Ctrl+Alt chord set contains no chord that collides with the tested meeting apps' accelerators"
    - "The focused app's own Ctrl+Alt accelerators still fire after each overlay chord (non-consumption proven empirically)"
    - "The finalized chords are recorded in a committed conflict-test document with per-app results"
    - "The HUD cheat-sheet and registrar constants reflect the finalized chords"
    - "After the chord swap, the Hotkeys: OK / N failed failure-surfacing path still works (CTL-03 not regressed)"
  artifacts:
    - path: ".planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md"
      provides: "Per-app (Teams/Zoom/VS Code) conflict-test matrix incl. an app-own-accelerator column and the signed-off final chord set"
      contains: "Final chord set"
    - path: "src/main/hotkey-registrar.service.ts"
      provides: "Finalized (post-testing) Ctrl+Alt chord constants replacing the 02-01 placeholders"
      contains: "Ctrl+Alt"
  key_links:
    - from: "src/main/hotkey-registrar.service.ts"
      to: ".planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md"
      via: "chord constants match the document's signed-off final set"
      pattern: "Ctrl\\+Alt"
    - from: "src/renderer/src/components/debug-hud.tsx"
      to: "finalized chords"
      via: "cheat-sheet labels updated to the final chords"
      pattern: "row-hotkey-"
---

<objective>
Close the phase by empirically testing the locked five-group hotkey action set against the three real apps the user runs (Teams, Zoom, VS Code) while each holds focus, then finalizing the concrete default Ctrl+Alt chord set so no shipped chord collides with a meeting app's accelerator (D-05). Any colliding placeholder chord is replaced, the registrar constants and the HUD cheat-sheet are updated to the final set, the registrar tests (incl. the failure-surfacing path) are kept green, and the results are recorded in a committed conflict-test document.

This is the final vertical slice and the empirical close of CTL-02: it proves on the real machine that the chosen chords fire while a real meeting app is focused WITHOUT stealing that app's accelerators (the non-consumption guarantee that distinguishes uiohook from globalShortcut) — consistent with the project's "verify on the real machine" discipline. After this plan the keyboard control loop ships with a conflict-free default chord set and the CTL-03 failure-surfacing path is confirmed intact after the chord swap.

Purpose: Replace the placeholder chords with an empirically conflict-free default set, prove non-consumption, confirm CTL-03 survives the swap, and record the evidence.
Output: Updated chord constants + HUD cheat-sheet, refreshed registrar tests, and `02-HOTKEY-CONFLICT-TEST.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-global-hotkeys-window-control/02-CONTEXT.md
@.planning/phases/02-global-hotkeys-window-control/02-PATTERNS.md
@.planning/phases/02-global-hotkeys-window-control/02-01-SUMMARY.md
@.planning/phases/02-global-hotkeys-window-control/02-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Produced by 02-01/02-02. -->

From 02-01 (src/main/hotkey-registrar.service.ts): the locked action set lives in a module-level
chord-constant registry, currently the PLACEHOLDER chords:
  show/hide -> Ctrl+Alt+J
  move      -> Ctrl+Alt+Left / Right / Up / Down
  opacity   -> Ctrl+Alt+[ (down) / Ctrl+Alt+] (up)
  HUD toggle-> Ctrl+Alt+H
  quit      -> Ctrl+Alt+Q
register() returns IHotkeyRegistrationResult { active, failed } and surfaces failures in the HUD
(Hotkeys: OK / N failed). The registrar test file may assert specific chord strings AND covers the
failure-surfacing path (a subset of chords failing yields a non-empty `failed`).

From 02-02 (src/renderer/src/components/debug-hud.tsx): the HUD renders a cheat-sheet with
`row-hotkey-*` data-testid rows showing each action's chord. These labels must track the final chords.

The ACTION SET is locked (D-05) — this plan changes only the concrete chord assignments
(within the Ctrl+Alt family), not which actions exist.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Empirically test the placeholder chords against Teams, Zoom, and VS Code; record results</name>
  <read_first>
    - .planning/phases/02-global-hotkeys-window-control/02-CONTEXT.md (D-05 chords deferred to conflict testing; suggested starting set; the "verify on the real machine" framing in §Specific Ideas)
    - .planning/phases/02-global-hotkeys-window-control/02-01-SUMMARY.md and 02-02-SUMMARY.md (what was built; which layer — uiohook or globalShortcut fallback — ended up active)
    - src/main/hotkey-registrar.service.ts (the current placeholder chord constants to test)
    - .planning/phases/01-overlay-shell-existential-behaviors (the VERIFICATION.md pattern for committed on-machine evidence, as a documentation analog)
  </read_first>
  <action>
    The executor automates everything it can first: confirm `npm run dev` launches the overlay with the placeholder chords active and the HUD showing `Hotkeys: OK`, and scaffold the results document `.planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md` with a per-app matrix (rows = the five action groups / their placeholder chords: Ctrl+Alt+J, Ctrl+Alt+Arrows, Ctrl+Alt+[ , Ctrl+Alt+] , Ctrl+Alt+H, Ctrl+Alt+Q). The matrix has THREE result columns per app (Teams, Zoom, VS Code): (1) "Overlay action fires?" (does the chord trigger the overlay behavior), (2) "Chord swallowed by app?" (does the focused app consume/block it), and (3) "App's own Ctrl+Alt behavior (if any) still fires?" — i.e. if the focused app binds that Ctrl+Alt chord to its own accelerator, does that accelerator STILL fire (proving uiohook's passive non-consumption, the empirical half of CTL-02), or note "n/a" if the app binds nothing to that chord. Also add a `Final chord set` section and a sign-off line (date + target machine). Then this is a human-verify checkpoint because only the user, on the target Windows 11 machine with Teams/Zoom/VS Code actually open and focused, can observe these outcomes. The user fills every cell. For any collision (e.g. an app binds Ctrl+Alt+<letter> and it gets swallowed, OR the overlay chord stops the app's own behavior), the user notes a replacement chord within the Ctrl+Alt family. No chord may be shipped that collides — a colliding placeholder MUST be replaced (D-05).
  </action>
  <verify>
    <automated>npm run dev &amp; sleep 8; test -f .planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md &amp;&amp; echo SCAFFOLD_OK</automated>
    <what-built>Overlay running (`npm run dev`) with placeholder chords; `02-HOTKEY-CONFLICT-TEST.md` matrix scaffolded for Teams/Zoom/VS Code across the five action groups with three result columns each (fires / swallowed / app-own-accelerator-still-fires).</what-built>
    <how-to-verify>
      1. Run `npm run dev`; confirm the HUD shows `Hotkeys: OK` (or note the failed count).
      2. Open Teams, Zoom, and VS Code. For each, give it focus and press every placeholder chord. Record THREE things per chord/app: (a) the overlay action fired, (b) whether the focused app swallowed the chord, and (c) whether the app's OWN Ctrl+Alt accelerator (if it has one on that chord) still fired despite the overlay also acting — this is the non-consumption proof.
      3. Record each result in the three-column matrix in `02-HOTKEY-CONFLICT-TEST.md`. For any collision, write the chosen replacement chord (Ctrl+Alt family).
      4. Fill the `Final chord set` section with the conflict-free chords and sign off (date + machine).
    </how-to-verify>
    <resume-signal>Type "approved" with the final chord set once ALL THREE columns are filled for ALL rows across Teams, Zoom, and VS Code (or "all placeholders pass" if none collide and every app-own accelerator still fired), or list the chords that must change and their replacements.</resume-signal>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md` exists with a filled matrix covering all five action groups across Teams, Zoom, and VS Code, with all THREE result columns (fires / swallowed / app-own-accelerator-still-fires) filled for every row.
    - The document contains a `Final chord set` section listing one concrete Ctrl+Alt chord per action group, none marked as colliding.
    - The document is signed off with a date and the target machine identifier.
  </acceptance_criteria>
  <done>The placeholder chords are tested against all three apps on the target machine across all three result columns (incl. the app's-own-accelerator non-consumption proof), results recorded, and a conflict-free final Ctrl+Alt chord set is chosen and signed off in 02-HOTKEY-CONFLICT-TEST.md.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the finalized chord set to the registrar + HUD cheat-sheet and update tests/docs</name>
  <files>src/main/hotkey-registrar.service.ts, src/main/hotkey-registrar.service.test.ts, src/renderer/src/components/debug-hud.tsx, .planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md</files>
  <read_first>
    - .planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md (Task 1 output — the signed-off Final chord set; the single source of truth for the chords to apply)
    - src/main/hotkey-registrar.service.ts (the placeholder chord-constant registry to update; remove the PLACEHOLDER TSDoc note now that chords are finalized)
    - src/main/hotkey-registrar.service.test.ts (any test that asserts specific chord strings must track the final set; the failure-surfacing test cases must stay green)
    - src/renderer/src/components/debug-hud.tsx (the `row-hotkey-*` cheat-sheet labels to update)
    - .planning/phases/02-global-hotkeys-window-control/02-CONTEXT.md (D-05 action set locked, only chords change; D-13 HUD cheat-sheet doubles as on-screen reference; D-06 Hotkeys: OK/failed status line)
  </read_first>
  <action>
    Replace the placeholder Ctrl+Alt chord constants in hotkey-registrar.service.ts with the signed-off Final chord set from 02-HOTKEY-CONFLICT-TEST.md, one chord per locked action group (do not add/remove actions — D-05 locks the action set). Remove or update the "PLACEHOLDER pending 02-03" TSDoc note to state the chords are the conflict-tested defaults, citing 02-HOTKEY-CONFLICT-TEST.md. Update any chord-string assertions in hotkey-registrar.service.test.ts to match the final set so the suite stays green — and explicitly confirm the failure-surfacing test cases (a subset of chords failing yields a non-empty `failed` without throwing, CTL-03) still pass unchanged after the chord swap, since swapping a chord constant must NOT silently regress the `{ active, failed }` aggregation path. Update the HUD cheat-sheet labels in debug-hud.tsx (`row-hotkey-*` rows) to show the final chords so the on-screen reference is accurate (D-13). Mark Phase 2 requirements as covered in the conflict-test doc's footer (OVL-03, OVL-05, CTL-01, CTL-02, CTL-03). If Task 1 found that all placeholders passed, this task confirms-in-place: keep the chords, drop the PLACEHOLDER wording, and note "all suggested defaults verified conflict-free" in the doc.
  </action>
  <verify>
    <automated>npx vitest run src/main/hotkey-registrar.service.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The chord constants in hotkey-registrar.service.ts exactly match the `Final chord set` in 02-HOTKEY-CONFLICT-TEST.md (verify by reading both).
    - The "PLACEHOLDER pending 02-03" wording is gone from hotkey-registrar.service.ts.
    - HUD `row-hotkey-*` cheat-sheet labels in debug-hud.tsx show the finalized chords.
    - After updating the chord constants, `npx vitest run src/main/hotkey-registrar.service.test.ts` exits 0 with the failure-surfacing test cases (non-empty `failed`, no throw — CTL-03) still passing; any chord-string assertions were updated to the final set rather than deleted.
    - In `npm run dev` the HUD still shows `Hotkeys: OK` with the finalized set (no chord regressed to failing), and the `N failed` path remains reachable per the green failure-surfacing tests.
    - `npx vitest run src/main/hotkey-registrar.service.test.ts` exits 0; `npm run typecheck` exits 0; `npm run lint` exits 0.
    - The conflict-test doc footer records all five Phase 2 requirements (OVL-03, OVL-05, CTL-01, CTL-02, CTL-03) as covered.
  </acceptance_criteria>
  <done>The registrar, its tests, and the HUD cheat-sheet all reflect the finalized conflict-free Ctrl+Alt chord set recorded in 02-HOTKEY-CONFLICT-TEST.md; the CTL-03 failure-surfacing path is confirmed intact after the chord swap; tests, typecheck, and lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS keyboard -> uiohook/globalShortcut (main) | The finalized chords are observed app-wide; a poorly chosen chord could collide with a meeting app accelerator. |
| committed planning doc | The conflict-test evidence is committed under .planning/ — must contain no secrets/keystroke content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Denial of Service | a shipped chord colliding with a meeting app accelerator | mitigate | Task 1 empirically tests every chord against Teams/Zoom/VS Code on the target machine across three columns (fires / swallowed / app-own-accelerator-still-fires); any collision is replaced before finalizing (D-05). No colliding chord ships. |
| T-02-10 | Information Disclosure | committed 02-HOTKEY-CONFLICT-TEST.md | mitigate | The document records only action labels, chord names, and pass/collide results — no keystroke content, no secrets. uiohook observes only the specific chords (T-02-01 carried from 02-01). |

No package installs in this plan; no Package Legitimacy Audit needed.
</threat_model>

<verification>
- `02-HOTKEY-CONFLICT-TEST.md` exists, three-column matrix filled across Teams/Zoom/VS Code, final chord set signed off.
- Registrar chord constants and HUD cheat-sheet match the final set.
- `npx vitest run src/main/hotkey-registrar.service.test.ts` exits 0 (incl. the CTL-03 failure-surfacing cases); `npm run typecheck` and `npm run lint` exit 0.
- `npm run dev`: the finalized chords fire the overlay actions while a meeting app holds focus, with no observed accelerator collision, and the HUD shows `Hotkeys: OK`.
</verification>

<success_criteria>
- The locked action set's chords are empirically tested against Teams, Zoom, and VS Code on the target Windows 11 machine, including proof the apps' own Ctrl+Alt accelerators still fire (CTL-02 non-consumption proven on the real machine).
- A conflict-free default Ctrl+Alt chord set is finalized, applied to the registrar + HUD cheat-sheet, and recorded with per-app evidence (D-05).
- The CTL-03 failure-surfacing path (`Hotkeys: OK / N failed`) remains intact after the chord swap.
- All Phase 2 requirements (OVL-03, OVL-05, CTL-01, CTL-02, CTL-03) are satisfied by the completed keyboard control loop with a shippable default chord set.
</success_criteria>

<output>
Create `.planning/phases/02-global-hotkeys-window-control/02-03-SUMMARY.md` when done.
</output>
</content>
