---
phase: 01-overlay-shell-existential-behaviors
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - VERIFICATION.md
  - electron-builder.yml
  - package.json
autonomous: false
requirements: ["OVL-06"]
user_setup: []
must_haves:
  truths:
    - "On the target Windows 11 machine, the active meeting app's title bar stays focused while the overlay is shown and moved (focus never stolen)"
    - "In a real screen share / system screenshot, the overlay is fully absent — not a black rectangle — and a proof screenshot is saved"
    - "The exact pinned Electron patch version, machine info, and date are recorded in a committed VERIFICATION.md"
    - "A packaged .exe still renders transparent/frameless (no opaque background / no white flash)"
    - "D-01: focus-never-stolen is proven by a manual visual checklist (watch the meeting app title bar stay active), not programmatic foreground-window logging"
    - "D-02: absence-from-screen-capture is proven by a real self screen-share / system screenshot with a saved proof screenshot (confirm fully absent, not a black rectangle)"
    - "D-03: all evidence lives in a committed VERIFICATION.md (checklist sign-off, exact Electron patch, machine info, date, proof-screenshot path) — the auditable GO/NO-GO record"
    - "D-09: the exact verified Electron patch is recorded in VERIFICATION.md as part of the pin-and-record procedure"
    - "D-10: the packaged smoke test is minimal — build once, launch the .exe, eyeball transparency only; installer/signing and full re-verification deferred to Phase 7"
  artifacts:
    - path: "VERIFICATION.md"
      provides: "Committed GO/NO-GO evidence: checklist sign-off, exact Electron version, machine info, date, proof-screenshot path"
      contains: "35.7.5"
    - path: "electron-builder.yml"
      provides: "Minimal Windows packaging config for the transparency smoke test"
      min_lines: 3
  key_links:
    - from: "VERIFICATION.md"
      to: "proof screenshot file"
      via: "recorded relative path to the saved screen-share/screenshot evidence"
      pattern: "screenshot|\\.png"
---

<objective>
Run the GO/NO-GO gate on the target Windows 11 machine: manually verify focus is never stolen (D-01) and the overlay is absent (not black) in a real screen share (D-02), record the exact pinned Electron patch + machine + date + proof-screenshot path in a committed VERIFICATION.md (D-03), and run one minimal electron-builder packaged smoke test to eyeball that transparency/frameless rendering still holds in a built .exe (D-10). This is the phase acceptance gate — "verified on this machine" is the criterion, not an optional check.

Purpose: OVL-06 — the two existential, version-coupled behaviors must be empirically proven on the real machine against the pinned Electron build before any feature work in Phase 2+. This plan produces the auditable record.
Output: A committed VERIFICATION.md with sign-off, a saved proof screenshot, and a built .exe eyeballed for transparency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-overlay-shell-existential-behaviors/01-CONTEXT.md
@.planning/phases/01-overlay-shell-existential-behaviors/01-RESEARCH.md
@.planning/phases/01-overlay-shell-existential-behaviors/01-VALIDATION.md
@.planning/phases/01-overlay-shell-existential-behaviors/01-02-SUMMARY.md
@.planning/phases/01-overlay-shell-existential-behaviors/01-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimal electron-builder packaged smoke build + VERIFICATION.md skeleton</name>
  <read_first>
    - electron-builder.yml (from 01-01 — read before editing; may be empty/minimal)
    - package.json (read scripts before adding the package script)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-CONTEXT.md (D-03 VERIFICATION.md contents, D-10 minimal smoke test)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-RESEARCH.md (Recommended Project Structure; D-10; Pitfall 6 white-flash; Environment Availability — NSIS vs portable target)
  </read_first>
  <action>
    Configure electron-builder.yml for a MINIMAL Windows target sufficient to produce a launchable artifact (a `portable` or `dir`/`nsis` Win target — portable is simplest and avoids signing; per D-10 installer/signing is deferred to Phase 7). Add a `package` npm script that runs `electron-vite build` then `electron-builder --win` (or equivalent) to produce the .exe under the build output directory. Keep it minimal — no signing, no auto-update.
    Run `npm run build` then `npm run package`; produce the .exe. (Launching + eyeballing transparency is the manual checkpoint in Task 3.)
    Create the VERIFICATION.md skeleton at the repo root with the required sections (D-03): a focus checklist (D-01 items), a content-protection / screen-share section (D-02 with a proof-screenshot path placeholder), a recorded-environment block (exact Electron patch = read from package.json, Windows build via `winver`/`ver`, date), and a packaged-smoke section (D-10). Pre-fill the exact Electron version from package.json (35.7.5 unless bisected) and today's date; leave the manual checkboxes unticked for Task 2/3 to fill.
  </action>
  <acceptance_criteria>
    - electron-builder.yml defines a Windows target (portable/nsis/dir)
    - package.json has a `package` script that builds then runs electron-builder for Windows
    - `npm run build` exits 0 and `npm run package` produces a .exe artifact (record artifact path in SUMMARY)
    - VERIFICATION.md exists at repo root with: focus checklist section, screen-share/content-protection section with a proof-screenshot path field, recorded-environment block (Electron version + Windows build + date), packaged-smoke section
    - VERIFICATION.md contains the string `35.7.5` (or the actually-pinned patch if bisected) in the recorded-environment block
  </acceptance_criteria>
  <verify>
    <automated>test -f electron-builder.yml && test -f VERIFICATION.md && grep -q "35.7.5" VERIFICATION.md && node -e "const p=require('./package.json'); if(!p.scripts||!p.scripts.package){process.exit(1)}; console.log('PASS')"</automated>
  </verify>
  <done>A minimal packaged .exe is built; VERIFICATION.md skeleton exists with the recorded exact Electron patch and all required sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: GO/NO-GO manual gate — focus discipline + content protection on-machine</name>
  <what-built>
    The overlay (01-02) shown via showInactive with content protection re-applied on every show, the HUD displaying Electron version + CP state + position, and the VERIFICATION.md skeleton (Task 1). This checkpoint performs the empirical on-machine proof (D-01/D-02) that cannot be automated.
  </what-built>
  <read_first>
    - VERIFICATION.md (from Task 1 — the checklist you will tick and sign)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-VALIDATION.md (Manual-Only Verifications table — exact test instructions for OVL-01/02/04/06)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Human-driven verification (cannot be automated — D-01/D-02). Run `npm run dev` on the target Windows 11 machine and perform the focus, transparency, content-protection, and recording steps in <how-to-verify>; tick and sign the VERIFICATION.md checklist and save the proof screenshot. If a black box or stolen focus is observed, bisect the Electron version toward 35.4.0 / fix the window code per <how-to-verify> and re-run.
  </action>
  <how-to-verify>
    Run `npm run dev` on the target Windows 11 machine, then:
    1. FOCUS (D-01 / OVL-02): Open a real meeting app (Teams or Zoom) and click into it so its title bar is active/focused. With the overlay shown and while it is on screen, confirm the meeting app's title bar STAYS active (not greyed). The overlay must never become the active window. Tick the focus checklist items in VERIFICATION.md.
    2. TRANSPARENCY/ALWAYS-ON-TOP (D-... / OVL-01): Confirm the HUD floats over a maximized window with NO opaque background and NO frame, and reads the pinned Electron version + CP=ON + a position.
    3. CONTENT PROTECTION (D-02 / OVL-04): Start a REAL screen share (Teams/Zoom "share screen") OR take a system screenshot (Win+Shift+S) while the overlay is visible. Confirm the overlay is FULLY ABSENT from the captured image — NOT a black rectangle. Save the capture as the proof screenshot into the repo (e.g. .planning/phases/01-overlay-shell-existential-behaviors/proof-screenshot.png) and record its relative path in VERIFICATION.md.
    4. RECORD (D-03 / OVL-06): In VERIFICATION.md, record the exact Electron patch (from package.json), the Windows build (`winver`), the machine, and the date; sign off the checklist.
    IF the overlay appears as a BLACK BOX in the share: the Electron patch is wrong or content protection was not re-applied — bisect the Electron version DOWN toward 35.4.0 (still has the core fix #47034), re-pin exactly, commit the lockfile, and re-run this checkpoint. Record the working patch.
    IF focus IS stolen (meeting title bar greys out): a `show()`/`focus()` slipped in or focusable is not false — return to 01-02 to fix, then re-run.
  </how-to-verify>
  <acceptance_criteria>
    - Meeting-app title bar stays focused while the overlay is shown and moved (focus checklist ticked in VERIFICATION.md)
    - Overlay is absent (not a black rectangle) in a real screen share/screenshot; proof screenshot saved and its path recorded in VERIFICATION.md
    - Exact Electron patch + Windows build + machine + date recorded and the checklist signed off in VERIFICATION.md
  </acceptance_criteria>
  <verify>
    <human-check>Meeting-app title bar stays focused while the overlay is shown/moved; overlay is ABSENT (not black) in a real screen share/screenshot with a saved proof screenshot; exact Electron patch + Windows build + machine + date recorded and signed in VERIFICATION.md.</human-check>
  </verify>
  <done>Focus never stolen + overlay absent (not black) in real capture are confirmed on-machine and recorded/signed in VERIFICATION.md with the proof-screenshot path.</done>
  <resume-signal>Type "approved" once the focus checklist passes, the proof screenshot shows NO overlay and NO black box, and VERIFICATION.md is filled and signed — or describe the failure (black box / focus stolen) so the Electron patch or window code can be corrected.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Packaged .exe transparency smoke (D-10) + commit the gate record</name>
  <what-built>
    The minimal packaged .exe from Task 1. This checkpoint eyeballs that transparency/frameless rendering survives packaging (D-10) — a narrow de-risk of PKG-01, which is fully owned by Phase 7.
  </what-built>
  <read_first>
    - VERIFICATION.md (packaged-smoke section to fill)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-CONTEXT.md (D-10 — minimal: transparency eyeball only; installer/signing + full re-verify deferred to Phase 7)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-RESEARCH.md (Pitfall 6 white-flash)
  </read_first>
  <action>
    Human-driven verification (D-10). Launch the packaged .exe from Task 1 and eyeball that transparency/frameless rendering still holds (no opaque bg, no persistent white flash); tick the packaged-smoke section in VERIFICATION.md, then ensure VERIFICATION.md is complete and commit it with the final pinned package.json + lockfile.
  </action>
  <how-to-verify>
    Launch the packaged .exe produced in Task 1. Eyeball ONLY that transparency/frameless rendering still holds: no opaque/solid background, no persistent white flash, the HUD visible over the desktop. (Full focus + content-protection re-verification of the packaged build is intentionally deferred to Phase 7 / PKG-01 per D-10.) Tick the packaged-smoke section in VERIFICATION.md.
    Then ensure VERIFICATION.md is complete and commit it together with the final pinned package.json + lockfile as the auditable GO/NO-GO record.
  </how-to-verify>
  <acceptance_criteria>
    - Packaged .exe launches and renders transparent/frameless (no opaque bg / no persistent white flash) — packaged-smoke section ticked in VERIFICATION.md
    - VERIFICATION.md is complete (focus + content-protection + environment + packaged-smoke all filled and signed) and committed
  </acceptance_criteria>
  <verify>
    <human-check>Packaged .exe launches and renders transparent/frameless (no opaque bg / no persistent white flash); VERIFICATION.md is complete and committed with the final pinned package.json + lockfile.</human-check>
  </verify>
  <done>Packaged .exe transparency eyeballed (D-10) and the complete VERIFICATION.md GO/NO-GO record is committed.</done>
  <resume-signal>Type "approved" once the packaged .exe renders transparent and VERIFICATION.md is complete and committed — or describe the rendering issue (opaque bg / white flash) for a transparency fix (backgroundColor:'#00000000' + ready-to-show).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overlay → screen-capture pipeline | The screen-share path is where content-protection failure leaks the overlay; the manual self-test verifies it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-I | Information Disclosure | overlay visible/black-box in screen share | mitigate | Real self screen-share proof (D-02) confirms absence (not black); wrong patch is bisected toward 35.4.0 and re-pinned. |
| T-01-04-T | Tampering | committed Electron version drift | mitigate | Exact pin + committed lockfile (D-09); VERIFICATION.md records the exact verified patch so the shipped build equals the verified build. |

Note: No new sensitive-data/auth surface in this plan; it inherits the boundary established in 01-01 and the secret round-trip from 01-03. The work here is empirical verification + packaging, not new code paths.
</threat_model>

<verification>
- Automated: VERIFICATION.md exists and records the exact Electron patch; `package` script + electron-builder.yml present; `npm run build` exits 0.
- Manual on-machine (the gate, NOT fabricated): focus never stolen (D-01), overlay absent/not-black in real screen share with saved proof screenshot (D-02), exact patch+machine+date recorded and signed (D-03/OVL-06), packaged .exe transparency eyeballed (D-10). These manual checks ARE the acceptance criteria for OVL-01/02/04/06.
</verification>

<success_criteria>
OVL-06 satisfied: on the target Windows 11 machine, focus is never stolen and the overlay is absent (not black) in a real screen share, all recorded in a committed VERIFICATION.md with the exact pinned Electron patch + machine + date + proof-screenshot path, and a packaged .exe renders transparent (D-10). This is the GO/NO-GO gate for Phase 2.
</success_criteria>

<output>
Create `.planning/phases/01-overlay-shell-existential-behaviors/01-04-SUMMARY.md` when done. Record the verified exact Electron patch, the Windows build, the proof-screenshot path, and the packaged-smoke result. If a version bisect was required, record the working patch and update package.json/lockfile.
</output>
