---
phase: 01-overlay-shell-existential-behaviors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - electron.vite.config.ts
  - electron-builder.yml
  - tsconfig.json
  - tsconfig.node.json
  - .gitignore
  - .prettierrc
  - .oxlintrc.json
  - src/main/index.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/index.html
  - src/renderer/src/main.tsx
  - src/renderer/src/App.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "`npm run dev` (electron-vite dev) launches the Electron app and a window paints without error"
    - "The renderer is contextIsolated and sandboxed — the renderer cannot reach Node/require"
    - "package.json pins electron to an exact version (no ^/~) and the lockfile is committed"
  artifacts:
    - path: "package.json"
      provides: "Exact electron pin, type:module, scripts (dev/build/lint/format/typecheck)"
      contains: "\"electron\": \"35.7.5\""
    - path: "electron.vite.config.ts"
      provides: "main/preload/renderer three-config build"
      min_lines: 10
    - path: "src/preload/index.ts"
      provides: "Typed contextBridge boundary (no secret channels yet)"
      contains: "contextBridge"
    - path: "src/main/index.ts"
      provides: "App lifecycle entry; createWindow after whenReady"
      contains: "whenReady"
  key_links:
    - from: "src/preload/index.ts"
      to: "renderer window.jedi"
      via: "contextBridge.exposeInMainWorld"
      pattern: "exposeInMainWorld"
---

<objective>
Scaffold the Electron + TypeScript + React three-process project (main / preload / renderer) with electron-vite, exact-pinned Electron, ESM main, the IDEXX lint/format toolchain, and the contextIsolation+sandbox+typed-contextBridge security boundary wired from the start. This is the Walking Skeleton foundation: after this plan the app launches in dev and the renderer is provably isolated from Node.

Purpose: Everything in Phase 1 (and all later phases) builds on this scaffold and this security boundary. The exact Electron pin (D-09) and the boundary (D-06) are load-bearing and must exist before any window or secret code.
Output: A launchable electron-vite project with committed lockfile, ESM main, and a minimal isolated preload bridge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-overlay-shell-existential-behaviors/01-CONTEXT.md
@.planning/phases/01-overlay-shell-existential-behaviors/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- The boundary contract this plan establishes. Later plans (01-02, 01-03, Phase 6) extend it. -->
Preload exposes a single typed, read-only, NON-SECRET namespace on window:
  window.jedi: { /* extended in 01-02 with a status channel; empty/minimal here */ }
No secret-bearing channels exist in Phase 1 (D-05). The boundary is structural only (D-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold electron-vite React+TS project and pin Electron exactly</name>
  <read_first>
    - .planning/phases/01-overlay-shell-existential-behaviors/01-RESEARCH.md (Standard Stack table; Installation block; Recommended Project Structure; Pitfall 5 ESM/CJS; A1/A2 assumptions)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-CONTEXT.md (D-09 exact pin + commit lockfile)
    - CLAUDE.md (Recommended Stack / Version Compatibility sections)
  </read_first>
  <action>
    Scaffold into the existing repo using the electron-vite React+TS template (`npm create @quick-start/electron@latest` with the React+TypeScript template; the documented flag is `--template react-ts` — confirm at scaffold time per assumption A2, re-run with the correct template name if it differs). Scaffold into the current directory (the repo already has .git and CLAUDE.md — do NOT create a nested project folder; move generated files to the repo root if the tool insists on a subfolder).
    Then pin Electron EXACTLY with no caret/tilde: `npm install --save-exact --save-dev electron@35.7.5`. Verify package.json shows `"electron": "35.7.5"` (no ^ or ~). This is the latest 35.x patch and contains BOTH the content-protection fix (#47034, first in 35.4.0) and the older-Windows fix (#47886); 35.0.x–35.3.0 are known-broken for content protection — never use them (per RESEARCH Pitfall 1 / D-09).
    Set `"type": "module"` in package.json (electron-store@11 is ESM-only, Node >=20 — Pitfall 5). Install `electron-store@11.0.2` (runtime dep) and `electron-builder@26.15.3` (dev dep) so 01-03 and 01-04 have them available; do not configure builder here beyond the file existing.
    For TypeScript, prefer the latest TS 5.9.x line (lower-risk than TS 6.x against the template per assumption A1); pin it explicitly. Document the chosen TS version in the SUMMARY.
    Ensure the committed lockfile (package-lock.json) pins electron 35.7.5 transitively. Add a `.gitignore` that excludes node_modules, dist/out build output, and any electron-store data file path under userData (the store file lives in userData, not the repo, but guard against any local copy).
  </action>
  <acceptance_criteria>
    - package.json contains `"electron": "35.7.5"` with no `^` or `~` prefix
    - package.json contains `"type": "module"`
    - package.json lists electron-store@11.0.2 and electron-builder@26.15.3
    - package-lock.json exists and is staged for commit; `grep -c '"version": "35.7.5"' package-lock.json` returns >= 1 for the electron entry
    - electron-vite config file electron.vite.config.ts exists with main, preload, and renderer config sections
    - .gitignore excludes node_modules and the build output directory
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); const v=(p.devDependencies||{}).electron; if(v!=='35.7.5'){console.error('FAIL electron='+v);process.exit(1)} if(p.type!=='module'){console.error('FAIL type');process.exit(1)} console.log('PASS')"</automated>
  </verify>
  <done>Project scaffolded; electron pinned to exact 35.7.5; type:module set; lockfile committed; electron-store and electron-builder installed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the contextIsolation + sandbox + typed contextBridge boundary and confirm dev launch</name>
  <read_first>
    - src/main/index.ts (generated by scaffold — read before editing)
    - src/preload/index.ts (generated by scaffold — read before editing)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-RESEARCH.md (Pattern 1 webPreferences block; Architecture Diagram preload section; Security Domain V10 row; D-06)
    - .planning/phases/01-overlay-shell-existential-behaviors/01-CONTEXT.md (D-05 no secret channels, D-06 boundary in scaffold)
    - CLAUDE.md (What NOT to Use — nodeIntegration/contextIsolation anti-pattern)
  </read_first>
  <action>
    Configure the BrowserWindow in src/main/index.ts (the scaffold default window for now — 01-02 replaces it with the overlay manager) with `webPreferences: { contextIsolation: true, sandbox: true, preload: <preload path>, backgroundThrottling: false }` and ensure `nodeIntegration` is NOT enabled (must be false/absent). Create the window inside `app.whenReady().then(...)` per D-06 and Pitfall 4 sequencing.
    In src/preload/index.ts, expose a single typed namespace via `contextBridge.exposeInMainWorld('jedi', { ... })`. In Phase 1 this namespace is structural only — NO secret-bearing channels (D-05). Leave it minimal (an empty object or a single placeholder field); 01-02 adds the read-only status channel. Provide a matching ambient type declaration in src/preload/index.d.ts so the renderer has `window.jedi` typed.
    Add npm scripts: `dev` (electron-vite dev), `build` (electron-vite build), `typecheck`, `lint` (oxlint), `format`/`format:check` (Prettier). Add IDEXX style config: .prettierrc (4-space indent, single quotes, 180 print width, ES5 trailing commas, semicolons) and .oxlintrc.json. Run `npm run format` and `npm run lint` to confirm the scaffold passes; fix style violations.
    Follow IDEXX code standards: explicit return types on exported functions, single quotes, 4-space indent.
  </action>
  <acceptance_criteria>
    - src/main/index.ts BrowserWindow webPreferences contains `contextIsolation: true` and `sandbox: true` and does NOT set `nodeIntegration: true`
    - src/main/index.ts creates the window after `app.whenReady`
    - src/preload/index.ts calls `contextBridge.exposeInMainWorld` and exposes NO secret value (only structural/non-secret fields)
    - `npm run typecheck` exits 0
    - `npm run format:check` and `npm run lint` exit 0
    - `npm run dev` launches the Electron app and a window paints (manual on-machine eyeball — note in SUMMARY whether it launched)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -q "contextIsolation: true" src/main/index.ts && grep -q "sandbox: true" src/main/index.ts && ! grep -q "nodeIntegration: true" src/main/index.ts && grep -q "exposeInMainWorld" src/preload/index.ts && echo PASS</automated>
  </verify>
  <done>Boundary wired (contextIsolation+sandbox+typed preload, no secret channels); typecheck/lint/format pass; `npm run dev` launches a window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (via preload) | The renderer is untrusted web content; the preload contextBridge is the only sanctioned channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-E | Elevation of Privilege | renderer → Node | mitigate | `contextIsolation: true` + `sandbox: true` + no `nodeIntegration` in the scaffold BrowserWindow (D-06). The structural boundary established here is what protects Phase 6 real secrets. |
| T-01-SC | Tampering | npm installs (scaffold deps) | mitigate | All packages are canonical high-trust Electron toolchain (RESEARCH Package Legitimacy Audit — none [ASSUMED]/[SUS]/[SLOP]); no human-verify gate required. Lockfile committed (D-09) pins transitive versions byte-for-byte. |

Note: No new sensitive-data surface in this plan beyond the structural boundary; the secret round-trip is in 01-03.
</threat_model>

<verification>
- `npm run dev` launches the app on the target Windows 11 machine.
- `npm run typecheck`, `npm run lint`, `npm run format:check` all exit 0.
- package.json + lockfile pin electron 35.7.5 exactly.
- Renderer is contextIsolated + sandboxed; preload exposes only the structural `window.jedi` namespace.
</verification>

<success_criteria>
A launchable electron-vite React+TS project exists with: exact-pinned electron@35.7.5 + committed lockfile (D-09), ESM main (type:module), the contextIsolation+sandbox+typed-contextBridge boundary wired with no secret channels (D-06/D-05), and IDEXX lint/format passing. `npm run dev` opens a window.
</success_criteria>

<output>
Create `.planning/phases/01-overlay-shell-existential-behaviors/01-01-SUMMARY.md` when done. Record the exact TypeScript version chosen and whether `npm run dev` launched on-machine.
</output>
