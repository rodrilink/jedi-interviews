---
phase: 09-card-based-q-a-panel-redesign
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/renderer/src/components/utterance-view.utility.ts
  - src/renderer/src/components/utterance-view.utility.test.ts
  - src/renderer/src/components/transcript-panel.tsx
  - src/renderer/src/assets/hud.css
autonomous: false
requirements: [QA-06, QA-05]
must_haves:
  truths:
    - "D-06: A compact people row is pinned at the top of the Q/A panel (below the title, above the scrolling card body), always visible while cards scroll, doubling as the color legend"
    - "D-07: Each identified Person N renders as a colored chip in that speaker's accent color with its utterance count, e.g. 'Person 1 (12)  Person 2 (5)'; the undiarized 'Speaker' bucket is NOT listed (D-05)"
    - "D-08: The people row updates as new Person N appear in the incoming utterances array (derived each render)"
    - "D-09: The people row is hidden/empty until the first Person N appears (D-12), and an in-progress utterance shows as a faint unlabeled ghost card (no label, no Q/S accent) at the bottom of the stack"
    - "D-10: The ghost card is replaced (not accumulated) on each interim update and resolves cleanly into a real card on finalize — no flicker/shrink to empty within a turn (preserves HEAD 87fbd19 behavior)"
    - "D-12: An empty session (start, or after Ctrl+Alt+K) shows a muted centered placeholder in the card body and an empty/hidden people row"
    - "D-13: Ctrl+Alt+K clears both the cards and the people list via the existing empty-push reset; new testable elements use row-/list-/card- data-testid prefixes; no new renderer→main channel is added"
  artifacts:
    - path: "src/renderer/src/components/utterance-view.utility.ts"
      provides: "Pure derivation of the people list (distinct Person N + counts + color, 'Speaker' excluded) from the utterances array"
      exports: ["derivePeople"]
    - path: "src/renderer/src/components/transcript-panel.tsx"
      provides: "People row, interim ghost card, and empty-state placeholder rendered from the pushed utterances + interimText"
      contains: "derivePeople"
    - path: "src/renderer/src/assets/hud.css"
      provides: "People-row/chip, ghost-card, and empty-placeholder CSS classes"
      contains: "transcript-panel__people"
  key_links:
    - from: "src/renderer/src/components/transcript-panel.tsx"
      to: "src/renderer/src/components/utterance-view.utility.ts"
      via: "import { derivePeople }"
      pattern: "derivePeople\\("
    - from: "src/renderer/src/components/transcript-panel.tsx"
      to: "interimText (IOverlayTranscript bridge)"
      via: "trailing ghost card rendered when interimText non-empty"
      pattern: "interimText"
---

<objective>
Complete the Q/A panel redesign: add the compact people row (QA-06), the interim "ghost" card (D-09/D-10), and the empty-state placeholder (D-12) — all derived from the same pushed `utterances` array + `interimText`, over the existing one-way bridge. This finishes the visible feature: identified speakers appear as counted colored chips that double as the color legend, the forming turn reads as a materializing card, and an empty session reassures the user the panel is live.

Purpose: Delivers QA-06 (compact people list) and completes QA-05 (question distinction is reinforced by the shared per-speaker color legend). Builds directly on the card stack from 09-01.

Output: A `derivePeople` pure function (with tests), the people row + ghost card + empty placeholder in the panel, and their CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-card-based-q-a-panel-redesign/09-CONTEXT.md
@.planning/phases/09-card-based-q-a-panel-redesign/09-PATTERNS.md
@.planning/phases/09-card-based-q-a-panel-redesign/09-01-SUMMARY.md

<interfaces>
<!-- This plan reuses the local mirror types + utility exports created in 09-01. -->
From src/renderer/src/components/utterance-view.utility.ts (created in 09-01):
  - deriveCardRows(utterances): ICardRow[]  — card labels, per-type seq, per-speaker color token
  - personAccentColor(speaker): string      — deterministic per-speaker color token ('pN' | 'neutral')

From the panel's local mirror (09-01): IUtteranceEvent { text, speaker, isDiarized, classification }, UtteranceClassification.

Ghost card / interim discipline: main pushes interimText as the live in-progress line (replaced in place, never accumulated — TranscriptBuffer.setInterim). Preserve the HEAD 87fbd19 fix behavior: the ghost must not flicker/shrink to empty between interim updates within one turn.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 failing test + pure people-list derivation</name>
  <files>src/renderer/src/components/utterance-view.utility.ts, src/renderer/src/components/utterance-view.utility.test.ts</files>
  <read_first>
    - src/renderer/src/components/utterance-view.utility.ts (09-01 — deriveCardRows + personAccentColor; add derivePeople alongside them, reuse personAccentColor for chip color)
    - src/renderer/src/components/utterance-view.utility.test.ts (09-01 — extend this file; keep existing card-row tests green)
    - .planning/phases/09-card-based-q-a-panel-redesign/09-CONTEXT.md (D-06 pinned people row, D-07 chip = 'Person N (count)', D-08 updates as new Person N appear, D-05 'Speaker' excluded)
    - vitest.config.ts (node env, 'src/**/*.test.ts' include)
  </read_first>
  <behavior>
    - Test: derivePeople([]) returns [].
    - Test: distinct numbered speakers are listed in first-appearance order, each with its utterance count — input with Person 1 x3, Person 2 x1 yields [{ speaker:'Person 1', count:3, color: personAccentColor('Person 1') }, { speaker:'Person 2', count:1, color: personAccentColor('Person 2') }].
    - Test: the undiarized 'Speaker' bucket (isDiarized:false) is excluded from the result even when present in the input.
    - Test: counts only tally that speaker's own utterances; a mix of question and statement utterances by the same Person still counts as that person's total.
    - Test: each returned person's color equals personAccentColor(speaker) so the row is the color legend for the cards.
  </behavior>
  <action>
    Extend utterance-view.utility.test.ts FIRST (RED) with the behaviors above (AAA comments), then add `derivePeople` to utterance-view.utility.ts (GREEN).

    Export `derivePeople(utterances: IUtteranceEvent[]): IPersonSummary[]` where `IPersonSummary` carries `speaker: string`, `count: number`, `color: string` (the same token from personAccentColor). Iterate the utterances in order; skip any utterance with `isDiarized === false` (the neutral 'Speaker' bucket, D-05); for diarized turns accumulate a per-speaker count keyed by the `speaker` string, preserving first-appearance order (e.g. a Map). Return one summary per distinct Person N with its total count and `personAccentColor(speaker)`.

    Keep the file pure and DOM-free. Do not modify deriveCardRows/personAccentColor behavior beyond what the tests require (existing 09-01 tests must stay green).
  </action>
  <verify>
    <automated>npx vitest run src/renderer/src/components/utterance-view.utility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/renderer/src/components/utterance-view.utility.test.ts` exits 0 with both the 09-01 card-row tests and the new derivePeople tests passing.
    - Source assertion: utterance-view.utility.ts exports `derivePeople`.
    - Behavior assertion: derivePeople excludes any `isDiarized:false` utterance from the result.
    - Behavior assertion: each person's `color` === `personAccentColor(person.speaker)`.
    - `npm run typecheck:web` passes.
  </acceptance_criteria>
  <done>The people-list derivation is a proven pure function: distinct Person N in appearance order with counts, 'Speaker' excluded, chip color matching the card color legend.</done>
</task>

<task type="auto">
  <name>Task 2: People row, interim ghost card, and empty-state placeholder</name>
  <files>src/renderer/src/components/transcript-panel.tsx, src/renderer/src/assets/hud.css</files>
  <read_first>
    - src/renderer/src/components/transcript-panel.tsx (the 09-01 rebuilt panel — the card `.map`, the interim span, the root section seams, the scroll-target body)
    - src/renderer/src/components/utterance-view.utility.ts (09-01 + Task 1 — import derivePeople + deriveCardRows)
    - src/renderer/src/components/ai-panel.tsx (the `list-`/`row-` testid prefix convention: list-ai-entries, row-ai-entry-${id})
    - src/renderer/src/assets/hud.css (09-01 card rules; the `.vision-panel__placeholder` muted-italic empty-line precedent lines 411-416; the `[data-speaker-color=]` chip color rules to reuse for people chips)
    - .planning/phases/09-card-based-q-a-panel-redesign/09-CONTEXT.md (D-06/07/08 people row, D-09/D-10 ghost card, D-11 no timestamps, D-12 empty state, D-13 preserved seams)
  </read_first>
  <action>
    Add three render elements to the panel, all derived from the already-subscribed `utterances` + `interimText` state (no new subscription, no new channel).

    People row (QA-06): between the `<h2>` title and the `transcript-panel__body` scroll container, render a pinned row `<div className="transcript-panel__people" data-testid="list-people">`. Call `derivePeople(utterances)`; `.map` each person to a chip `<span className="transcript-panel__person-chip" data-testid={\`row-person-${person.speaker.replace(/\D/g, '')}\`} data-speaker-color={person.color}>{`${person.speaker} (${person.count})`}</span>` — chip text is exactly `Person 1 (12)` and its color reuses the `[data-speaker-color='pN']` CSS from 09-01 so the row IS the color legend (D-06). Hide the row when `derivePeople` returns empty (D-12): render the row only when there is at least one person (conditionally render, or add a `data-empty` attribute + CSS `display:none` — pick one, no inline style). The 'Speaker' bucket is already excluded by derivePeople (D-05/D-07).

    Interim ghost card (D-09/D-10): after the mapped committed cards inside `transcript-panel__body`, when `interimText` is non-empty render ONE trailing `<article className="transcript-panel__card transcript-panel__card--ghost" data-testid="row-utterance-interim">` with NO header/label and NO Q/S accent — a body `<p className="transcript-panel__card-body">{interimText}</p>` in a dashed/low-opacity frame reading as "forming." It is replaced in place on each interim update (it is driven by the single `interimText` state, so it never accumulates); do NOT introduce a second interim state or key it in a way that remounts/flickers within a turn (preserve the HEAD 87fbd19 behavior — one continuous ghost per turn). Remove the old plain `transcript-panel__interim` span from 09-01 in favor of this ghost card.

    Empty-state placeholder (D-12): when `deriveCardRows(utterances)` is empty AND `interimText` is empty, render a muted centered placeholder inside the body `<p className="transcript-panel__placeholder" data-testid="cell-transcript-placeholder">Listening…</p>` (copy the `.vision-panel__placeholder` muted-italic treatment). It disappears as soon as the first card or interim ghost appears.

    Keep the empty-push reset branch working: on an all-empty push, `utterances` becomes `[]` and `interimText` becomes `''`, so the card list empties, the people row hides, and the placeholder returns automatically — no extra reset code needed beyond the 09-01 branch. Keep the stick-to-bottom follow effect dep array as `[utterances, interimText]` so the ghost card and new cards both auto-follow.

    Preserve ALL 09-01 seams: `card-transcript-panel`, `icon-active-panel-transcript`, `card-transcript` body ref target, the scroll gate (`activePanelRef.current !== 'transcript'`), and the title. Add NO renderer→main channel (IN-01).

    hud.css: add `.transcript-panel__people` (compact flex row, wrap, small gap, pinned above the scrolling body — `flex: 0 0 auto`), `.transcript-panel__person-chip` (small rounded pill, count in the speaker's accent color — reuse the `[data-speaker-color='pN']` color rules, or add chip-scoped variants), `.transcript-panel__card--ghost` (dashed border and/or low opacity, no accent stripe, no tint — visually distinct as "forming" per D-09), and `.transcript-panel__placeholder` (cloned from `.vision-panel__placeholder`: margin 0, muted grey rgb(148 163 184 / 85%), italic; centered in the body). Ensure the ghost frame is distinguishable from both question and statement cards.
  </action>
  <verify>
    <automated>npm run typecheck:web && npx vitest run src/renderer/src/components/utterance-view.utility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: transcript-panel.tsx contains `derivePeople(`, a `data-testid="list-people"` element, `row-person-` chip testids, a `data-testid="row-utterance-interim"` ghost card, and a `data-testid="cell-transcript-placeholder"` placeholder.
    - Source assertion: chip text renders the exact `${speaker} (${count})` shape (contains the literal ` (` + `)` count wrapper).
    - Source assertion: the ghost card uses `transcript-panel__card--ghost` and has NO speaker label element.
    - Source assertion: transcript-panel.tsx contains NO `style={{`, NO `dangerouslySetInnerHTML`, NO new `window.jedi` control call beyond onTranscript/onStatus/onScrollTranscript, and NO import from `src/main`.
    - Source assertion: hud.css contains `.transcript-panel__people`, `.transcript-panel__person-chip`, `.transcript-panel__card--ghost`, and `.transcript-panel__placeholder`.
    - `npm run typecheck:web` passes; the utility vitest exits 0.
  </acceptance_criteria>
  <done>The people row (counted colored chips, 'Speaker' excluded, hidden when empty), the interim ghost card (replaced-not-accumulated, no label/accent), and the empty-state placeholder all render from the existing one-way stream; every preserved seam and the scroll gate remain intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Q/A panel now shows a compact people row of counted colored chips (`Person 1 (12)  Person 2 (5)`) pinned at the top as the color legend, a faint "ghost" card at the bottom for the in-progress turn that solidifies into a real card on finalize, and a muted "Listening…" placeholder when empty.</what-built>
  <how-to-verify>
    1. Run the app in dev and start a session with 2+ speakers.
    2. Confirm the people row appears only after the first Person N, shows `Person N (count)` chips in each speaker's card color (same hue as their card labels), and the counts increase as they speak. The undiarized 'Speaker' turns do NOT add a chip.
    3. Confirm the currently-forming turn shows as a faint/dashed unlabeled ghost card at the bottom; it does NOT flicker or shrink to empty mid-turn, and it is replaced by a proper labeled+classified card when the turn finalizes.
    4. Confirm at session start (and after Ctrl+Alt+K) the body shows a muted centered "Listening…" placeholder and the people row is hidden/empty; both clear as soon as speech arrives.
    5. Re-confirm the 09-01 affordances still hold: Ctrl+Alt+F focus to Q/A, Ctrl+Alt+PgUp/PgDn scroll, auto-follow, Ctrl+Alt+K clear, click-through, no focus steal. Confirm the transcript scrollbar remains reachable through the card body (transcript-panel side of todo 260618-scrollbar).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. chip color mismatch, 'Speaker' listed, ghost flicker, placeholder stuck, scroll unreachable).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (`onTranscript` push) | Utterance text, speaker labels, and interim text (from captured audio via Deepgram) render into the people chips, cards, and ghost card. Untrusted content. |

No new trust boundary: this plan renders additional views of the SAME pushed payload over the SAME read-only channels (IN-01, roadmap SC 5). No renderer→main control surface is added.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering / XSS | people chip text + ghost card body rendering untrusted speaker/interim text | mitigate | Render chip label and interim text as React children ({`${speaker} (${count})`}, {interimText}) so React auto-escapes. No dangerouslySetInnerHTML in the panel. |
| T-09-05 | Elevation of Privilege | renderer→main surface | mitigate | Add no new channel; people row / ghost / placeholder derive purely from already-subscribed state. Verified by source assertion (no new window.jedi.* control call, no src/main import). |
| T-09-06 | Denial of Service | unbounded people row / card growth | accept | The people row is bounded by distinct Person N (no cap by design, D-03 accepts minor over-split risk); the utterances array is main-bounded upstream. Rendering the pushed list is O(n) per push; acceptable for a single-user glanceable overlay. |
</threat_model>

<verification>
- `npx vitest run src/renderer/src/components/utterance-view.utility.test.ts` exits 0 (card-row + people derivation proven).
- `npm run typecheck:web` passes.
- Source assertions: people row / ghost card / placeholder testids present; no inline style, no dangerouslySetInnerHTML, no new control channel, no src/main import.
- Human-verify checkpoint: people chips (color + count, 'Speaker' excluded, hidden-when-empty), ghost card (no flicker, resolves cleanly), and empty placeholder confirmed on the live overlay, with 09-01 affordances intact.
</verification>

<success_criteria>
QA-06 met: a compact people list of identified Person N (colored counted chips, 'Speaker' excluded) is shown and updates as new speakers appear.
QA-05 fully met: question distinction is reinforced by the shared per-speaker color legend in the people row.
D-09/D-10 (interim ghost, replaced-not-accumulated, no flicker) and D-12 (empty-state placeholder + hidden people row) delivered; all D-13 seams preserved.
</success_criteria>

<output>
Create `.planning/phases/09-card-based-q-a-panel-redesign/09-02-SUMMARY.md` when done.
</output>
