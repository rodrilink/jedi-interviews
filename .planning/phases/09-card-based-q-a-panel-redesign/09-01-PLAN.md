---
phase: 09-card-based-q-a-panel-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/utterance-view.utility.ts
  - src/renderer/src/components/utterance-view.utility.test.ts
  - src/renderer/src/components/transcript-panel.tsx
  - src/renderer/src/assets/hud.css
autonomous: false
requirements: [QA-04, QA-05]
must_haves:
  truths:
    - "D-02: Each committed utterance renders as its own card (header row + body, reusing the ai-panel__entry anatomy) in the Q/A panel body, replacing the flat finalLog/interim text blob"
    - "D-03: Every card header shows the exact label {seq} - {speaker}: a question by Person 1 reads 'Q1 - Person 1', the third statement by Person 2 reads 'S3 - Person 2', an undiarized question reads 'Q1 - Speaker'"
    - "D-03: The Q/N and S/N sequence counters are per-type and session-scoped: Q1,Q2,Q3 count independently of S1,S2,S3, derived in the panel from utterance list order"
    - "D-01: Question cards are visually distinct from statement cards at a glance (accent stripe + tinted background on questions, neutral/flat statements) — the primary QA-05 mechanism"
    - "D-04: Each numbered Person N speaker name renders in a stable deterministic accent color derived from the Person number; D-05: the undiarized 'Speaker' bucket renders in neutral grey (no per-speaker color)"
    - "D-11: Card density is roomy (generous padding/spacing, no per-card timestamps)"
    - "D-13: The panel's active-panel scroll gate, stick-to-bottom follow, onStatus wiring, and card-transcript-panel / icon-active-panel-transcript seams still work after the rebuild; no new renderer→main channel is added"
  artifacts:
    - path: "src/renderer/src/components/utterance-view.utility.ts"
      provides: "Pure derivation of per-type sequence label, Q/S prefix, and deterministic per-speaker accent color from the utterances array"
      exports: ["deriveCardRows", "personAccentColor"]
    - path: "src/renderer/src/components/utterance-view.utility.test.ts"
      provides: "Unit tests for the pure derivation logic (label format, per-type sequencing, color stability)"
    - path: "src/renderer/src/components/transcript-panel.tsx"
      provides: "Card-stack render body driven by the pushed utterances array; reconcileFinalLog removed"
      contains: "deriveCardRows"
    - path: "src/renderer/src/assets/hud.css"
      provides: "Card / question-variant / statement-variant / speaker-color CSS classes"
      contains: "transcript-panel__card"
  key_links:
    - from: "src/renderer/src/components/transcript-panel.tsx"
      to: "src/renderer/src/components/utterance-view.utility.ts"
      via: "import { deriveCardRows, personAccentColor }"
      pattern: "deriveCardRows\\("
    - from: "src/renderer/src/components/transcript-panel.tsx"
      to: "next.utterances (IOverlayTranscript bridge)"
      via: "onTranscript push renders utterances directly"
      pattern: "next\\.utterances"
---

<objective>
Rebuild the Q/A panel body (`transcript-panel.tsx`) IN PLACE so it renders each committed utterance as its own card labeled `{seq} - {speaker}` (`Q1 - Person 1`, `S3 - Person 2`), with question cards visually distinct from statement cards, driven directly by the Phase 8 `utterances: IUtteranceEvent[]` array pushed over the existing read-only `onTranscript` bridge. The flat-text `reconcileFinalLog`/`finalLog` path is removed.

Purpose: This is the first working vertical slice of the Phase 9 redesign — real speaker-attributed, classified utterances become labeled, question-distinct cards on the live overlay. Covers QA-04 (per-utterance cards labeled sequence+speaker) and QA-05 (questions visually distinct).

Output: A `utterance-view.utility.ts` pure-derivation module (with a passing `.test.ts`), the rebuilt panel render body reading `utterances` directly, and the card / question-variant / speaker-color CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-card-based-q-a-panel-redesign/09-CONTEXT.md
@.planning/phases/09-card-based-q-a-panel-redesign/09-PATTERNS.md

<interfaces>
<!-- Local mirror types the panel must declare (renderer is bundled separately from main; do NOT import from src/main). -->
<!-- Authoritative source: src/main/stt/stt-provider.interface.ts lines 44, 53-67 (READ-ONLY this phase). -->

Mirror locally in transcript-panel.tsx (follow the ai-panel.tsx lines 8-23 local-mirror convention):

  type UtteranceClassification = 'question' | 'statement';

  interface IUtteranceEvent {
      text: string;                            // finalized utterance text
      speaker: string;                         // 'Person 1' | 'Person 2' | … | 'Speaker' (undiarized bucket)
      isDiarized: boolean;                     // false => neutral 'Speaker' bucket (D-05); keep OUT of people list
      classification: UtteranceClassification; // 'question' => Q prefix + accent/tint; else 'statement'
  }

Extend the panel's LOCAL IOverlayTranscript (current lines 9-18) with:

      utterances: IUtteranceEvent[];

Authoritative bridge shape (src/main/overlay-window.manager.ts lines 96-111, READ-ONLY): IOverlayTranscript already carries finalText, interimText, connectionState, audioLevel, utterances. Main pushes the FULL session-scoped utterances array every push and empties it in place on Ctrl+Alt+K (src/main/index.ts line 129/133) — so an empty push carries utterances: [].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 failing test + pure card-row derivation utility</name>
  <files>src/renderer/src/components/utterance-view.utility.ts, src/renderer/src/components/utterance-view.utility.test.ts</files>
  <read_first>
    - src/renderer/src/components/ai-panel.tsx (module-level pure-helper + PascalCase-keyed Record label-map convention: MODE_LABEL lines 38-42, formatRelativeTime lines 52-62)
    - src/renderer/src/components/format-uptime.utility.test.ts (the existing renderer .test.ts AAA + describe-hierarchy analog that runs under the node vitest env)
    - src/main/stt/stt-provider.interface.ts (lines 44, 53-67 — authoritative IUtteranceEvent / UtteranceClassification shape to mirror)
    - .planning/phases/09-card-based-q-a-panel-redesign/09-CONTEXT.md (D-03 label format, D-04 per-speaker color, D-05 undiarized 'Speaker' handling)
    - vitest.config.ts (environment: 'node', include 'src/**/*.test.ts' — this utility is a .ts file so it is picked up; keep it DOM-free)
  </read_first>
  <behavior>
    - Test: deriveCardRows([]) returns [].
    - Test: a single question by 'Person 1' derives label 'Q1 - Person 1' with prefix 'Q', seq 1, classification 'question'.
    - Test: per-type independent sequencing — input [statement P1, question P1, statement P2, question P1] derives labels 'S1 - Person 1', 'Q1 - Person 1', 'S2 - Person 2', 'Q2 - Person 1' (Q and S counters advance independently, in list order).
    - Test: undiarized turn (isDiarized:false, speaker 'Speaker') derives label 'Q1 - Speaker' and reports isDiarized:false so the panel renders it neutral grey.
    - Test: personAccentColor('Person 1') returns a stable enumerable palette-slot token (e.g. 'p0') deterministic across calls; personAccentColor('Person 2') returns a different slot token from Person 1; personAccentColor for the same Person number is identical on repeat calls.
    - Test: personAccentColor for the neutral 'Speaker' bucket returns the designated 'neutral' token (not a per-person slot token).
  </behavior>
  <action>
    Create the .test.ts FIRST (RED) with the behaviors above using the AAA-comment style from format-uptime.utility.test.ts, then implement utterance-view.utility.ts to make it pass (GREEN).

    Export a pure function `deriveCardRows(utterances: IUtteranceEvent[]): ICardRow[]` where `ICardRow` carries: `label: string` (exact format `{seq} - {speaker}` e.g. 'Q1 - Person 1'), `prefix: 'Q' | 'S'` (from classification: 'question'->'Q', 'statement'->'S'), `seq: number`, `speaker: string`, `isDiarized: boolean`, `classification: UtteranceClassification`, `text: string`, and `speakerColor: string` (the accent color token from personAccentColor). Sequence is a per-type running counter over the array in order: maintain independent question and statement counters, increment the matching counter for each utterance. Label string is built as `${prefix}${seq} - ${speaker}` (space-hyphen-space, exact).

    Export `personAccentColor(speaker: string): string` returning a deterministic short enumerable token (NOT a raw CSS color) — a palette-slot name like `p0`..`p7` for numbered speakers and `neutral` for the undiarized bucket. This keeps the function DOM/CSS-free and lets the panel (Task 2) express the actual hue via a `data-speaker-color` attribute + matching hud.css rules (inline `style` props are FORBIDDEN per the IDEXX rule). For a numbered `Person N` speaker, parse the trailing integer from the 'Person N' string and index into a fixed ordered set of slot tokens `p0`..`pK` (wrap with modulo so numbers beyond the set still map deterministically). Choose 6-8 slots; the concrete hues each slot maps to are defined in hud.css in Task 2 (pick values that read on a dark translucent overlay — Claude's discretion). For the neutral 'Speaker' bucket (or any non-'Person N' input), return the single `neutral` token.

    Mirror `IUtteranceEvent` and `UtteranceClassification` locally in this utility file (do NOT import from src/main — renderer bundle boundary). Keep the file pure and DOM-free (no React import, no window access) so it runs under the node vitest environment.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/src/components/utterance-view.utility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/renderer/src/components/utterance-view.utility.test.ts` exits 0 with all listed behaviors covered.
    - Source assertion: utterance-view.utility.ts exports `deriveCardRows` and `personAccentColor`; contains no `import` from a path starting `../../../main` or `src/main`.
    - Source assertion: the label is built with the exact template producing `Q1 - Person 1` (contains the literal ` - ` separator and a `${prefix}${seq}` head).
    - Behavior assertion: for input order [statement, question, statement, question] all by tracked speakers, the derived seq values are S1, Q1, S2, Q2 (per-type counters independent).
    - `npm run typecheck:web` passes.
  </acceptance_criteria>
  <done>Pure derivation utility exists with a passing unit test; label format, per-type sequencing, and deterministic per-speaker color are proven without any DOM or main-process import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild the panel body to render committed cards; remove reconcileFinalLog</name>
  <files>src/renderer/src/components/transcript-panel.tsx, src/renderer/src/assets/hud.css</files>
  <read_first>
    - src/renderer/src/components/transcript-panel.tsx (the file being rebuilt — current subscription/scroll/reset scaffolding to PRESERVE lines 92-160; reconcileFinalLog lines 46-72 and finalLog state lines 95-96 to REMOVE; root section seams lines 172-179 to KEEP)
    - src/renderer/src/components/ai-panel.tsx (card-stack JSX + list ref + .map anatomy lines 209-230; the `ai-panel__entry--${state}` variant-class pattern to mirror as question/statement)
    - src/renderer/src/components/utterance-view.utility.ts (Task 1 — import deriveCardRows + personAccentColor)
    - src/renderer/src/assets/hud.css (lines 309-340 transcript-panel__* body block being reworked; lines 343-409 .ai-panel__entry* block to clone as the card template; transcript title accent rgb(125 211 252) line 282, base text rgb(226 232 240), muted rgb(148 163 184))
    - src/main/overlay-window.manager.ts (lines 96-111 — authoritative IOverlayTranscript shape being mirrored; utterances field)
    - .planning/phases/09-card-based-q-a-panel-redesign/09-CONTEXT.md (D-01 accent+tint question distinction, D-02 card anatomy reuse, D-11 roomy density no timestamps, D-13 preserved seams)
  </read_first>
  <action>
    Rewrite the panel render body IN PLACE while preserving all subscription/scroll/reset scaffolding.

    Local interface changes: extend the local `IOverlayTranscript` with `utterances: IUtteranceEvent[]`, and add the local `IUtteranceEvent` + `UtteranceClassification` mirror types (per the <interfaces> block; follow the ai-panel.tsx local-mirror convention). REMOVE `reconcileFinalLog` (lines 46-72), the `finalLogRef` ref, and the `finalLog` state. Add a `const [utterances, setUtterances] = useState<IUtteranceEvent[]>([])`.

    In the `onTranscript` handler: keep `setInterimText(next.interimText)` and `setConnectionState(next.connectionState)`. Replace the reconcile block with `setUtterances(next.utterances)` — main pushes the full session-scoped list each push, so render directly from it (do NOT keep a panel-side accumulator). Keep the empty-push reset branch (`next.finalText.length === 0 && next.interimText.length === 0`): since main also empties `utterances` to `[]` on clear, the direct `setUtterances(next.utterances)` already resets cards; keep the branch to also clear interim/derived state and early-return.

    Keep the onStatus wiring, the `activePanelRef` scroll gate (`activePanelRef.current !== 'transcript'`), and the stick-to-bottom follow effect — but change its dependency array from `[finalLog, interimText]` to `[utterances, interimText]`. Keep the root `<section>` with `data-testid="card-transcript-panel"`, `data-active`, `data-connection-state`, the `icon-active-panel-transcript` active indicator, and the `<h2>` title exactly (D-13).

    Render body: keep the scroll-target `<div className="transcript-panel__body" data-testid="card-transcript" ref={transcriptRef}>` (the scroll gate + stick-to-bottom depend on this being the ref target). Inside it, call `deriveCardRows(utterances)` and `.map` each row to an `<article>` with class `transcript-panel__card transcript-panel__card--${row.classification === 'question' ? 'question' : 'statement'}`, `data-testid={\`row-utterance-${index}\`}`, and `key={index}`. Card header: `<header className="transcript-panel__card-header">` containing a speaker chip `<span className="transcript-panel__card-speaker" data-testid={\`cell-utterance-speaker-${index}\`} data-diarized={row.isDiarized} data-speaker-color={row.speakerColor}>{row.label}</span>` — the label is the exact `deriveCardRows` output (`Q1 - Person 1`). Card body: `<p className="transcript-panel__card-body">{row.text}</p>`.

    Speaker color: because inline `style` props are FORBIDDEN (IDEXX rule), express the per-speaker color via CSS. `personAccentColor` (Task 1) already returns the enumerable slot token (`p0`..`p7`, `neutral`); set `data-speaker-color` on the speaker name element to that token (available as `speakerColor` on each `ICardRow`), and add matching `.transcript-panel__card-speaker[data-speaker-color='p0'] { color: <hue> }` rules in hud.css — one per slot plus `neutral` mapped to `rgb(148 163 184)`. Pick 6-8 distinct hues that read on the dark translucent overlay (Claude's discretion), each distinguishable from the question tint/accent (D-01).

    Leave the interim/ghost card, people row, and empty-state placeholder to Plan 09-02 — this plan renders committed cards + keeps the existing plain interim span rendered after the cards for continuity (do NOT regress the live interim line).

    hud.css: rework the `.transcript-panel__*` body block (lines 309-340) — remove the flat `__final`/`__interim` inline-text treatment's role as the primary body (keep an interim span style for now) and add roomy padded card rules cloned from `.ai-panel__entry*`: `.transcript-panel__card` (rounded, spaced padded box, generous margin between cards per D-11 — NOT hairline rows), `.transcript-panel__card-header` (flex space-between, small font), `.transcript-panel__card-speaker` (font-weight 600), `.transcript-panel__card-body` (`white-space: pre-wrap; word-break: break-word;` base text color). Question distinction (D-01): `.transcript-panel__card--question` gets a colored left accent stripe (`border-left`) AND a subtly brighter/tinted background; `.transcript-panel__card--statement` stays neutral/flat. Add the per-speaker `[data-speaker-color='pN']` color rules and the `[data-speaker-color='neutral']` grey rule. Keep the custom-scrollbar treatment on `.transcript-panel__body`. Ensure the question tint is distinguishable from any speaker chip hue (Claude's discretion for exact values).
  </action>
  <verify>
    <automated>npm run typecheck:web && npx vitest run src/renderer/src/components/utterance-view.utility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: transcript-panel.tsx contains `next.utterances` and `deriveCardRows(` and does NOT contain `reconcileFinalLog` or `finalLogRef`.
    - Source assertion: the root `<section>` still has `data-testid="card-transcript-panel"` and the `icon-active-panel-transcript` indicator; the body still has `data-testid="card-transcript"` and is the `ref={transcriptRef}` target.
    - Source assertion: each card article has `data-testid={\`row-utterance-${index}\`}` and a class containing `transcript-panel__card--question` / `--statement` driven by classification.
    - Source assertion: transcript-panel.tsx contains NO `style={{` (no inline style props) and NO import from `src/main`/`../../../main`.
    - Source assertion: hud.css contains `.transcript-panel__card`, `.transcript-panel__card--question`, and `[data-speaker-color=` rules.
    - `npm run typecheck:web` passes; the Task 1 vitest still exits 0.
  </acceptance_criteria>
  <done>The panel renders committed utterances as labeled cards driven by the pushed array; questions are styled distinctly; per-speaker color is applied via CSS classes; reconcileFinalLog is gone; all preserved seams and the scroll gate remain intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Q/A panel now renders each committed utterance as a card labeled `{seq} - {speaker}` (`Q1 - Person 1`, `S3 - Person 2`), with question cards visually distinct (accent stripe + tint) from statement cards, and each Person N name in a stable per-speaker color. Rendered from the live Phase 8 utterance stream over the existing read-only bridge.</what-built>
  <how-to-verify>
    1. Run the app in dev (`npm run dev`) and start an audio session so utterances flow (or replay a meeting with 2+ speakers).
    2. Confirm each finalized turn appears as its own padded card, NOT one flat paragraph.
    3. Confirm the card header label reads exactly `Q1 - Person 1` / `S3 - Person 2` format (space-hyphen-space), Q counts independently of S.
    4. Confirm question cards clearly stand out at a glance (accent stripe + tinted background) vs neutral statement cards.
    5. Confirm the same Person N keeps the same accent color across their cards; an undiarized turn reads `… - Speaker` in neutral grey.
    6. Confirm Ctrl+Alt+F cycles focus to Q/A and Ctrl+Alt+PgUp/PgDn scrolls the card stack; new cards auto-follow to the bottom unless scrolled up. Ctrl+Alt+K still clears the cards. Overlay stays click-through and never takes focus.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. label format wrong, questions not distinct, color unstable, scroll/clear regressed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (`onTranscript` push) | Transcript/utterance text (originating from captured audio via Deepgram) crosses into the renderer DOM. Untrusted content. |

This phase adds NO new trust boundary: the panel is a pure one-way read-only view over the existing `onTranscript`/`onStatus`/`onScrollTranscript` channels (IN-01, roadmap SC 5). No renderer→main control surface is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering / XSS | transcript-panel.tsx card body + header rendering utterance/speaker text | mitigate | Render all utterance text and labels as React children ({row.text}, {row.label}) so React auto-escapes. Do NOT use dangerouslySetInnerHTML anywhere in the panel. |
| T-09-02 | Elevation of Privilege | renderer→main surface | mitigate | Add no new IPC/control channel. Panel only subscribes to existing read-only channels; verified by source assertion (no new window.jedi.* control call, no import from src/main). |
| T-09-03 | Information Disclosure | pushed payload | accept | IOverlayTranscript carries text/state only (no secret/key, per overlay-window.manager D-08 contract); this plan does not change what is pushed. Low risk. |
</threat_model>

<verification>
- `npx vitest run src/renderer/src/components/utterance-view.utility.test.ts` exits 0 (pure derivation proven).
- `npm run typecheck:web` passes (panel + utility typecheck).
- Source assertions: no `dangerouslySetInnerHTML`, no inline `style={{`, no `src/main` import, no `reconcileFinalLog` in transcript-panel.tsx.
- Human-verify checkpoint: card labels, question distinction, per-speaker color, and preserved scroll/clear affordances confirmed on the live overlay.
</verification>

<success_criteria>
QA-04 met: each utterance renders as its own card labeled `{seq} - {speaker}` with per-type session-scoped sequencing, replacing the flat finalLog view.
QA-05 (partial, completed alongside 09-02): question cards are visually distinct from statement cards at a glance.
All D-13 seams and the active-panel scroll gate / stick-to-bottom follow preserved.
</success_criteria>

<output>
Create `.planning/phases/09-card-based-q-a-panel-redesign/09-01-SUMMARY.md` when done.
</output>
