---
phase: 08-diarized-utterance-pipeline
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - src/main/overlay-window.manager.ts
  - src/main/index.ts
autonomous: true
requirements: [QA-01, QA-02, QA-07]
must_haves:
  truths:
    - "Committed utterances reach the overlay over the EXISTING read-only jedi:transcript push channel (no new renderer→main control channel)"
    - "The gateway 'utterance' binding is attached in the shared attach helper so a Deepgram re-key re-attaches it (transcript does not freeze)"
    - "Pressing Ctrl+Alt+K clears the transcript buffer AND resets the speaker map / accumulator so Person N restarts at Person 1"
    - "The overlay transcript payload additively carries the committed utterance stream; the existing finalText/interimText fields are unchanged so the current renderer keeps working"
  artifacts:
    - path: "src/main/overlay-window.manager.ts"
      provides: "IOverlayTranscript extended with the committed-utterance list (additive)"
      contains: "IUtteranceEvent"
    - path: "src/main/index.ts"
      provides: "gateway.on('utterance') binding + speaker reset wired into clear-transcript"
      contains: "on('utterance'"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/stt/deepgram-stt.gateway.ts"
      via: "gateway.on('utterance', …) in attachSttGatewayHandlers + gateway.clearSpeakers() in clear-transcript"
      pattern: "on\\('utterance'|clearSpeakers"
    - from: "src/main/index.ts"
      to: "src/main/overlay-window.manager.ts"
      via: "pushTranscript carries the utterance list"
      pattern: "pushTranscript"
---

<objective>
Complete the vertical slice to the overlay: wire the gateway's `utterance` event through the main process onto the EXISTING read-only `jedi:transcript` push channel, and make Ctrl+Alt+K reset the speaker map alongside the transcript buffer so `Person N` numbering restarts cleanly.

This is the last data-layer hop — structured utterance events now reach the overlay (the only UI-touching behavior in this phase; Phase 9 renders the cards from this payload). No new renderer→main control channel is added: the utterance stream is an additive field on the existing one-way transcript payload.

Purpose: Delivers the observable half of QA-01 (discrete utterance events reaching the overlay), the session-reset half of QA-02 (D-05 clear-together), and keeps QA-07 intact (main consumes only the seam; no `@deepgram/sdk` import added to `index.ts`).
Output: An extended `IOverlayTranscript`, an `on('utterance')` binding in the shared attach helper (re-key safe), and a `clear-transcript` handler that also resets speakers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-diarized-utterance-pipeline/08-CONTEXT.md
@.planning/phases/08-diarized-utterance-pipeline/08-RESEARCH.md
@.planning/phases/08-diarized-utterance-pipeline/08-PATTERNS.md

<interfaces>
<!-- Exact existing wiring the executor extends. Plan 02 must be complete. No codebase exploration needed. -->

From `src/main/stt/stt-provider.interface.ts` (Plan 01) + gateway (Plan 02):
```
export interface IUtteranceEvent { text: string; speaker: string; isDiarized: boolean; classification: 'question' | 'statement'; }
gateway.on('utterance', (u: IUtteranceEvent) => void)   // emitted once per committed turn
gateway.clearSpeakers(): void                           // resets SpeakerMap + accumulator (D-05)
```

Existing overlay payload — `src/main/overlay-window.manager.ts` ~:90-99:
```
export interface IOverlayTranscript { finalText: string; interimText: string; connectionState: SttConnectionState; audioLevel: number; }
export function pushTranscript(window: BrowserWindow, payload: IOverlayTranscript): void  // sends over TRANSCRIPT_CHANNEL (jedi:transcript)
```

Existing main wiring — `src/main/index.ts`:
```
// buildHandlers(actions, window, buffer, getConnectionState, aiOrchestrator, aiHistory) — ~:98-105
'clear-transcript': () => { buffer.clear(); pushTranscript(window, { ...buffer.renderable(), connectionState: getConnectionState(), audioLevel: 0 }); }   // ~:121-126
// attachSttGatewayHandlers(gateway, window, buffer, getConnectionState, setConnectionState, getAudioLevel) — ~:316-323
gateway.on('transcript', (event: ISttTranscriptEvent) => { if (event.isFinal) buffer.appendFinal(event.text); else buffer.setInterim(event.text); pushTranscript(window, { ...buffer.renderable(), connectionState: getConnectionState(), audioLevel: getAudioLevel() }); })   // ~:324-332
const buffer = new TranscriptBuffer();   // ~:366, inside app.whenReady().then
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additively extend the overlay transcript payload to carry the committed utterance stream</name>
  <files>src/main/overlay-window.manager.ts</files>
  <read_first>
    - src/main/overlay-window.manager.ts (IOverlayTranscript ~:90-99, pushTranscript ~:411-426, TRANSCRIPT_CHANNEL definition, the file header on the read-only D-04 channel)
    - src/main/stt/stt-provider.interface.ts (IUtteranceEvent — Plan 01)
    - .planning/phases/08-diarized-utterance-pipeline/08-CONTEXT.md (Integration Points — "existing read-only bridge; NO new renderer→main control channel")
  </read_first>
  <action>
    In `src/main/overlay-window.manager.ts` add an `import type { IUtteranceEvent } from './stt/stt-provider.interface'` (or extend the existing type import if one is present). Extend `IOverlayTranscript` ADDITIVELY with one new field `utterances: IUtteranceEvent[]` (JSDoc: "The committed, speaker-attributed, classified utterances for this session, oldest first — consumed by the Phase 9 card panel over the same read-only channel"). Do NOT remove or rename `finalText`, `interimText`, `connectionState`, or `audioLevel` (the current renderer still reads them; this is additive so Phase 9 migrates the panel without breaking the HUD). Do NOT add a new channel — the field rides the existing `TRANSCRIPT_CHANNEL` (`jedi:transcript`) via the unchanged `pushTranscript`. Keep the "text + state only, never the Deepgram key or any secret" contract note accurate (an `IUtteranceEvent` carries only text/speaker/classification — no secret).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "overlay-window.manager" | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `IOverlayTranscript` gains `utterances: IUtteranceEvent[]` and retains all four existing fields unchanged.
    - Source assertion: no new `webContents.send` channel constant is introduced; `pushTranscript` still sends over `TRANSCRIPT_CHANNEL`.
    - Source assertion: `grep -c "@deepgram/sdk" src/main/overlay-window.manager.ts` returns 0 (imports the seam type, not the SDK).
    - Behavior assertion: `npx tsc --noEmit` reports no error in `overlay-window.manager.ts` (this task alone will leave call sites needing the new field — Task 2 supplies them; run tsc after Task 2 for a clean whole-project pass).
  </acceptance_criteria>
  <done>The overlay transcript payload additively carries the utterance list on the existing one-way channel; no new channel, no secret, no removed field.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the utterance binding and the Ctrl+Alt+K speaker reset in main (re-key safe)</name>
  <files>src/main/index.ts</files>
  <read_first>
    - src/main/index.ts (buildHandlers ~:98-126 clear-transcript; wireSttPipeline ~:221-297; attachSttGatewayHandlers ~:316-345; the buffer instantiation ~:366; rekeyDeepgram ~:281-294)
    - src/main/overlay-window.manager.ts (IOverlayTranscript after Task 1; pushTranscript)
    - src/main/stt/deepgram-stt.gateway.ts (on('utterance') + clearSpeakers — Plan 02)
    - .planning/phases/08-diarized-utterance-pipeline/08-PATTERNS.md (section "src/main/index.ts (MODIFY)" — utterance binding + clear-path extension, re-key Pitfall 3)
    - .planning/phases/08-diarized-utterance-pipeline/08-CONTEXT.md (D-05 clear-together)
  </read_first>
  <action>
    In `src/main/index.ts`, maintain a main-owned committed-utterance list alongside the `buffer` (an `IUtteranceEvent[]` that grows on each committed utterance and is emptied on clear — the authoritative session utterance stream, mirroring how `buffer` is the authoritative transcript store). Wire it so every `pushTranscript(...)` call site passes `utterances: <the list>` in the payload (the transcript handler, the connection-state handler, the clear handler, and the audio-level push in `wireSttPipeline`). Add a `gateway.on('utterance', (utterance: IUtteranceEvent) => { <push the utterance into the main-owned list>; pushTranscript(window, { ...buffer.renderable(), utterances: <list>, connectionState: getConnectionState(), audioLevel: getAudioLevel() }); })` binding INSIDE `attachSttGatewayHandlers` (alongside the existing `on('transcript')`), so the live re-key path (`rekeyDeepgram`) re-attaches it too — otherwise a re-keyed socket emits utterances to no listener (Pitfall 3). Thread the utterance list into `attachSttGatewayHandlers` and `buildHandlers` the SAME way `buffer` is threaded (add it as a sibling parameter and pass it from `app.whenReady().then`). Extend the `'clear-transcript'` handler (D-05): after `buffer.clear()`, also empty the main-owned utterance list AND call `sttGateway?.clearSpeakers()` (reset the Person N map + accumulator on the CURRENT gateway instance via the module-level re-pointable ref, so a re-keyed gateway is cleared), then `pushTranscript` the emptied snapshot (with `utterances: []`). Do NOT add any `@deepgram/sdk` import to `index.ts` — main depends only on the seam (QA-07). Do NOT add a renderer→main channel.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `attachSttGatewayHandlers` contains a `gateway.on('utterance', …)` binding that pushes the committed utterance to the overlay via `pushTranscript` on the existing channel.
    - Source assertion: the `'clear-transcript'` handler calls `buffer.clear()`, empties the main-owned utterance list, and calls `sttGateway?.clearSpeakers()` before pushing (D-05); the utterance binding is inside `attachSttGatewayHandlers` (re-key safe, Pitfall 3).
    - Source assertion: `grep -c "@deepgram/sdk" src/main/index.ts` returns 0 (main consumes the seam only, QA-07).
    - Source assertion: no new `webContents.send`/`ipcMain.handle`/`ipcMain.on` channel is introduced for utterances (no new control surface).
    - Behavior assertion: `npm test` exits 0 and `npx tsc --noEmit` is clean project-wide (all `pushTranscript` call sites supply the new `utterances` field).
  </acceptance_criteria>
  <done>Committed utterances flow to the overlay over the existing channel on the boot and re-key paths, and Ctrl+Alt+K resets transcript + utterances + Person N numbering together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (push) | The committed utterance list crosses to the overlay over the existing one-way `jedi:transcript` channel; the renderer remains a pure view (no reverse control channel added). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-07 | Information Disclosure | overlay transcript push payload | mitigate | The payload carries only `IUtteranceEvent` fields (text/speaker/classification) + existing text/state; `DEEPGRAM_API_KEY` and all secrets stay in main and are never placed in the payload (unchanged D-08 contract). |
| T-8-08 | Elevation of Privilege | renderer→main surface | mitigate | No `ipcMain.handle`/`ipcMain.on`/new channel is added; the utterance stream is push-only over the existing read-only bridge (CONTEXT Integration Points), preserving the click-through, view-only overlay discipline. |
| T-8-09 | DoS | main-owned utterance list unbounded growth | accept | The list is session-scoped and drained on Ctrl+Alt+K (D-05), mirroring `TranscriptBuffer`'s session lifetime; a single meeting's utterance count is bounded in practice. If hardening is later desired, cap it like `MAX_SEGMENTS` — not required this phase (RESEARCH V5). |
| T-8-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No slopcheck action required. |
</threat_model>

<verification>
- `npm test` green (full suite; confirms the whole pipeline typechecks and the STT unit suites still pass with the new wiring).
- `npx tsc --noEmit` clean project-wide (every `pushTranscript` call supplies `utterances`).
- `rg "@deepgram/sdk" src --files-with-matches` lists ONLY `src/main/stt/deepgram-stt.gateway.ts` (+ its test) — `index.ts` and `overlay-window.manager.ts` are absent (QA-07/D-09).
- Manual (VALIDATION Manual-Only): live session with 2 speakers shows distinct Person 1/Person 2 utterances reaching the overlay; Ctrl+Alt+K restarts numbering at Person 1.
</verification>

<success_criteria>
- Committed `utterance` events reach the overlay over the existing read-only push channel (QA-01 observable half) with no new control channel.
- The utterance binding is re-key safe (attached in the shared helper).
- Ctrl+Alt+K resets buffer + utterance list + Person N numbering together (QA-02 / D-05).
- Main imports no `@deepgram/sdk` — it depends only on the seam (QA-07).
</success_criteria>

<output>
Create `.planning/phases/08-diarized-utterance-pipeline/08-03-SUMMARY.md` when done.
</output>
