---
phase: 05-ai-orchestration-answer-talking-points
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - src/main/ai/prompt-assembler.test.ts
  - src/main/ai/ai-history.test.ts
  - src/main/ai/ai-orchestrator.test.ts
  - src/main/ai/ai-gateway.interface.ts
  - src/main/ai/anthropic-ai.gateway.ts
  - src/main/ai/prompt-assembler.ts
  - src/main/ai/ai-history.ts
  - src/main/ai/ai-orchestrator.ts
  - src/main/overlay-window.manager.ts
  - src/main/hotkey-registrar.service.ts
  - src/main/index.ts
  - src/preload/index.ts
  - src/renderer/src/components/ai-panel.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/assets/hud.css
autonomous: false
requirements: [AI-01, AI-04]
user_setup:
  - service: anthropic
    why: "Streaming Claude calls (Answer mode); STT already uses Deepgram"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> Settings -> API keys"

must_haves:
  truths:
    - "Pressing Ctrl+Alt+A mid-conversation appends a new AI panel entry that shows thinking… then streams an answer token-by-token"
    - "The AI panel is always visible regardless of the Ctrl+Alt+H HUD toggle (it is a sibling of DebugHud, not inside it)"
    - "An empty ~60s transcript span shows 'No recent transcript to act on' and makes NO API call"
    - "Re-pressing Ctrl+Alt+A while its own stream is in flight cancels it cleanly and marks the entry (cancelled)"
    - "A missing ANTHROPIC_API_KEY shows 'AI error: missing API key' inline without logging the key"
  artifacts:
    - path: "src/main/ai/ai-gateway.interface.ts"
      provides: "IAiGateway seam + AiMode + IAiPromptRequest + IAiStream (no SDK import)"
      contains: "export interface IAiGateway"
    - path: "src/main/ai/anthropic-ai.gateway.ts"
      provides: "AnthropicGateway implements IAiGateway over @anthropic-ai/sdk; constructor-injected key"
      contains: "implements IAiGateway"
    - path: "src/main/ai/prompt-assembler.ts"
      provides: "Pure assemblePrompt(input) with empty Phase-5 grounding-context slot (D-13)"
      contains: "export function assemblePrompt"
    - path: "src/main/ai/ai-history.ts"
      provides: "Bounded AI entry history (last-N + char ceiling) with injected clock"
      contains: "export class AiHistory"
    - path: "src/main/ai/ai-orchestrator.ts"
      provides: "Single-in-flight lifecycle: trigger(mode), empty-span guard, request-id guard"
      contains: "export class AiOrchestrator"
    - path: "src/renderer/src/components/ai-panel.tsx"
      provides: "Always-on streaming AI panel rendered outside the HUD-toggle gate"
      contains: "card-ai-panel"
  key_links:
    - from: "src/main/index.ts"
      to: "AnthropicGateway constructor"
      via: "process.env.ANTHROPIC_API_KEY read in main after loadDotenvFile"
      pattern: "new AnthropicGateway\\(process\\.env\\.ANTHROPIC_API_KEY"
    - from: "src/main/ai/ai-orchestrator.ts"
      to: "transcriptBuffer.recentSince"
      via: "RECENT_SPAN_MS span read before any gateway call"
      pattern: "recentSince\\(RECENT_SPAN_MS\\)"
    - from: "src/main/overlay-window.manager.ts"
      to: "renderer AiPanel"
      via: "pushAi over the new jedi:ai channel"
      pattern: "AI_CHANNEL"
---

<objective>
Deliver the first real AI value as a complete vertical slice: pressing **Ctrl+Alt+A** (Answer mode) reads the recent transcript span, streams a Claude answer token-by-token, and renders it in a new always-on AI panel on the overlay. This is the thinnest end-to-end path — UI surface, main-side orchestration, and the Anthropic SDK seam — that makes AI-01 and AI-04 observable. Talking-points (05-02) and full scrollback/focus (05-03) build on this.

Purpose: Establish the entire main-side AI stack (gateway seam, prompt assembler, bounded history, single-in-flight orchestrator) and the `jedi:ai` push + AiPanel render so every later plan is additive, not foundational.

Output: `@anthropic-ai/sdk@0.104.2` installed; three Wave-0 test stubs; the `src/main/ai/` module; the new `jedi:ai` channel + `onAi` preload bridge; the always-on `AiPanel`; the `Ctrl+Alt+A` Answer chord wired end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-CONTEXT.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-RESEARCH.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-PATTERNS.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-VALIDATION.md

<interfaces>
<!-- Contracts the executor builds against. Extracted from the codebase; do not re-explore. -->

From src/main/stt/transcript-buffer.ts (span read + bounded-buffer analog):
```typescript
export const WINDOW_MS = 90_000;
export const MAX_SEGMENTS = 400;
export const MAX_TOTAL_CHARS = 20_000;
export class TranscriptBuffer {
    public constructor(private readonly now: () => number = Date.now) {}
    public recentSince(ms: number): string; // space-joined finalized text within last `ms` ('' if none)
    public clear(): void;
}
```

From src/main/stt/stt-provider.interface.ts (seam analog — copy the file-level TSDoc + @remarks by-convention-singleton block):
```typescript
export type SttConnectionState = 'connecting' | 'connected' | 'reconnecting' | 'disconnected' | 'error';
export interface ISttProvider {
    on(event: 'transcript', listener: (e: ISttTranscriptEvent) => void): void;
    on(event: 'error', listener: (error: Error) => void): void;
}
```

From src/main/overlay-window.manager.ts (push + flag analogs):
```typescript
export const TRANSCRIPT_CHANNEL = 'jedi:transcript';
export interface IOverlayStatus { electronVersion: string; contentProtection: boolean; position: { x: number; y: number }; hotkeys: { active: string; failed: string[] }; hudVisible: boolean; }
export function pushTranscript(window: BrowserWindow, payload: IOverlayTranscript): void; // guards window.isDestroyed() || window.webContents.isDestroyed()
export function pushStatus(window: BrowserWindow): void;
// hudVisible module-level flag pattern: let hudVisible = true; setHudVisible/getHudVisible; carried in buildStatus()
```

From src/main/index.ts (key load + wiring analogs):
```typescript
loadDotenvFile(resolve(app.getAppPath(), '.env')); // MUST run before any process.env read
const gateway = new DeepgramSttGateway(process.env.DEEPGRAM_API_KEY ?? ''); // mirror for ANTHROPIC_API_KEY
// buildHandlers(...) returns Record<string, () => void>; 'clear-transcript' calls buffer.clear() + pushTranscript(...)
// 66ms trailing throttle precedent: if (nowMs - lastLevelPushMs >= 66) { ... }
```

From src/preload/index.ts (bridge analog — declare types locally, do NOT import from main):
```typescript
const TRANSCRIPT_CHANNEL = 'jedi:transcript';
onTranscript(callback: (t: IOverlayTranscript) => void): () => void; // ipcRenderer.on + returns removeListener
```

From src/renderer/src/components/debug-hud.tsx (render + scroll analog):
```typescript
const stickToBottomRef = useRef<boolean>(true);
// useEffect: capture offStatus/offTranscript/offScroll, return cleanup calling all three
// auto-stick useEffect keyed on [transcript?.finalText, transcript?.interimText]
// DebugHud returns null when !hudVisible  — AiPanel must NOT do this (always-on, D-01)
```

From RESEARCH.md §IAiGateway seam / §AnthropicGateway / §PromptAssembler / §AiOrchestrator — the exact target signatures (lines 296-456) and the two DRAFT system prompts (lines 402-429, marked [ASSUMED]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Install @anthropic-ai/sdk and create the three failing test stubs</name>
  <files>package.json, package-lock.json, .env.example, src/main/ai/prompt-assembler.test.ts, src/main/ai/ai-history.test.ts, src/main/ai/ai-orchestrator.test.ts</files>
  <read_first>
    - package.json (confirm @deepgram/sdk@5.4.0 present, no native postinstall for the new dep)
    - .planning/phases/05-ai-orchestration-answer-talking-points/05-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - src/main/stt/transcript-buffer.test.ts (fake-clock AAA idiom: `let nowMs = 0; const buffer = new TranscriptBuffer(() => nowMs)`)
    - src/main/stt/deepgram-stt.gateway.test.ts (FakeV1Socket EventEmitter + vi.mock SDK substitution + vi.useFakeTimers for debounce)
  </read_first>
  <action>
    Run `npm install @anthropic-ai/sdk@0.104.2` (pure JS — NO `--ignore-scripts`, NO @electron/rebuild; unlike uiohook/native-recorder). Confirm `npm view @anthropic-ai/sdk@0.104.2 scripts.postinstall` is empty.
    Create the three CO-LOCATED test stubs at `src/main/ai/<name>.test.ts` (NOT a `test/` subdir — PATTERNS.md test-location correction): `prompt-assembler.test.ts`, `ai-history.test.ts`, `ai-orchestrator.test.ts`. Each stub imports from its (not-yet-created) source sibling and contains describe/it blocks that FAIL initially (the source does not exist yet) covering: prompt-assembler → AI-01 answer system prompt selected + labeled span embedded + `context: undefined` yields empty context block (D-13); ai-history → append, last-N eviction, char ceiling, relative-time header, clear() with injected fake clock; ai-orchestrator → D-11 empty-span guard makes NO gateway.stream() call, D-06 re-press aborts, Pitfall-1 request-id guard (a stale aborted stream's late delta does not append to the new entry), AI-04 trailing-edge debounce via vi.useFakeTimers. Build a `FakeAiGateway` in the orchestrator test mirroring FakeV1Socket: an EventEmitter subclass with `stream = vi.fn()` returning `{ abort: vi.fn() }`. Add `ANTHROPIC_API_KEY=` to `.env.example` (create if absent; the real key goes in the gitignored `.env`).
    Talking-points-specific prompt assertions belong to 05-02; this stub asserts the answer path + the shared D-13 empty-slot behavior only.
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/prompt-assembler.test.ts src/main/ai/ai-history.test.ts src/main/ai/ai-orchestrator.test.ts ; test $? -ne 0 && echo "STUBS-RED-AS-EXPECTED"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('@anthropic-ai/sdk/package.json').version"` prints `0.104.2`
    - Three files exist: `src/main/ai/prompt-assembler.test.ts`, `src/main/ai/ai-history.test.ts`, `src/main/ai/ai-orchestrator.test.ts` (none under a `test/` subdir)
    - The three test files reference a `FakeAiGateway` and `vi.useFakeTimers` (grep -l FakeAiGateway src/main/ai/ai-orchestrator.test.ts)
    - `.env.example` contains `ANTHROPIC_API_KEY=`
  </acceptance_criteria>
  <done>SDK installed (pure JS, no rebuild); three co-located test stubs exist and fail because their sources do not yet exist; .env.example documents the new key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1): AI seam, prompt assembler, bounded history (pure main-side logic)</name>
  <files>src/main/ai/ai-gateway.interface.ts, src/main/ai/prompt-assembler.ts, src/main/ai/ai-history.ts, src/main/ai/prompt-assembler.test.ts, src/main/ai/ai-history.test.ts</files>
  <read_first>
    - src/main/stt/stt-provider.interface.ts (whole file — copy the file-level seam TSDoc + the @remarks by-convention-singleton block verbatim, swap Deepgram→Anthropic)
    - src/main/stt/transcript-buffer.ts (whole file — the constants/append/prune/clear/injected-clock pattern is the AiHistory template)
    - src/main/config/load-dotenv.utility.ts (pure-function explicit-return-type shape for the assembler)
    - .planning/phases/05-ai-orchestration-answer-talking-points/05-RESEARCH.md (§IAiGateway seam lines 296-338; §PromptAssembler seam lines 431-456; DRAFT prompts lines 402-429)
    - src/main/ai/prompt-assembler.test.ts, src/main/ai/ai-history.test.ts (the stubs that must turn green)
  </read_first>
  <behavior>
    - assemblePrompt: mode `'answer'` selects `ANSWER_SYSTEM_PROMPT`; userContent embeds the labeled span as `Recent transcript (last ~60s):\n<span>`; `context: undefined` → `formatContext` returns `''` so the call site is unchanged in Phase 6 (D-13).
    - AiHistory.append then prune to MAX_AI_ENTRIES (last-N) and MAX_AI_TOTAL_CHARS; oldest entries shift() out first; clear() empties; each entry carries mode + the `at` timestamp captured from the injected clock (the D-03 header source).
    - D-03 relative-time split: the `at` capture is unit-testable here and IS asserted (append at a known fake-clock time, snapshot()[i].at equals that time). The relative-time *string* formatting ("3s ago") lives in the renderer (ai-panel.tsx, Task 3) and is manual-only (overlay is focusable:false). This task owns the testable timestamp; Task 3 owns the display string.
    - AiHistory.snapshot() returns the bounded entry list for the terminal/clear push.
  </behavior>
  <action>
    Create `src/main/ai/ai-gateway.interface.ts`: `export type AiMode = 'answer' | 'talking-points';` (keep the HotkeyLayer string-union lineage note), `IAiStream { abort(): void }`, `IAiPromptRequest { model: string; maxTokens: number; system: string; userContent: string }`, and `IAiGateway` with overloaded `on('text'|'done'|'error'|'abort', listener)` + `stream(request): IAiStream`. No `@anthropic-ai/sdk` import in this file. Copy the @remarks by-convention-singleton block from ISttProvider.
    Create `src/main/ai/prompt-assembler.ts` (pure, no class, no IO): `IGroundingContext { notes?; ticketText?; repoSnippets?; links?: string[] }`, `IAssembleInput { mode: AiMode; span: string; context?: IGroundingContext }`, `IAssembledPrompt { system: string; userContent: string }`, exported `assemblePrompt(input)` and the two named system-prompt constants `ANSWER_SYSTEM_PROMPT` / `TALKING_POINTS_SYSTEM_PROMPT` using the RESEARCH.md DRAFT wording. `formatContext(undefined)` MUST return `''` (D-13). Define `RECENT_SPAN_MS = 60_000` as the named span constant (D-09) — export it here or in a small constants module the orchestrator imports.
    Create `src/main/ai/ai-history.ts`: `export class AiHistory` mirroring TranscriptBuffer — `MAX_AI_ENTRIES = 50` and `MAX_AI_TOTAL_CHARS = 20_000` as exported named constants (D-02; planner-chosen grounded defaults), injected `now: () => number = Date.now`, `append(entry)`/`prune()`/`clear()`/`snapshot()`, each entry `{ id, mode, text, kind, at }`. Mirror the prune `while (over ceiling) this.entries.shift()` loop and the "can never grow unbounded" rationale TSDoc.
    Implement enough of the assembler + history to turn the two stubs green. The orchestrator (Task 2) consumes these.
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/prompt-assembler.test.ts src/main/ai/ai-history.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/ai/prompt-assembler.test.ts src/main/ai/ai-history.test.ts` exits 0
    - `grep -L "@anthropic-ai/sdk" src/main/ai/ai-gateway.interface.ts` lists the file (no SDK import in the seam)
    - `grep -c "export const RECENT_SPAN_MS = 60_000" src/main/ai/prompt-assembler.ts` (or the constants module) is 1
    - prompt-assembler.ts contains `ANSWER_SYSTEM_PROMPT` and `TALKING_POINTS_SYSTEM_PROMPT`
    - ai-history.ts contains `export class AiHistory` and `MAX_AI_ENTRIES`
    - ai-history.test.ts asserts the `at` timestamp is captured from the injected clock (D-03 header source): append at a fake-clock time and assert `snapshot()` entry `.at` equals that time
  </acceptance_criteria>
  <done>The seam, the pure prompt assembler (with the empty D-13 context slot), and the bounded history (with clock-sourced entry timestamps for the D-03 header) exist; their two unit test stubs pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): AnthropicGateway + AiOrchestrator (single-in-flight lifecycle)</name>
  <files>src/main/ai/anthropic-ai.gateway.ts, src/main/ai/ai-orchestrator.ts, src/main/ai/ai-orchestrator.test.ts</files>
  <read_first>
    - src/main/stt/deepgram-stt.gateway.ts (whole file — `extends EventEmitter implements I…`, constructor-injected key TSDoc verbatim, the `emitError` no-listener guard lines 200-212, `error instanceof Error ? … : new Error(…)` normalization, the held-handle teardown-never-throws discipline)
    - src/main/index.ts lines 106-161 (wireSttPipeline event-handler→buffer→push wiring; the error handler that does NOT log the payload, lines 131-135; the 66ms throttle lines 140-148)
    - .planning/phases/05-ai-orchestration-answer-talking-points/05-RESEARCH.md (§AnthropicGateway lines 340-371; §AiOrchestrator cancel logic lines 373-399; Pitfall 1 lines 258-263; Pitfall 4 lines 276-281)
    - src/main/ai/ai-orchestrator.test.ts, src/main/ai/ai-gateway.interface.ts, src/main/ai/prompt-assembler.ts, src/main/ai/ai-history.ts
  </read_first>
  <behavior>
    - D-11: `trigger(mode)` reads `recentSince(RECENT_SPAN_MS)`; if `span.trim().length === 0`, append an `empty`-kind entry ("No recent transcript to act on") and return WITHOUT calling `gateway.stream`.
    - D-06: if a stream for the SAME mode is in flight, `abort()` it and return (entry → `(cancelled)`).
    - D-07: if a stream for the OTHER mode is in flight, `abort()` current then start the new one.
    - Pitfall 1: every stream + its target entry is tagged with a monotonic `requestId`; gateway-event handlers ignore events whose requestId !== the active one.
    - AI-04: text deltas are pushed on a trailing-edge debounce (~40ms named constant, in the 30-60ms band); a terminal `done`/`error`/`abort` flushes immediately.
  </behavior>
  <action>
    Create `src/main/ai/anthropic-ai.gateway.ts`: `import Anthropic from '@anthropic-ai/sdk';` + `export class AnthropicGateway extends EventEmitter implements IAiGateway`. Constructor `(private readonly apiKey: string)` — copy the Deepgram constructor TSDoc warning verbatim (key read in main only, never logged/emitted/IPC'd, swap Deepgram→Anthropic). `stream(request)` calls `this.client.messages.stream({ model: request.model, max_tokens: request.maxTokens, system: request.system, messages: [{ role: 'user', content: request.userContent }] })`, wires `stream.on('text', d => this.emit('text', d))`, `stream.on('abort', () => this.emit('abort'))`, `stream.on('error', e => this.emitError(e instanceof Error ? e : new Error('AI stream error')))`, and `void stream.finalText().then(t => this.emit('done', t)).catch(() => {})`, returns `{ abort: () => stream.abort() }`. Copy the `emitError` no-listener guard exactly. NEVER read `process.env` inside this file.
    Create `src/main/ai/ai-orchestrator.ts`: `export class AiOrchestrator` (by-convention singleton). Constructor injects the `IAiGateway`, the `TranscriptBuffer`, the `AiHistory`, and a `pushAi(event)` callback (the orchestrator does not import the overlay manager directly — index.ts injects a push closure, mirroring how wireSttPipeline closes over `pushTranscript(window, …)`). Implement `trigger(mode: AiMode)` per the behaviors above using per-mode model constants `ANSWER_MODEL = 'claude-haiku-4-5'` and `TALKING_POINTS_MODEL = 'claude-opus-4-8'` (D-10, named so a mode can be re-tiered) and `MAX_TOKENS = { answer: 400, 'talking-points': 500 }` (Pitfall 6, named, tunable in 05-03). Define the `IAiPushEvent` discriminated union (`thinking | delta | done | error | cancelled | history-snapshot`, each carrying `requestId`) — push incremental `delta` payloads during streaming, push a terminal snapshot + append to AiHistory on `done` (Pitfall 4: do NOT re-push the full snapshot per delta). Hand-roll the trailing-edge debounce as a `setTimeout` (no library), modeled on the 66ms throttle.
    Turn `ai-orchestrator.test.ts` green using the FakeAiGateway. Never log the key or the error payload anywhere.
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/ai-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/ai/ai-orchestrator.test.ts` exits 0
    - `grep -L process.env src/main/ai/anthropic-ai.gateway.ts` lists the file (key never read from env inside the gateway)
    - anthropic-ai.gateway.ts contains `constructor(private readonly apiKey: string` and `extends EventEmitter implements IAiGateway`
    - ai-orchestrator.ts contains `ANSWER_MODEL = 'claude-haiku-4-5'` and `TALKING_POINTS_MODEL = 'claude-opus-4-8'`
    - ai-orchestrator.ts contains a request-id guard (`requestId !==` appears in an event handler)
  </acceptance_criteria>
  <done>Gateway streams via the SDK helper (never throwing, key constructor-injected); orchestrator enforces single-in-flight + empty-span guard + request-id guard + debounced deltas; the orchestrator test passes.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 1): jedi:ai channel + onAi bridge + always-on AiPanel + Ctrl+Alt+A wiring (end-to-end)</name>
  <files>src/main/overlay-window.manager.ts, src/preload/index.ts, src/renderer/src/components/ai-panel.tsx, src/renderer/src/App.tsx, src/renderer/src/assets/hud.css, src/main/hotkey-registrar.service.ts, src/main/index.ts</files>
  <read_first>
    - src/main/overlay-window.manager.ts (TRANSCRIPT_CHANNEL + pushTranscript lines 36-47, 179-194 — the channel + teardown-guard template)
    - src/preload/index.ts (onTranscript lines 87-94 — copy the subscription shape; declare IAiPushEvent locally, do NOT import from main)
    - src/renderer/src/components/debug-hud.tsx (whole file — subscribe-in-useEffect cleanup + stickToBottomRef; AiPanel must NOT return null on a flag, D-01)
    - src/renderer/src/App.tsx (renders <DebugHud/>; add <AiPanel/> as a sibling under a fragment)
    - src/main/index.ts (whole file — wireSttPipeline injection pattern, buildHandlers map, key load after loadDotenvFile, module-level singletons + window-all-closed teardown)
    - src/main/hotkey-registrar.service.ts lines 53-76 (HOTKEY_CHORDS rows; the clear-transcript row is the discrete-chord template)
    - src/renderer/src/assets/hud.css (existing HUD styles to extend for the panel)
  </read_first>
  <action>
    `overlay-window.manager.ts`: add `export const AI_CHANNEL = 'jedi:ai';` + `export function pushAi(window, payload: IAiPushEvent)` with the IDENTICAL `isDestroyed()` teardown guard; declare/export `IAiPushEvent` (the discriminated union from Task 2) here. Reuse the "separate channel from jedi:status because high-frequency" TSDoc rationale.
    `preload/index.ts`: add `onAi(callback: (event: IAiPushEvent) => void): () => void` on `jediApi` mirroring `onTranscript` (local `const AI_CHANNEL = 'jedi:ai';`, locally-declared `IAiPushEvent`, returns an unsubscribe). One-way only — NO renderer→main control channel (IN-01).
    `ai-panel.tsx` (NEW): subscribe to `window.jedi.onAi(...)` in a useEffect returning a combined cleanup; maintain a local mirror of the entry list, append `delta` payloads to the in-progress entry, reconcile to the pushed snapshot on terminal events. Render each entry with a small header (mode label `Answer`/`Talking points` + relative time, D-03) and an inline state (`thinking…` / streaming text / `AI error: <reason>` / `(cancelled)`, D-04). Reuse the `stickToBottomRef` auto-follow useEffect keyed on the in-progress entry text. The panel is ALWAYS rendered (never returns null on a flag — D-01). Use `data-testid="card-ai-panel"` and `row-ai-entry-${id}`. Add panel CSS to `hud.css`.
    `App.tsx`: render `<><DebugHud /><AiPanel /></>` so the panel is a sibling unaffected by `hudVisible`. Update the file-level TSDoc.
    `hotkey-registrar.service.ts`: add ONLY the `ai-answer` row to HOTKEY_CHORDS: `{ label: 'ai-answer', keycode: UiohookKey.A, accelerator: 'Ctrl+Alt+A', kind: 'discrete' }` with an inline comment noting A is outside the locked conflict-tested set and the 05-02 conflict re-check is pending (fallback letter reserved). No registrar logic change.
    `index.ts`: declare THREE new module-level by-convention singletons alongside `sttGateway`/`audioCapture` (the same pattern at index.ts:18-26) — `let aiGateway: AnthropicGateway | undefined;`, `let aiOrchestrator: AiOrchestrator | undefined;`, and `let aiHistory: AiHistory | undefined;`. `aiHistory` MUST be this single shared module-level instance (NOT constructed inside the orchestrator), because 05-02's clear-AI handler calls `aiHistory.clear()`/`snapshot()` and must bind to the SAME instance the orchestrator was injected with (Fix 3). After `loadDotenvFile`: assign `aiGateway = new AnthropicGateway(process.env.ANTHROPIC_API_KEY ?? '');`, `aiHistory = new AiHistory();`, then `aiOrchestrator = new AiOrchestrator(aiGateway, buffer, aiHistory, (event) => pushAi(window, event));` (the shared `buffer`, the shared `aiHistory`, and a `pushAi(window, event)` closure mirroring how wireSttPipeline closes over pushTranscript). If the key is empty, the orchestrator surfaces `AI error: missing API key` inline (Pitfall 3) — never log the key or the error payload.
    EXPLICIT signature change (Fix 2): extend `buildHandlers` from `buildHandlers(actions, window, buffer, getConnectionState)` (index.ts:42) to `buildHandlers(actions, window, buffer, getConnectionState, aiOrchestrator, aiHistory)` and update its single call site (index.ts:184) to pass the new singletons — inject them, do NOT reach a module global from inside a handler. Add only the `ai-answer` handler here → `aiOrchestrator.trigger('answer')`; the `clear-ai` handler (using the injected `aiHistory`) is added in 05-02 against this same signature. The gateway holds no socket, so no extra teardown is required in window-all-closed; keep the module-level handles for symmetry.
    Wire only the Answer path here; talking-points/focus/clear chords are 05-02 and the scroll routing is 05-03.
  </action>
  <verify>
    <automated>npx vitest run && npm run format:check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite; no regressions in STT tests)
    - `npm run format:check && npm run lint` exit 0
    - `grep -c "AI_CHANNEL = 'jedi:ai'" src/main/overlay-window.manager.ts` is 1
    - `grep -c "new AnthropicGateway(process.env.ANTHROPIC_API_KEY" src/main/index.ts` is 1
    - ai-panel.tsx contains `data-testid="card-ai-panel"` and does NOT contain a `return null` gated on a visibility flag
    - App.tsx renders both `<DebugHud` and `<AiPanel`
    - hotkey-registrar.service.ts contains `label: 'ai-answer'` with `accelerator: 'Ctrl+Alt+A'`
    - index.ts declares a module-level `let aiHistory` (shared singleton) and passes it into both the AiOrchestrator constructor and buildHandlers (same instance — Fix 3)
    - `buildHandlers` signature includes `aiOrchestrator` and `aiHistory` parameters and its call site passes them (Fix 2): `grep -c "aiOrchestrator" src/main/index.ts` is >= 3 (declaration, constructor, handler)
  </acceptance_criteria>
  <done>jedi:ai channel + onAi bridge exist; the always-on AiPanel renders streamed entries outside the HUD gate; Ctrl+Alt+A triggers the Answer path end-to-end; full suite + lint + format green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: The end-to-end Answer slice</name>
  <files>(manual on-machine verification — no files modified by this task)</files>
  <action>This is a human-verify checkpoint. The executor has already automated all build work in the prior tasks; pause here and present the how-to-verify steps to the user, then wait for the resume signal. Do not proceed until approved.</action>
  <what-built>The end-to-end Answer slice: Ctrl+Alt+A streams a Claude Haiku answer from the recent transcript into a new always-on AI panel on the overlay, with thinking…/streaming/cancelled/error inline states and an empty-span guard.</what-built>
  <how-to-verify>
    1. Put `ANTHROPIC_API_KEY=<your key>` in the gitignored `.env`, then `npm run dev`.
    2. Play system audio with a question (e.g. an interview-style question) so the live transcript fills.
    3. Press `Ctrl+Alt+A`. Confirm a new AI panel entry appears below the HUD: header `Answer` + a relative time, a brief `thinking…`, then text streaming in token-by-token (debounced, not janky).
    4. Press `Ctrl+Alt+H` to toggle the HUD off. Confirm the AI panel + its answer stay visible (D-01).
    5. Press `Ctrl+Alt+A` again while a fresh answer is mid-stream. Confirm the entry is marked `(cancelled)` and no further tokens arrive (D-06).
    6. Press `Ctrl+Alt+K` (clear transcript) to empty the buffer, then immediately press `Ctrl+Alt+A`. Confirm an entry reads `No recent transcript to act on` and (check the dev console) NO Anthropic request was made (D-11).
    7. Temporarily blank the key in `.env`, restart, press `Ctrl+Alt+A`. Confirm `AI error: missing API key` inline and that the dev console log line does NOT contain the key.
    8. Read the streamed Answer-mode reply and confirm the ANSWER_SYSTEM_PROMPT wording reads acceptably (direct, spoken-style, no preamble/markdown — D-12). NOTE: the two system prompts are [ASSUMED] DRAFT wording (the claude-api skill was unavailable at research time); the shape satisfies D-12 but the exact wording was never user-confirmed. Wording is tunable as Claude's discretion post-verify — flag any phrasing you want changed in the resume signal.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting any system-prompt wording you want tuned) or describe what failed (e.g. no streaming, panel hidden by HUD toggle, cancel leaves text appending).</resume-signal>
  <verify><human-check>Complete the how-to-verify steps on the target machine; resume only on "approved".</human-check></verify>
  <done>User typed "approved" after completing every how-to-verify step (or reported and resolved issues).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → Anthropic API | Transcript span (plain text) leaves the machine to api.anthropic.com over TLS; accepted v1 privacy posture (PROJECT.md). |
| main → renderer (`jedi:ai`) | One-way push of AI text + state. Renderer is a pure view; no control channel back (IN-01). |
| env/.env → main | The Anthropic key enters main only, via `process.env` after `loadDotenvFile`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | Anthropic API key | mitigate | Key read in main only (index.ts after loadDotenvFile), constructor-injected to AnthropicGateway; gateway never reads process.env, never logs/emits/IPCs the key (mirrors Deepgram D-08). `.env` gitignored; `.env.example` carries an empty placeholder only. |
| T-5-02 | Information Disclosure | AI `error` handler | mitigate | Never log the error payload (mirror index.ts:131-135 Deepgram policy); surface a sanitized `AI error: <reason>` inline only — the SDK error object can embed `x-api-key` request headers. |
| T-5-03 | Tampering / resource leak | Stream abort cleanliness | mitigate | Monotonic request-id guard (Pitfall 1): handlers ignore events whose requestId is no longer active, so an aborted stream's late deltas cannot bleed into the new entry; single-in-flight invariant aborts the current stream before starting a new one (D-07). Unit-tested in ai-orchestrator.test.ts. |
| T-5-04 | Denial of Service / cost | Oversized/repeated prompts | mitigate | Span is the ~60s `recentSince` sub-window of an already-bounded buffer (90s/400 seg/20k char); `max_tokens` per-mode cap; AI calls are user-triggered only; empty-span guard (D-11) prevents zero-value calls. |
| T-5-05 | Elevation of Privilege | Renderer→main control | accept | No control channel added; AI triggers come from main-side hotkeys only (IN-01). The renderer stays a pure `onAi` subscriber. |
| T-5-SC | Tampering | `npm install @anthropic-ai/sdk@0.104.2` | accept | Official first-party Anthropic SDK under `@anthropic-ai` org, 3+ yrs old, multi-million weekly downloads, no postinstall (RESEARCH Package Legitimacy Audit — Approved). Not a slopsquat candidate; no human gate required. |
</threat_model>

<verification>
- Full unit suite green: `npx vitest run`
- Lint + format: `npm run lint && npm run format:check`
- Key hygiene: `grep -L process.env src/main/ai/anthropic-ai.gateway.ts` (gateway never reads env); no test or source logs the key
- On-machine human-verify checkpoint above (live streaming render is not unit-testable — overlay is focusable:false)
</verification>

<success_criteria>
- AI-01: Ctrl+Alt+A produces a Claude answer drawn from the recent transcript span (live verify step 3).
- AI-04: tokens stream into the panel debounced and readable (live verify step 3).
- D-01: AI panel stays visible through the HUD toggle (step 4). D-06: re-press cancels (step 5). D-11: empty-span guard, no API call (step 6). Key hygiene (step 7).
- prompt-assembler / ai-history / ai-orchestrator unit suites pass.
</success_criteria>

<output>
Create `.planning/phases/05-ai-orchestration-answer-talking-points/05-01-SUMMARY.md` when done.
</output>
