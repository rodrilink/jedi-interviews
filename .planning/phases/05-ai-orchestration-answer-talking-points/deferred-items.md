# Deferred Items — Phase 05

Out-of-scope discoveries logged during execution (per executor SCOPE BOUNDARY). NOT fixed here.

| Discovered | Item | Why deferred |
|-----------|------|--------------|
| 05-01 Task 3 | `npm run format:check` reports 43 files (incl. untouched `vitest.config.ts`, `tsconfig.json`, `VERIFICATION.md`) as needing Prettier. The diff is **CRLF-only**: `git config core.autocrlf=true` checks files out with CRLF while Prettier's default `endOfLine: lf` expects LF. All files I touched in this plan are Prettier-clean ignoring line endings (verified file-by-file). | Pre-existing, repo-wide line-ending condition unrelated to this plan's changes. Mass-reformatting every file (or changing the Git/Prettier line-ending policy) is out of scope for an AI-orchestration plan and would balloon the diff. Recommend a dedicated `/gsd:quick` to set `endOfLine` policy (e.g. add `endOfLine: 'crlf'` or a `.gitattributes` `* text eol=lf` + renormalize) so `format:check` is green on Windows checkouts. |
| 05-03 Task 1 | Same repo-wide `npm run format:check` CRLF condition recurs (now 54 files flagged, incl. untouched `tsconfig.json`/`env.d.ts`). Confirmed: `git show HEAD:tsconfig.json \| prettier --check --stdin-filepath` is CLEAN while the on-disk file FAILS (CRLF working copy vs LF blob). All 9 files touched in 05-03 were `prettier --write` and re-verified individually CLEAN — no NEW violations introduced. | Same pre-existing line-ending root cause as the 05-01 entry above. Still recommend a dedicated `/gsd:quick` to set the `endOfLine` policy / `.gitattributes` renormalize so whole-repo `format:check` is green on Windows checkouts. |
| 05-02 Task 1 | `npm run typecheck` fails with 3 TS errors in `src/main/ai/sanitize-ai-error.utility.test.ts` (TS2345/TS2749 — `Anthropic.BadRequestError`/`InternalServerError` constructor signatures not assignable to `typeof APIError`, and `InternalServerError` used as a type). The file is **byte-identical to this plan's base commit** (introduced in 05-01) and was NOT touched by 05-02. The vitest suite (esbuild, no type-check) passes all 7 of its cases. | Pre-existing failure in a Wave-1 file unrelated to 05-02's scope (talking-points + clear-AI chords). The `@anthropic-ai/sdk@0.104.2` typed-error constructors are generic over `TStatus`; the test passes the concrete subclass constructors to a helper typed for the `APIError` base. Fix belongs to a 05-01 follow-up / dedicated `/gsd:quick`: either relax the helper's param type or construct via the base `APIError`. None of 05-02's touched files produce any typecheck error. |
