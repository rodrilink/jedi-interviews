---
phase: 05-ai-orchestration-answer-talking-points
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/main/overlay-window.manager.ts
  - src/main/hotkey-registrar.service.ts
  - src/main/index.ts
  - src/main/ai/ai-orchestrator.ts
  - src/preload/index.ts
  - src/renderer/src/components/ai-panel.tsx
  - src/renderer/src/components/debug-hud.tsx
  - src/renderer/src/assets/hud.css
  - .planning/phases/05-ai-orchestration-answer-talking-points/05-HOTKEY-CONFLICT-TEST.md
autonomous: false
requirements: [AI-05]
must_haves:
  truths:
    - "D-08: Ctrl+Alt+F cycles the active panel between the AI panel and the transcript; a corner indicator shows which is active; default active panel on launch is the AI panel"
    - "D-08: Ctrl+Alt+PgUp/PgDn scroll whichever panel is active (replacing the Phase-4 transcript-only scroll)"
    - "A response longer than the visible AI panel is fully readable via keyboard scrollback"
    - "D-07: Pressing the other mode chord mid-stream cancels the current stream and immediately starts the new one (cancel-current-start-new)"
    - "D-10: Each hotkey-to-first-token latency is logged to the main log only (never to the renderer), enabling the talking-points re-tier watch"
  artifacts:
    - path: "src/main/overlay-window.manager.ts"
      provides: "activePanel flag (default 'ai') carried in IOverlayStatus + setActivePanel/getActivePanel"
      contains: "activePanel"
    - path: "src/main/ai/ai-orchestrator.ts"
      provides: "hotkey->first-token latency logging to the main log"
      contains: "first-token"
  key_links:
    - from: "src/main/index.ts"
      to: "setActivePanel + pushStatus"
      via: "focus-cycle handler flipping the main-owned flag"
      pattern: "setActivePanel"
    - from: "src/renderer/src/components/ai-panel.tsx"
      to: "the active-panel flag"
      via: "scroll handler guarded on status.activePanel === 'ai'"
      pattern: "activePanel"
---

<objective>
Make long AI output fully readable and the cancel UX complete: a main-owned **activePanel** flag with a corner indicator, **Ctrl+Alt+F** to cycle focus, **Ctrl+Alt+PgUp/PgDn** routed to the active panel, cancel-current-start-new across modes, and hotkey-to-first-token latency logging. After this plan, AI-05 (keyboard-scrollable output) is satisfied for both panels and success criterion 5 (re-press / cross-mode cancel) is fully wired.

Purpose: AI-05 keyboard scrollback under the focused-panel model (D-08); the latency instrumentation that gates the Opus talking-points re-tier decision (D-10).

Output: activePanel flag + corner indicator + focus-cycle chord; active-panel-routed scroll; cross-mode cancel verified; latency log lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-CONTEXT.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-RESEARCH.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-PATTERNS.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-01-SUMMARY.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-02-SUMMARY.md

<interfaces>
<!-- From overlay-window.manager.ts (the hudVisible flag triplet is the activePanel template). -->
```typescript
export interface IOverlayStatus { /* ...; */ hudVisible: boolean; } // ADD: activePanel: 'transcript' | 'ai'
let hudVisible = true; // template -> let activePanel: 'transcript' | 'ai' = 'ai';
export function setHudVisible(v: boolean): void; export function getHudVisible(): boolean;
function buildStatus(window): IOverlayStatus { return { /* ... */ hudVisible }; } // carry activePanel too
export const SCROLL_TRANSCRIPT_CHANNEL = 'jedi:scroll-transcript';
export function pushScrollTranscript(window, direction: 'up' | 'down'): void; // existing
```
From src/preload/index.ts: IOverlayStatus is declared locally (add `activePanel`); onScrollTranscript already exists.
From src/renderer/src/components/debug-hud.tsx: onScrollTranscript handler scrolls transcriptRef when active; must now guard on `status.activePanel === 'transcript'`.
From src/renderer/src/components/ai-panel.tsx (05-01): owns its own scroll ref + stickToBottomRef; must subscribe to onScrollTranscript and scroll only when `status.activePanel === 'ai'`.
From src/main/ai/ai-orchestrator.ts (05-01): trigger(mode) already aborts current before starting on cross-mode (D-07); add latency timing here.
From src/main/hotkey-registrar.service.ts: scroll rows are `scroll-transcript-up`/`-down` (Ctrl+Alt+PageUp/PageDown, kind 'repeat'); focus-cycle row to add.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: activePanel flag + Ctrl+Alt+F focus-cycle + corner indicator + active-panel-routed scroll</name>
  <files>src/main/overlay-window.manager.ts, src/main/hotkey-registrar.service.ts, src/main/index.ts, src/preload/index.ts, src/renderer/src/components/debug-hud.tsx, src/renderer/src/components/ai-panel.tsx, src/renderer/src/assets/hud.css</files>
  <read_first>
    - src/main/overlay-window.manager.ts (the hudVisible flag triplet lines 92-116 + buildStatus line 153-163 + IOverlayStatus lines 13-30 — the exact activePanel template)
    - src/main/hotkey-registrar.service.ts lines 53-76 (HOTKEY_CHORDS; scroll rows are 'repeat')
    - src/main/index.ts buildHandlers (the getOverlayVisible()-style main-owned-flag branch for show/hide is the focus-cycle template; scroll-transcript-up/down handlers call pushScrollTranscript)
    - src/preload/index.ts (IOverlayStatus declared locally — add activePanel; onScrollTranscript exists)
    - src/renderer/src/components/debug-hud.tsx (onScrollTranscript handler + IOverlayStatus local decl + stickToBottomRef)
    - src/renderer/src/components/ai-panel.tsx (its scroll ref + onStatus subscription from 05-01)
  </read_first>
  <action>
    `overlay-window.manager.ts`: add `activePanel: 'transcript' | 'ai'` to `IOverlayStatus` (and the matching field where the renderer mirrors it); add `let activePanel: 'transcript' | 'ai' = 'ai';` (D-08 default = AI panel), `export function setActivePanel(panel)` / `export function getActivePanel()`, and carry `activePanel` in `buildStatus()`. Mirror the hudVisible triplet exactly.
    `hotkey-registrar.service.ts`: add `{ label: 'focus-cycle', keycode: UiohookKey.F, accelerator: 'Ctrl+Alt+F', kind: 'discrete' }` to HOTKEY_CHORDS (F outside the locked set — conflict-test in Task 2; fallback reserved).
    `index.ts` buildHandlers: add `'focus-cycle': () => { setActivePanel(getActivePanel() === 'ai' ? 'transcript' : 'ai'); pushStatus(window); }` (mirror the getOverlayVisible() branch). The existing `scroll-transcript-up`/`-down` handlers stay calling `pushScrollTranscript(window, 'up'|'down')` — routing is now decided in the renderer by the active-panel flag (keeps one scroll channel; D-08).
    `preload/index.ts`: add `activePanel` to the locally-declared `IOverlayStatus`.
    `debug-hud.tsx`: add `activePanel` to its local IOverlayStatus; guard the `onScrollTranscript` handler so it only scrolls the transcript element when `status?.activePanel === 'transcript'`.
    `ai-panel.tsx`: subscribe to `onScrollTranscript` (reuse the debug-hud scroll-step + stickToBottomRef re-arm pattern) and scroll the AI panel element only when `status?.activePanel === 'ai'`. Render a small corner active-panel indicator (e.g. `data-testid="icon-active-panel"` showing `AI` / `Transcript`) keyed off the pushed flag. Add indicator CSS to `hud.css`.
    `debug-hud.tsx`: update the cheat-sheet — add `Focus panel` (Ctrl+Alt+F) and change the `Scroll` label to `Scroll active panel`.
  </action>
  <verify>
    <automated>npx vitest run && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (no regressions)
    - `grep -c "activePanel" src/main/overlay-window.manager.ts` is >= 4 (interface field, flag, set, get, buildStatus)
    - overlay-window.manager.ts contains `let activePanel: 'transcript' | 'ai' = 'ai'`
    - hotkey-registrar.service.ts contains `label: 'focus-cycle'` with `accelerator: 'Ctrl+Alt+F'`
    - `grep -c "setActivePanel" src/main/index.ts` is 1
    - ai-panel.tsx contains `activePanel` (scroll guard) and `icon-active-panel`
    - `npm run lint && npm run format:check` exit 0
  </acceptance_criteria>
  <done>activePanel flag (default 'ai') pushed in status; Ctrl+Alt+F cycles it; corner indicator renders; PgUp/PgDn scroll the active panel; both panels guard scroll on the flag.</done>
</task>

<task type="auto">
  <name>Task 2: Hotkey-to-first-token latency logging (main log only)</name>
  <files>src/main/ai/ai-orchestrator.ts</files>
  <read_first>
    - src/main/ai/ai-orchestrator.ts (trigger(mode) — where the stream starts and the first 'text'/'delta' fires; the request-id state)
    - src/main/index.ts lines 131-135 (the Deepgram error handler that deliberately does NOT log a payload — the log-discipline precedent)
  </read_first>
  <action>
    In `ai-orchestrator.ts`, record a monotonic start timestamp when `trigger(mode)` begins a stream (after the empty-span guard passes). On the FIRST text delta for that requestId, compute `Date.now() - startMs` and emit ONE main-log line via `console.log` (or the project's main logger if one exists) of the form `[ai] first-token mode=<mode> model=<model> latencyMs=<n>`. Log nothing key-adjacent and never push latency to the renderer (D-10 latency watch is a main-log-only concern). Use the requestId guard so a cancelled stream's late first-delta does not log a misleading latency for the new stream.
    Confirm cross-mode cancel-current-start-new (D-07) is intact: pressing the other mode mid-stream aborts the current and starts the new — this was wired in 05-01's orchestrator; verify the latency timer resets per requestId so the new stream gets its own measurement.
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/ai-orchestrator.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/ai/ai-orchestrator.test.ts` exits 0
    - ai-orchestrator.ts contains `first-token` and `latencyMs` in a log line
    - `grep -L "apiKey\|x-api-key\|process.env.ANTHROPIC" src/main/ai/ai-orchestrator.ts` lists the file (no key-adjacent logging)
    - latency timing is keyed on the request id (the existing requestId guard is reused)
  </acceptance_criteria>
  <done>Hotkey-to-first-token latency is logged once per stream to the main log only, keyed on requestId; cross-mode cancel resets the timer; no renderer push, no key logging.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Keyboard scrollback under the focused-panel model</name>
  <files>(manual on-machine verification — no files modified by this task)</files>
  <action>This is a human-verify checkpoint. The executor has already automated all build work in the prior tasks; pause here and present the how-to-verify steps to the user, then wait for the resume signal. Do not proceed until approved.</action>
  <what-built>Keyboard scrollback under the focused-panel model: Ctrl+Alt+F cycles the active panel (AI default), a corner indicator shows it, Ctrl+Alt+PgUp/PgDn scroll the active panel, cross-mode cancel-current-start-new, and main-log latency lines for both modes.</what-built>
  <how-to-verify>
    1. `npm run dev`. Confirm a corner indicator shows `AI` as the active panel on launch (D-08 default).
    2. Generate an AI answer longer than the visible panel. Press `Ctrl+Alt+PgUp`/`PgDn` — confirm the AI panel scrolls and the full response is readable; scrolling to the bottom re-arms auto-follow (AI-05).
    3. Press `Ctrl+Alt+F` — confirm the indicator flips to `Transcript`; now `Ctrl+Alt+PgUp`/`PgDn` scroll the transcript inside the HUD, not the AI panel (D-08 routing). Press `Ctrl+Alt+F` again to return to `AI`.
    4. Run the 02-03 conflict protocol for `Ctrl+Alt+F` against Teams/Zoom/VS Code (append to 05-HOTKEY-CONFLICT-TEST.md). Pick the reserved fallback letter if it collides.
    5. Press `Ctrl+Alt+A`, then `Ctrl+Alt+T` mid-stream — confirm the answer entry stops (no more tokens) and a new talking-points entry begins; no concurrent output interleaving (D-07).
    6. Check the dev/main console: confirm one `[ai] first-token ... latencyMs=<n>` line per press, for both Haiku (answer) and Opus (talking points). Judge whether Opus first-token feels live enough; if not, note the re-tier decision (D-10) — the per-mode model constant in ai-orchestrator.ts can be flipped to Haiku.
  </how-to-verify>
  <resume-signal>Type "approved" with the F-chord conflict result and the observed Haiku/Opus first-token latencies (and any re-tier decision), or describe what failed.</resume-signal>
  <verify><human-check>Complete the how-to-verify steps on the target machine; resume only on "approved".</human-check></verify>
  <done>User typed "approved" after completing every how-to-verify step (or reported and resolved issues).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (status) | activePanel flag pushed read-only; renderer renders the indicator + routes scroll. No control channel back (IN-01). |
| OS hotkey layer → app | Ctrl+Alt+F must not consume the meeting app's accelerators. |
| main log | Latency lines written to the main log only — never the renderer, never key-adjacent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-09 | Tampering / availability | Ctrl+Alt+F collision | mitigate | 02-03 conflict protocol for F; reserved fallback letter; passive uiohook non-consumption (CTL-02). |
| T-5-10 | Information Disclosure | Latency log line | mitigate | Log only `mode`, `model`, `latencyMs` — no transcript text, no key, no error payload (mirror index.ts:131-135 discipline). Main log only, never the renderer. |
| T-5-11 | Tampering / resource leak | Cross-mode cancel cleanliness | mitigate | requestId guard (inherited from 05-01) ensures the aborted stream's late first-delta does not log/append for the new stream; single-in-flight invariant holds (D-07). Verified live (step 5). |
| T-5-12 | Elevation of Privilege | Renderer scroll routing | accept | Scroll routing is a pure view of the pushed activePanel flag; the renderer decides which element to scroll but never controls main state (IN-01). |
</threat_model>

<verification>
- `npx vitest run` full suite green (orchestrator latency timing keyed on requestId)
- `npm run lint && npm run format:check`
- Latency log discipline: `grep -L "x-api-key\|apiKey" src/main/ai/ai-orchestrator.ts`
- On-machine human-verify checkpoint above (live scrollback, focus-cycle, cross-mode cancel, latency feel — all manual-only; overlay is focusable:false)
</verification>

<success_criteria>
- AI-05: long responses are fully readable via Ctrl+Alt+PgUp/PgDn under the focused-panel model (live verify step 2-3).
- D-08: activePanel default 'ai', Ctrl+Alt+F cycles, corner indicator, active-panel-routed scroll (steps 1-3).
- D-07: cross-mode cancel-current-start-new, no interleaving (step 5).
- D-10: hotkey-to-first-token latency logged to the main log for both modes; re-tier decision informed (step 6).
</success_criteria>

<output>
Create `.planning/phases/05-ai-orchestration-answer-talking-points/05-03-SUMMARY.md` when done.
</output>
