---
phase: 05-ai-orchestration-answer-talking-points
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/main/ai/prompt-assembler.test.ts
  - src/main/ai/prompt-assembler.ts
  - src/main/ai/ai-history.ts
  - src/main/hotkey-registrar.service.ts
  - src/main/index.ts
  - src/renderer/src/components/debug-hud.tsx
  - .planning/phases/05-ai-orchestration-answer-talking-points/05-HOTKEY-CONFLICT-TEST.md
autonomous: false
requirements: [AI-02]
must_haves:
  truths:
    - "D-05/D-10/D-12: Pressing Ctrl+Alt+T produces 3-5 talking-point bullets about the project work in the recent transcript span, via the Talking-points system prompt routed to claude-opus-4-8 through its per-mode named model constant"
    - "D-02: A clear-AI-history chord empties the AI panel and pushes an emptied snapshot"
    - "D-05: Ctrl+Alt+A, Ctrl+Alt+T, and the clear-AI chord all register (none in register().failed) and are conflict-tested against Teams/Zoom/VS Code"
    - "D-09: Both modes read the same ~60s recent finalized window via the RECENT_SPAN_MS named constant"
  artifacts:
    - path: ".planning/phases/05-ai-orchestration-answer-talking-points/05-HOTKEY-CONFLICT-TEST.md"
      provides: "On-machine 02-03-protocol conflict results for the new chords + any fallback letters"
      contains: "Ctrl+Alt+T"
    - path: "src/main/hotkey-registrar.service.ts"
      provides: "ai-talking-points + clear-ai chord rows in HOTKEY_CHORDS"
      contains: "ai-talking-points"
  key_links:
    - from: "src/main/index.ts"
      to: "aiOrchestrator.trigger('talking-points')"
      via: "ai-talking-points handler in buildHandlers"
      pattern: "trigger\\('talking-points'\\)"
    - from: "src/main/index.ts"
      to: "aiHistory.clear()"
      via: "clear-ai handler pushing an emptied snapshot"
      pattern: "aiHistory\\.clear\\(\\)"
---

<objective>
Complete the second AI mode as a vertical slice: pressing **Ctrl+Alt+T** (Talking points) produces 3-5 bullets about the project work in the recent transcript, routed to `claude-opus-4-8`. Add the clear-AI-history chord, finalize the new chord set, and run the 02-03 on-machine conflict test against Teams/Zoom/VS Code with documented fallbacks. After this plan a user can get both an answer AND talking points by hotkey (AI-01 + AI-02), and clear the AI panel.

Purpose: AI-02 (talking points) plus the full mode/clear chord surface, conflict-verified on the target machine.

Output: talking-points prompt assertions green; the `ai-talking-points` and `clear-ai` chords wired and conflict-tested; `05-HOTKEY-CONFLICT-TEST.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-CONTEXT.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-RESEARCH.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-PATTERNS.md
@.planning/phases/02-global-hotkeys-window-control/02-HOTKEY-CONFLICT-TEST.md
@.planning/phases/05-ai-orchestration-answer-talking-points/05-01-SUMMARY.md

<interfaces>
<!-- From 05-01 (already built). -->
From src/main/ai/prompt-assembler.ts:
```typescript
export const ANSWER_SYSTEM_PROMPT: string;
export const TALKING_POINTS_SYSTEM_PROMPT: string;
export const RECENT_SPAN_MS = 60_000;
export function assemblePrompt(input: { mode: AiMode; span: string; context?: IGroundingContext }): { system: string; userContent: string };
```
From src/main/ai/ai-orchestrator.ts:
```typescript
public trigger(mode: AiMode): void; // 'talking-points' uses TALKING_POINTS_MODEL = 'claude-opus-4-8'
```
From src/main/ai/ai-history.ts:
```typescript
public clear(): void;
public snapshot(): IAiEntry[];
```
From src/main/hotkey-registrar.service.ts (HOTKEY_CHORDS row template — clear-transcript at lines 63-69):
```typescript
{ label: 'clear-transcript', keycode: UiohookKey.K, accelerator: 'Ctrl+Alt+K', kind: 'discrete' }
```
From src/main/index.ts buildHandlers — 'clear-transcript' calls `buffer.clear()` + `pushTranscript(window, {...})`; mirror for clear-ai with `aiHistory.clear()` + `pushAi(window, { type: 'history-snapshot', ... })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Talking-points prompt assertions + clear-AI-history wiring</name>
  <files>src/main/ai/prompt-assembler.test.ts, src/main/ai/prompt-assembler.ts, src/main/ai/ai-history.ts, src/main/hotkey-registrar.service.ts, src/main/index.ts, src/renderer/src/components/debug-hud.tsx</files>
  <read_first>
    - src/main/ai/prompt-assembler.test.ts (the 05-01 stub — add talking-points assertions)
    - src/main/ai/prompt-assembler.ts (TALKING_POINTS_SYSTEM_PROMPT, assemblePrompt)
    - src/main/ai/ai-history.ts (clear() / snapshot())
    - src/main/index.ts buildHandlers + the 'clear-transcript' handler (the clear template)
    - src/main/hotkey-registrar.service.ts lines 53-76 (HOTKEY_CHORDS)
    - .planning/phases/05-ai-orchestration-answer-talking-points/05-RESEARCH.md (DRAFT talking-points prompt lines 418-429)
  </read_first>
  <behavior>
    - assemblePrompt with mode `'talking-points'` selects `TALKING_POINTS_SYSTEM_PROMPT` and embeds the same labeled span; the prompt instructs 3-5 one-line bullets prefixed `- ` (D-12).
    - clear-ai handler empties AiHistory and pushes an emptied `history-snapshot` so the panel clears.
  </behavior>
  <action>
    Extend `prompt-assembler.test.ts` with talking-points cases: mode `'talking-points'` → `TALKING_POINTS_SYSTEM_PROMPT` selected, userContent still embeds the labeled span, `context: undefined` still yields an empty context block (D-13 holds for both modes). Confirm the DRAFT talking-points wording in prompt-assembler.ts matches D-12 (3-5 short bullets, project-work focus, `- ` prefix); refine wording if the assertion forces it.
    `hotkey-registrar.service.ts`: add two rows to HOTKEY_CHORDS — `{ label: 'ai-talking-points', keycode: UiohookKey.T, accelerator: 'Ctrl+Alt+T', kind: 'discrete' }` and `{ label: 'clear-ai', keycode: UiohookKey.G, accelerator: 'Ctrl+Alt+G', kind: 'discrete' }` (G chosen as the clear-AI letter; planner reserves a fallback if Task 2 finds a collision). Inline-comment the pending-conflict-test note like the clear-transcript row.
    `index.ts` buildHandlers: add `'ai-talking-points': () => aiOrchestrator.trigger('talking-points')` and `'clear-ai': () => { aiHistory.clear(); pushAi(window, { type: 'history-snapshot', entries: aiHistory.snapshot() }); }` (mirror the clear-transcript handler shape). Ensure `aiHistory` is in scope at the handler-build site.
    `debug-hud.tsx`: add cheat-sheet rows for `Answer` (Ctrl+Alt+A), `Talking points` (Ctrl+Alt+T), and `Clear AI` (Ctrl+Alt+G) to HOTKEY_CHEAT_SHEET.
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/prompt-assembler.test.ts && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/ai/prompt-assembler.test.ts` exits 0 (answer + talking-points cases)
    - hotkey-registrar.service.ts contains `label: 'ai-talking-points'` with `accelerator: 'Ctrl+Alt+T'` and `label: 'clear-ai'`
    - `grep -c "trigger('talking-points')" src/main/index.ts` is 1
    - `grep -c "aiHistory.clear()" src/main/index.ts` is 1
    - `npm run lint && npm run format:check` exit 0
  </acceptance_criteria>
  <done>Talking-points prompt assembly verified; the ai-talking-points and clear-ai chords are wired through buildHandlers; cheat-sheet updated; lint/format green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: The full new chord set (Ctrl+Alt+A Answer, Ctrl+Alt+T Talking points, Ctrl+Alt+G Clear-AI) wired through Hot</name>
  <files>(manual on-machine verification — no files modified by this task)</files>
  <action>This is a human-verify checkpoint. The executor has already automated all build work in the prior tasks; pause here and present the how-to-verify steps to the user, then wait for the resume signal. Do not proceed until approved.</action>
  <what-built>The full new chord set (Ctrl+Alt+A Answer, Ctrl+Alt+T Talking points, Ctrl+Alt+G Clear-AI) wired through HotkeyRegistrarService, plus the talking-points end-to-end path. This checkpoint runs the 02-03 conflict-test protocol and verifies talking points live.</what-built>
  <how-to-verify>
    1. `npm run dev`. Confirm the HUD `Hotkeys` row shows `OK` (no entries in register().failed) — i.e. A/T/G all bound.
    2. Run the 02-03 protocol from `02-HOTKEY-CONFLICT-TEST.md` for Ctrl+Alt+A, Ctrl+Alt+T, Ctrl+Alt+G with Microsoft Teams, Zoom, and VS Code each holding focus in turn: press each chord and confirm (a) the overlay action fires and (b) the focused app's own Ctrl+Alt accelerators still work (passive uiohook non-consumption, CTL-02). Record results in a new `05-HOTKEY-CONFLICT-TEST.md`. If any chord collides, pick a documented fallback letter (the planner reserved this), update HOTKEY_CHORDS + the cheat-sheet, and re-test.
    3. With a project-work discussion in the live transcript, press `Ctrl+Alt+T`. Confirm a new entry headed `Talking points` streams in 3-5 short bullets, each on its own line (D-12), and that it feels reasonably live (note first-token latency for the 05-03 latency watch — Opus may be re-tiered if too slow).
    4. Press `Ctrl+Alt+G`. Confirm the AI panel empties (all entries cleared).
  </how-to-verify>
  <resume-signal>Type "approved" with the conflict-test outcome (and any fallback letters used), or describe what failed.</resume-signal>
  <verify><human-check>Complete the how-to-verify steps on the target machine; resume only on "approved".</human-check></verify>
  <done>User typed "approved" after completing every how-to-verify step (or reported and resolved issues).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → Anthropic API | Talking-points call sends the same transcript span; Opus tier. Accepted v1 privacy posture. |
| OS hotkey layer → app | New Ctrl+Alt chords must not consume the meeting app's accelerators (passive uiohook). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-06 | Tampering / availability | New chords (A/T/G) colliding with Teams/Zoom/VS Code | mitigate | 02-03 on-machine conflict protocol re-run for each new chord; documented fallback letters reserved; results recorded in 05-HOTKEY-CONFLICT-TEST.md. Passive uiohook hook never consumes the focused app's accelerators (CTL-02). |
| T-5-07 | Information Disclosure | Talking-points API key/error | mitigate | Reuses the 05-01 AnthropicGateway; key stays main-only constructor-injected; error payload never logged (inherited T-5-01/T-5-02). |
| T-5-08 | Denial of Service / cost | Opus talking-points cost | accept | User-triggered only; same bounded ~60s span; empty-span guard (D-11) inherited from the orchestrator. Opus re-tier to Haiku reserved if latency/cost warrants (D-10). |
</threat_model>

<verification>
- `npx vitest run` full suite green (answer + talking-points prompt cases)
- `npm run lint && npm run format:check`
- HUD `Hotkeys: OK` (A/T/G registered) — live
- 02-03 conflict protocol documented in 05-HOTKEY-CONFLICT-TEST.md (manual-only — OS chord capture is not unit-testable)
</verification>

<success_criteria>
- AI-02: Ctrl+Alt+T yields 3-5 talking-point bullets from the recent transcript via Opus (live verify step 3).
- D-02 clear path: Ctrl+Alt+G empties the AI panel (step 4).
- New chords register and are conflict-tested with documented fallbacks (steps 1-2).
- D-09: both modes share RECENT_SPAN_MS (asserted in prompt-assembler test + orchestrator).
</success_criteria>

<output>
Create `.planning/phases/05-ai-orchestration-answer-talking-points/05-02-SUMMARY.md` when done.
</output>
