# Dependencies
node_modules/

# Build output
out/
dist/
*.tsbuildinfo

# electron-builder packaging output
release/

# Logs
*.log
npm-debug.log*

# Local electron-store data (lives in userData, but guard against any local copy)
*.json.bak
config.json.tmp

# Local dev secrets (D-08): the Deepgram API key is sourced from an env var or a local
# untracked dev-config read in main only — never committed (honors SET-03). Phase 6's
# safeStorage-backed entry replaces this source without touching the gateway.
.env
.env.*
# Keep the committed, secret-free template that documents which keys to set locally.
!.env.example
*.local
secrets.local.json
dev-config.local.json

# Throwaway capture go/no-go spike scaffolding (Task 1, OQ-1) — never part of the bootstrap.
capture-spike.scratch.cjs
src/main/audio/capture-spike.scratch.ts
*.scratch.cjs
*.scratch.ts
*.scratch.js

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
